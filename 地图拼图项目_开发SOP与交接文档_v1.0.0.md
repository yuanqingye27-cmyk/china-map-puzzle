# 地图拼图项目 · 开发 SOP 与交接文档（v1.0.0）

> **文件名变更**：本文件原名 `成都拼图项目开发SOP与经验复盘.md`，
> 在 v1.0.0 交付时重命名为 `地图拼图项目_开发SOP与交接文档_v1.0.0.md`
> —— 因为它已经从"成都一个城市的复盘"长成了"多层级地图工厂 + 合规数据源 + 交接说明"。
>
> **怎么用这份文档**：
> - 想接手继续做 → 直接看 **附录 A**（本轮复盘 / 遗留清单 / 明天第一件事）
> - 想加地图 → **5.11 批量地图操作手册**
> - 想懂数据来源与合规 → **5.12 地图合规与数据来源**
> - 想懂地图包规范 → **3.5 世界地图层级规范**
> - 想避坑 → **四、踩坑全记录**（28 条，每条都有现象/根因/修法/教训）

> 项目：地图拼图（用交互式拼图讲解行政区划；已接入中国 / 四川 / 成都 / 自贡四级地图）
> 形态：零依赖纯前端单页，双击 `index.html` 即可运行
> 架构：**通用引擎 `MapPuzzleEngine` + 地图工厂** —— 加一张地图只需一条命令 + 补文案，不动引擎
> 本文用途：记录架构决策、踩坑过程、解决方法，以及下次做同类项目可复用的流程

---

## 目录

- [一、项目概况](#一项目概况)
- [二、开发历程](#二开发历程)
- [三、最终架构方案](#三最终架构方案)
- [四、踩坑全记录](#四踩坑全记录)
- [五、开发 SOP](#五开发-sop)
- [六、文件目录结构](#六文件目录结构)
- [七、已知限制](#七已知限制)
- [八、几条最值钱的经验](#八几条最值钱的经验)

---

## 一、项目概况

| 项 | 值 |
| --- | --- |
| 目标 | 用拼图游戏讲解行政区划；已抽成"通用引擎 + 地图工厂"，可自动接入无限多张地图，页面上可直接切换 |
| 技术栈 | 原生 HTML / CSS / JavaScript，**无任何依赖、无任何外链** |
| 数据 | 国家地理信息公共服务平台（天地图）· 服务中心行政区划，审图号 GS(2024)0650号；中国（34 省级）/ 四川（21 市州）/ 21 个市各自的区县，**共 23 张地图** |
| 运行方式 | 双击 `index.html`（`file://`），无需构建、无需服务器 |
| 代码规模 | `engine.js` 1554 行（通用逻辑）、`game.js` 208 行（启动器 + 导航 UI）、`loader.js` 141 行（按需加载）、`add-map.js` 620 行（单张接入）、`batch-add-maps.js` 320 行（批量接入）、`style.css` 1695 行、`index.html` 431 行 |
| 首屏体积 | **75 KB**（registry + loader + geomap + engine + game）；23 张地图的数据按需注入，从不全量下载 |
| 测试 | **882 项断言**（89 城市回归 + 77 引擎功能 + 585 多地图冒烟/导航/窄屏 + 13 项 WKT→GeoJSON 单测 + 118 项地图包自洽）+ 1 项 CSS 静态检查，全部通过 |

**为什么坚持零依赖**：这是一份"送给别人也能直接打开"的小作品。任何 `npm install`、CDN、构建步骤都会成为分享时的摩擦点。事后看这个决定是正确的 —— 它倒逼出了几个有意思的实现（手写墨卡托、内联数据、自己搭测试），也避开了所有网络相关的坑。

---

## 二、开发历程

整个过程分了 6 个阶段，中间穿插了 3 轮独立的功能追加。

### 阶段 0 · 需求确认（没有直接动手）

原始需求里混了一句"比如快速排序或 TCP 三次握手"，明显是模板残留，与成都地图无关。没有直接猜测，而是**先用三个问题把关键分歧点问清楚**：

1. 玩法：拖拽 / 点击 / 两者都要 → 选了**拖拽**
2. 难度：20 个一次上 / 分关卡 → 选了**分关卡**（中心城区 / 近郊 / 远郊）
3. 拼对后弹不弹介绍 → 选了**要**

**经验**：需求里出现"和主题无关的示例"时，先质疑再动手，比做完了返工便宜得多。

### 阶段 1 · 核心实现

- 拉取并精简 GeoJSON → 内联成 `js/map-data.js`
- 手写墨卡托投影 + path/bbox/质心计算（`js/geomap.js`）
- 关卡系统、拖拽、落点判定、信息卡（`js/game.js`）
- 深色主题样式（`css/style.css`）
- **搭起测试体系**（这一步的价值在后面反复体现）

### 阶段 2 · 方向修复（用户报的第一个 bug）

用户指出"地图方向是反的"。我一开始的注释里信誓旦旦写着"方向正确"，用数据一验 —— 用户是对的，整张图南北颠倒。详见[坑 #5](#坑-5地图南北颠倒)。

### 阶段 3 · 视觉升级

- 手绘 SVG 图案库：熊猫、竹子、银杏、盖碗茶、川剧脸谱 + 6 个功能图标
- 配色从"深墨蓝"改为"青绿竹韵"，颜色收口成**通道变量**
- 屏幕适配（这一轮踩了 3 个布局坑）

### 阶段 4 · 三套主题 + 脸谱重设计

- 青绿竹韵 / 银杏暖秋 / 蜀锦朱砂
- 川剧脸谱重画（第一版太"线描"，不像脸谱）
- 顺手发现"逐关解锁"从未生效（[坑 #13](#坑-13numbernull-等于-0-导致全部解锁)）

### 阶段 5 · 开场动画 + 结算画面

- 开场动画：五个成都元素依次弹入
- 结算画面：星级评价 + 数字滚动 + 区县印记

### 阶段 6 · 工程化收尾

- 进度持久化、音效、无障碍、移动端性能降级
- 发现并修复最隐蔽的一个 bug：横向溢出（[坑 #12](#坑-12min-width-auto-撑破容器移动端横向溢出的真凶)）

### 阶段 7 · 引擎化重构（支持多城市）

四步走，每步都跑测试、每步都停下来验收：

1. **抽出城市配置**（`js/cities/chengdu.js`）：把主色调 `LEVEL_HUE`、存储 key、画布/碎片尺寸、文案收进一个 `CONFIG` 对象 —— 纯新增，零行为变化
2. **引擎参数化**：`game.js` 里的硬编码常量改为从 `CONFIG` 读，缺项一律有默认值兜底
3. **拆出 `js/engine.js`**：主体改名为 `MapPuzzleEngine.create(CONFIG)`（`git mv` 保历史、正文逐行不动，只改首尾包装），`game.js` 缩成 34 行启动器
4. **双测试体系**：新增假数据 fixture（3 个虚构区县）+ 瘦宿主页 + 引擎功能套件，驱动改成一次跑两套

**这一步的意外收获**：假数据一上来就把引擎的**输入契约太窄**逼了出来 —— 几何层只认 `MultiPolygon`，喂标准 `Polygon` 直接抛错（见[坑 #22](#坑-22几何层只认-multipolygonpolygon-直接抛错)）。

---

## 三、最终架构方案

### 3.1 分层

```
index.html
├── <svg class="svg-sprite">      图案库：12 个 <symbol> 定义一次
├── 页面结构（顶栏 / 地图板 / 托盘 / 侧栏 / 弹窗 / 开场层）
└── <script> 顺序加载 6 个文件

css/style.css
├── :root 设计令牌（通道变量）
├── 两套主题覆盖（data-theme）
├── 组件样式
└── 5 个媒体查询断点

【数据层】与引擎无关的原始数据，换城市就换这几个文件
js/map-data.js         成都 GeoJSON（构建产物，内联）
js/districts.js        20 个区县的资料卡 + 关卡设定

【配置层】一座城市 = 一份配置：把数据 + 配色 + 存储 key + 文案组装起来
js/cities/chengdu.js   window.MAP_PUZZLE_CONFIG

【引擎层】不含任何城市数据，只认传进来的 CONFIG
js/geomap.js           投影与几何：经纬度 → path / bbox / 质心（Polygon / MultiPolygon 都吃）
js/engine.js           MapPuzzleEngine：凹槽 / 碎片 / 拖拽 / 落点判定 / 存档 / 通关

【启动层】
js/game.js             34 行：把配置交给引擎并 start()
```

### 3.2 六个关键设计决策

#### ① 数据内联成 `.js`，不用 `.json`

`file://` 下 `fetch` 本地 JSON 会被 CORS 拦死，但 `<script>` 标签不受影响。所以构建脚本把 GeoJSON 处理成 `window.CHENGDU_GEO = {...}`。**这是"双击即玩"能成立的前提。**

#### ② 手写墨卡托，不引 d3

用到的地理计算只有"经纬度 → 平面坐标"一件事，20 行够了：

```js
function project(lon, lat) {
  return [lon * DEG, -Math.log(Math.tan(Math.PI / 4 + (lat * DEG) / 2))];
}
```

**那个负号是整个项目的命门**，详见[坑 #5](#坑-5地图南北颠倒)。

#### ③ 碎片和地图共用同一份 path

碎片 `<svg>` 的 `viewBox` 直接设为该区县的 `bbox`：

```html
<svg viewBox="120 340 180 150"><path d="（全局坐标的 path）"/></svg>
```

好处是**落位动画变得极其简单**：幽灵层从托盘尺寸 `scale` 到地图上的真实尺寸时，因为两者共用同一份 path 和同一个 bbox，缩放后必然严丝合缝地重合 —— 看起来就是碎片"嵌"了进去。

#### ④ 拖拽用 Pointer Events，不用 Mouse/Touch 双份

单套 `pointerdown/move/up` 同时覆盖鼠标、触屏、手写笔。配合 `.piece { touch-action: none }` 阻止移动端滚动。**这一条让"移动端适配"从"需要重构"变成了"只需验证"。**

#### ⑤ 落点判定分两层

```js
// 第一层：精确 —— 点是否落在凹槽多边形内部
slot.path.isPointInFill(p)
// 第二层：容错 —— 离最近的凹槽中心够近吗
dist < clamp(min(w,h) * 0.85, 34, 110)
```

坐标换算走 `getScreenCTM().inverse()`，因此**地图 viewBox 怎么变都不影响判定**（这一条支撑了后面的"视图聚焦"）。

#### ⑥ 颜色用通道变量

```css
--c-jade: 63 201 156;              /* 定义：R G B 三段 */
color: rgb(var(--c-jade) / 0.14);  /* 使用：透明度在调用处决定 */
```

全站 40+ 处颜色引用都走变量，换主题只需给 `<html>` 换个 `data-theme`。**代价是变量名成了"角色代号"**（`--c-jade` 在暖秋主题里装的是金色），代码里已注明。

### 3.3 自建测试体系（这是最值得保留的资产）

因为**没有 npm、不装 puppeteer**，测试是这样搭起来的：

```
tools/e2e-test.js        Node 侧：起 HTTP 服务 → 按套件启动 headless Chrome → 收结果 → 汇总
      ↓ 依次打开两个载体页（每套独立 Chrome、独立 user-data-dir）
tools/selftest.html      【城市回归】装 index.html 进 iframe，用成都真实数据 + 真实 DOM 断言
tools/engine-test.html   【引擎功能】装 engine-host.html 进 iframe，只喂虚构 tiny-city 数据
      ↓ iframe 内
index.html / engine-host.html      被测页面
      ↓ 结果用隐表单 POST 回传（绕开 CORS 和 PNA）
tools/e2e-test.js        收到结果 → 打印 / 设置退出码
```

两套测试的分工（重构后新增的一层保险）：

| 套件 | 被测页 | 数据 | 管什么 |
| --- | --- | --- | --- |
| 城市回归 · 89 项 | `index.html` | 成都真实 GeoJSON | UI / 动画 / 布局 / 真实数据正确性（含 adcode 没写反） |
| 引擎功能 · 77 项 | `engine-host.html` | 虚构 tiny-city（3 个假区县） | 放置判定、错误拒绝、解锁、持久化、配置驱动 —— **与具体城市无关** |

**为什么要分成两套**：引擎改动后，用假数据那一套就能验证通用逻辑，不必依赖成都地图的细节；而 UI 回归由城市那一套守着，两边互不干扰。实测收益见[坑 #22](#坑-22几何层只认-multipolygonpolygon-直接抛错)：假数据第一次运行就逮住了真实数据掩盖了一个项目周期的兼容问题。

**为什么不用 CDP**：headless Chrome 153 在 `Runtime.enable` 时直接 SIGTRAP 崩溃，试了 `--headless=old`、`--no-sandbox` 各种组合都不行，果断放弃（详见[坑 #2](#坑-2headless-chrome-在-runtimeenable-时崩溃)）。

**这个体系的价值在后续每一轮改动里都体现了** —— 它逮住了 4 个我自己没意识到的 bug（含两个静默错误）。

### 3.4 数据 / 配置 / 引擎 三层解耦

重构的核心一句话：**引擎不认识任何一个具体城市。**

```
js/cities/<city>.js  ──┐
  geo        地图数据   │  window.MAP_PUZZLE_CONFIG
  districts  区县资料   │  （一个纯数据对象，没有任何行为）
  levels     关卡设定   │
  palette    配色       │
  storage    存储 key   │
  texts      文案     ──┘
                        ↓
              MapPuzzleEngine.create(CONFIG).start()
                        ↓
  引擎负责：底图 / 凹槽 / 碎片 / 拖拽 / 落点判定 / 提示 / 存档 / 通关
```

`CONFIG` 字段表 —— **只有 `geo` 和 `levels` 是必需的**，其余省略时走引擎内置默认值：

| 字段 | 必需 | 说明 |
| --- | --- | --- |
| `geo` | ✔ | GeoJSON FeatureCollection；每个 feature 需带 `properties.adcode` / `properties.name`。`Polygon` 与 `MultiPolygon` **两种写法都支持** |
| `levels` | ✔ | `[{ id, name, short, blurb, adcodes, hue? }]`；`hue` 可选，作为该关主色相 |
| `districts` | | `adcode → { area, landmark, tagline, funFact }`，拼对后信息卡的内容 |
| `palette` | | `hueByLevel` / `fallbackHue` / `saturation` / `lightBase` / `lightStep` / `lightSpan` / `hueSpread` —— **城市主色调就在这里** |
| `map` | | `{ width, padding }` 画布逻辑尺寸 |
| `piece` | | `{ max, minSide, pieces: [[视口宽度上限, 最大边], …] }` 碎片尺寸档位 |
| `storage` | | `{ save, theme, sound, intro, saveVersion }` ① |
| `themes` | | `{ list, fallback }`，`list` 里是 CSS 里存在的 `data-theme` 值 |
| `texts` | | `{ cityName, districtCount, missingDataHint }` |

① 多城市**必须**各用一套 `storage` key，否则两个城市会互相覆盖存档。

引擎的对外接口只有两个（刻意收得很窄，免得宿主页面改坏内部状态）：

```js
const engine = MapPuzzleEngine.create(CONFIG);
engine.start();      // 数据与 DOM 就绪后启动（引擎不自动启动，何时启动由宿主页决定）
engine.getState();   // 运行状态快照：levelIndex / placed / tries / hints / elapsed /
                     // solved / unlocked / finishedLevels / slotsLeft / piecesLeft …
```

**为什么用工厂函数而不是 class**：内部 `state` / `drag` / `el` / `shapes` 全是 `create()` 的闭包变量，天然做到"每个实例一套状态"，同时省掉了把上百处引用改成 `this.xxx` 的机械改动 —— **改动面越小，越不容易在重构里引入新 bug**。

**为什么引擎不自动启动**：`start()` 由宿主页显式调用，于是"同一页创建多个实例""测试宿主页用假数据启动"都变得自然，而不需要给引擎加一堆开关。

---

### 3.5 世界地图层级规范（地图工厂）

> 这一节是**地图包的契约**：工具（`tools/add-map.js`、`tools/build-registry.js`）、
> 运行时（`js/maps/loader.js`）、以及将来任何一张新地图，都按它来。
> 契约一旦破坏，`build-registry.js` 会报错拒绝生成登记册 —— 校验写在工具里，不靠自觉。

#### 3.5.1 一张地图 = 三个文件

按**谁来维护**拆开，这是整套自动化的前提：

| 文件 | 谁维护 | 内容 | 会被脚本覆盖吗 |
| --- | --- | --- | --- |
| `<id>.geo.js` | 构建产物 | GeoJSON（内联成 `window.MAP_GEO[<id>]`） | ✔ 每次重刷都会重写 |
| `<id>.data.js` | **人** | 下级行政区资料卡 + 关卡设定（`window.MAP_DATA[<id>]`） | ✘ **永不覆盖** |
| `<id>.js` | 半自动 | 配置：层级、配色、存储 key、文案（`window.MAP_PACKAGES[<id>]`） | ✘ 已存在就不动 |

**为什么必须拆**：面积、地标、冷知识这类文字是人工核实过的资产，脚本编不出来也不该瞎编。
拆分之后，"重新下载边界数据"这个高频操作永远碰不到人写的文字 —— 这是把"会不会冲掉人工成果"从**靠小心**变成**靠结构**。

#### 3.5.2 目录规则（一条递归规则，没有例外）

> **地图 `X` 的包文件路径 = 父地图的子目录 + `X.js`**
> **某地图的子目录 = 它包文件所在目录 + 它自己的 id + `/`**

推导出来的布局：

| 地图 | id | parent | 包文件 | 子地图目录 |
| --- | --- | --- | --- | --- |
| 中国 | `china` | `null`（根） | `js/maps/china.js` | `js/maps/china/` |
| 四川 | `sichuan` | `china` | `js/maps/china/sichuan.js` | `js/maps/china/sichuan/` |
| 成都 | `chengdu` | `sichuan` | `js/maps/china/sichuan/chengdu.js` | `js/maps/china/sichuan/chengdu/` |
| 自贡 | `zigong` | `sichuan` | `js/maps/china/sichuan/zigong.js` | （暂无下级地图） |

要点：

- **根地图直接放在 `js/maps/` 下**（`parent = null`）
- 地图有没有下级**不影响它自己的位置**：叶子地图和带下级的地图放在同一层，
  将来长出下级也不会被迫搬家（只有"接入世界层"那一次特殊搬迁，见 3.5.7）
- 目录名 = 文件名的前缀 = 配置里的 `id` = `MAP_PACKAGES` / `MAP_GEO` / `MAP_DATA` 的 key，
  **四处必须完全一致**，`build-registry.js` 会把不一致直接判为 error

#### 3.5.3 字段规范

**引擎强制**的只有 `geo` 和 `levels`；后面几项是**地图工厂强制**的（少了它们，这张地图就没法被登记、被选择、被导航）：

| 字段 | 谁强制 | 说明 |
| --- | --- | --- |
| `id` | 地图工厂 | 唯一 id，= 文件名 = 目录名；只允许小写英文、数字、`_`、`-` |
| `name` | 地图工厂 | 显示名，如 `自贡市`（从父地图 GeoJSON 里自动借到） |
| `parent` | 地图工厂 | **上一级地图 id**；根地图写 `null` |
| `adcode` | 地图工厂 | 本级行政区划代码（6 位数字）；**世界/大洲等自然地理层级写 `null`** |
| `geo` | 引擎 | GeoJSON FeatureCollection；每个 feature 需 `properties.adcode` / `properties.name`；`Polygon` 与 `MultiPolygon` 都吃 |
| `levels` | 引擎 | `[{ id, name, short, blurb, adcodes, hue? }]` |
| `districts` | — | `adcode → { area, landmark, tagline, funFact }`；缺失时信息卡显示 `—`，不会崩 |
| `palette` | — | `hueByLevel` / `fallbackHue` / `saturation` / `lightBase` / `lightStep` / `lightSpan` / `hueSpread` |
| `map` | — | `{ width, padding }` 画布逻辑尺寸 |
| `piece` | — | `{ max, minSide, pieces }` 碎片尺寸档位 |
| `storage` | — | **每张地图必须换新前缀**，否则两张地图的存档互相覆盖 |
| `themes` | — | `{ list, fallback }` |
| `texts` | — | `{ cityName, districtCount, missingDataHint }` |

配置里**还可以**写 `children`，但它是**可选的自我声明**：

- 真相由子地图的 `parent` 反推（下节），父地图不写 `children` 也完全正确
- 若写了，`build-registry.js` 会拿它和反推结果对照，不一致就报警告
  （防的是"父子两处都手写、结果打架"这种最难查的错）

#### 3.5.4 父子关系：只写 `parent`，`children` 反推

新增一张地图时，**你只需要在新包自己的配置里写 `parent`**，不需要回头改父地图的文件。
`tools/build-registry.js` 扫描全部包、反推 `children`、生成登记册：

```
js/maps/**/*.js  ──扫描+沙箱求值──▶  { id, name, parent, adcode }
                                        │
                       按 parent 反推 children
                                        ▼
                              js/maps/registry.js（自动生成，勿手改）
```

一条实测证据（`sichuan` 从未声明过自己的孩子，`chengdu` 也从未声明过兄弟）：

```
roots: ["china"] | orphans: []
  china    中国    parent=null    children=[sichuan]
  sichuan  四川省  parent=china   children=[chengdu, zigong]   ← 反推出来的
  chengdu  成都    parent=sichuan children=[]
  zigong   自贡市  parent=sichuan children=[]
```

**为什么这样设计**：如果 `children` 也要手写，那么"加一张地图"就得改两个文件，
而且两份声明迟早会不一致。把 `parent` 定为唯一真相来源后，加地图是**纯粹的追加操作**，
天然可并行、不会冲突 —— 这正是"地图工厂"能自动化下去的前提。

`registry.js` 里另有两个诊断字段：

- `roots`：`parent = null` 的地图（树的根）
- `orphans`：声明了 `parent` 但**父级还没接入**的地图，如 `[{ id, parent }]`
  这不是错误，而是"父级空位还等着填"的信号（接入四川之前，成都就一直挂在这里）

#### 3.5.5 registry.js：元信息清单，与加载策略

`registry.js` 只装**元信息**（每张地图几十字节），不含任何 GeoJSON，所以可以一次性全量加载：

| 字段 | 说明 |
| --- | --- |
| `version` | 登记册结构版本 |
| `roots` | 根地图 id 数组 |
| `orphans` | 父级待接入的地图 |
| `maps[id].id / name / parent / adcode / children` | 层级信息 |
| `maps[id].dir` | 相对 `js/maps/` 的目录，如 `china/sichuan`（根为 `""`） |
| `maps[id].scripts` | 该地图三个文件的路径，相对 `js/maps/`，按依赖顺序 |

运行时由 `js/maps/loader.js` 按需注入：

```
首屏固定加载： registry.js(2.9KB) + loader.js(4.7KB) + geomap + engine + game  ≈ 75KB
用户选中某地图： 才注入它的三个文件            成都 ≈ 145KB
```

- 于是**地图数量再多，首屏也不变重**
- 注入用 `<script>` 而不是 `fetch`：`file://` 下浏览器会拦 fetch 本地 JSON，`<script>` 不受影响 ——
  **"双击即玩"这条底线决定了加载方式，不是随便选的**
- `scripts` 路径相对 **`js/maps/`**，loader 用自己 `<script src>` 的地址推算基准目录，
  所以宿主页放在哪一层都不会错

#### 3.5.6 面包屑 / "返回上一级"：为什么不在引擎里，怎么实现

**引擎不参与层级导航。** 引擎只认 `geo / levels / districts / palette / storage / texts` 这些
"渲染和玩法"字段，`parent` / `children` / `adcode` 它**完全不认识**（多传了也直接无视）。
这不是偷懒，而是分层原则：**引擎负责"拼一张图"，宿主层负责"在树里走"**。

所以"返回上一级"按钮和面包屑属于**宿主层**（`js/game.js` / 将来的选择器 UI），
它拿 `js/maps/loader.js` 的公开接口拼路径：

```js
// loader.js 已提供（运行时读 registry，不碰引擎）
MapLoader.trail('zigong')
// → [{ id:'china', name:'中国' }, { id:'sichuan', name:'四川省' }, { id:'zigong', name:'自贡市' }]
MapLoader.parentOf('zigong')   // → 四川省这条记录（父级未接入时返回 null）
MapLoader.childrenOf('sichuan')// → [chengdu, zigong]
```

导航规则（已按这个实现，代码在 `js/game.js` 的宿主层）：

1. 用 `trail(id)` 渲染面包屑：`中国 › 四川省 › 自贡市`，前几级是 `<a href="?map=<id>">`，末级用 `aria-current="page"` 标出且不可点
2. **"返回上一级"就是面包屑的上一级**，不再单独做一个按钮 —— 少一个控件就少一处状态不一致
3. 地图选择器是一个原生 `<select>`：按 `children` 递归渲染、用全角空格按层级缩进（`　　成都`），
   所以"选地图"和"看层级"是同一个控件。父级未接入的孤儿地图会挂到顶层并标注"（父级 … 待接入）"
4. 切换地图 = 改 URL 重新加载页面（`?map=<id>`）。
   **为什么是重新加载而不是就地切换**：引擎没有 `destroy()`，也刻意不加（红线），
   而"换地图"意味着换掉 geo/关卡/存档 key 一整套状态；重新加载最干净，
   也顺手让每张地图的进度天然独立（各自的 `storage` key）
5. 图层相关的 UI **一律不许塞进 `engine.js`**。宿主层要加东西时，只用引擎的三个公开接口：
   `create()` / `start()` / `getState()`（见 3.4）

整条导航的渲染顺序也定死了一件事：**先 `engine.start()`，再渲染导航，最后才打就绪标记**
（`<html data-map-ready="<id>">` + `map-ready` 事件）。于是"就绪"就意味着"页面完全可用"，
测试和宿主页都不必再猜时间 —— 这个约定是从坑 #25 里长出来的。

#### 3.5.7 层级与现实行政区划、以及世界层（待接入）

当前只做了中国区划（**天地图官方数据**），层级深度靠 adcode 天然对应：

| 层级 | adcode 形态 | 例子 | 数据源 |
| --- | --- | --- | --- |
| 世界 | `null` | `world` | **官方通道没有，待接入（Natural Earth）** |
| 大洲 | `null` | `asia` | **官方通道没有，待接入（Natural Earth）** |
| 国家 | `100000` | 中国 | 天地图官方 ✔ |
| 一级行政区（省） | `XX0000` | `510000` 四川省 | 天地图官方 ✔ |
| 二级（地级市） | `XXXX00` | `510300` 自贡市 | 天地图官方 ✔ |
| 三级（区县） | `XXXXXX` | `510302` 自流井区 | 天地图官方 ✔ |

**已实测：DataV 没有世界地图数据**（`areas_v3/bound/world.json` 返回 404），
世界/大洲层必须换数据源（Natural Earth、world-atlas 的 TopoJSON 是常见选择）。

接入时有两个必须提前知道的约束：

1. **仍要内联成 `.js`**：为了 `file://` 双击可用，任何数据源最终都得在构建期转成
   `window.MAP_GEO[<id>] = {...}`，不能让浏览器去 fetch
2. **TopoJSON 需要拓扑重建**：Natural Earth 官方是 Shapefile/GeoJSON，world-atlas 是 TopoJSON
   （坐标是量化后的增量，需要解码）。若选 TopoJSON，构建脚本里要自带一个小解码器；
   若选 GeoJSON，体积会大不少（世界级边界几 MB），可能需要再做简化
3. **一次搬迁**：世界层一旦接入，中国就不再是根了 —— `parent` 改成 `asia`，
   包文件从 `js/maps/china.js` 移到 `js/maps/world/asia/china.js`，
   子目录 `js/maps/china/` 跟着变成 `js/maps/world/asia/china/`。
   目录规则本身支持这次搬迁（`parent` 一改，`build-registry.js` 会告诉你"放错位置"），
   但要**单独作为一次任务做**，不要和别的改动混在一起

> 世界层数据源调研与接入是**独立任务**，不混进中国区划这一轮 ——
> 混做会同时动到"数据源、几何精度、层级根节点"三样东西，出问题很难定位。

#### 3.5.8 新增一张地图的标准流程

中国区划（DataV 有数据）——一条命令：

```bash
node tools/add-map.js --adcode=510300 --name=zigong --parent=sichuan
```

**整批接入**（一个省 / 一个市）见 [5.11 批量地图操作手册](#511-批量地图操作手册)：

```bash
node tools/batch-add-maps.js --parent=510000
```

它会：

1. 算出这张地图该放在哪个目录（父级还没接入时**自动补全祖先链**，如自贡会先补出四川、中国）
2. 下载边界（网络失败最多重试 **2 次**，仍失败就停下报错，不死磕）
3. 生成三个文件：`.geo.js` 覆盖刷新、`.data.js` 与 `.js` **已存在就不动**
4. 调 `build-registry.js` 重新生成登记册（`children` 自动反推）
5. 打印**还需要人工补什么**（哪几个区的面积/地标/冷知识/关卡分组）

常用参数：`--dry-run`（只打印计划，不联网不写盘）、`--geo-only`（只刷新边界，
人工文件一律不动）、`--force`（连人工文件一起覆盖，慎用）。

跑完必须做的事：

```bash
node tools/e2e-test.js     # 166 项必须全绿（新地图不许带坏老地图）
```

**脚本的边界**：能自动生成 `geo`、`levels` 的骨架、`palette` 的色相、`storage` 的 key；
**不能**生成面积、地标、冷知识、以及"为什么这样分关"——这些是内容，不是数据。
生成出来的占位长这样，跑得起来、信息卡显示 `—` 或"（待补充）"：

```js
510302: { area: null, landmark: '（待补充）', tagline: '（待补充）', funFact: '（待补充）' }
```

---

## 四、踩坑全记录

按"坑的类型"分组。每条都附上**现象 → 根因 → 修法 → 教训**。

### 4.1 坐标系与几何

#### 坑 #5：地图南北颠倒

**现象**：整张成都上下颠倒。彭州（最北）画在蒲江（最南）下面。

**根因**：墨卡托原始公式 `y = ln(tan(π/4 + φ/2))` 属于**数学坐标系** —— 那里 y 轴朝上，所以纬度越高 y 越大 = 越靠北，公式本身没错。但 **SVG 的 y 轴朝下**，直接把这个 y 当 SVG 坐标用，南北就翻了。

**更该警惕的是**：我一开始的注释把它**反着写成了"正确"**：

```js
// ✘ 错的注释，而且它让当时的我停止了怀疑
// 注意 y 轴方向：纬度越高 y 越小，正好和 SVG 的 y 轴向下一致，
// 所以成都北边的彭州、都江堰会自动画在画布上方，不需要翻转。
```

**修法**：取负号。

```js
return [lon * DEG, -Math.log(Math.tan(Math.PI / 4 + (lat * DEG) / 2))];
```

**验证方法**（这个方法论要记住）：不要只看"形状像不像成都"，要**拿真实地理常识去断言**：

```
画布从上到下应该是：彭州(31.14°N) → 都江堰 → 市中心 → 简阳 → 蒲江(30.22°N)
```

**教训**：
1. **注释写错比代码写错更危险** —— 它让后来的人（包括自己）停止怀疑
2. 只验"bbox 不越界""面积排名合理"是不够的，**方位也要断言**
3. 修完立刻加了 3 条断言锁死（投影层 2 条 + 渲染层 1 条），防止回归

#### 坑 #11：极扁的碎片被"放大"后被撑破

**现象**：手机端托盘里的碎片溢出容器。

**根因**：碎片尺寸计算里有一段"短边不足 15px 就按短边撑开"的逻辑：

```js
// ✘ 旧逻辑：短边太细时无脑放大，长边也跟着冲出去了
let k = max / Math.max(w, h);
if (Math.min(w, h) * k < PIECE_MIN_SIDE) k = PIECE_MIN_SIDE / Math.min(w, h);
```

对于一个 5:1 的长条碎片，放大后长边能到 75px，远超 `max`（40px）。

**修法**：同时卡住长边上限，两边取更小的那个 k。

```js
k = Math.min(PIECE_MIN_SIDE / short, (max * 1.15) / long);
```

**教训**：等比缩放时，"保证短边不小于 X"和"保证长边不超过 Y"是两个会打架的约束，必须同时满足。

#### 坑 #22：几何层只认 MultiPolygon，Polygon 直接抛错

**现象**：引擎功能测试第一次跑，城市回归 89 项全绿，但假数据宿主页的**底图、凹槽、碎片全是 0**，进度停在 `0/0`；页面正文完好，**控制台也没有任何提示**（数据缺失的兜底提示没触发，说明 `init()` 是中途抛异常，而不是走了兜底分支）。

**排查**（比读代码猜快得多的一步）：把 `start()` 再调一次并用 `try/catch` 抓住异常现场 ——

```js
try { W.__ENGINE__.start(); } catch (e) { info.err = e.message; }
// → number 100 is not iterable (cannot read property Symbol(Symbol.iterator))
//    at js/geomap.js:94
```

**根因**：`buildGeoMap` 直接按 `coordinates.map(多边形).map(环).map(点)` 三层取点，等于**隐含要求 `MultiPolygon`**。真实成都数据恰好就是 `MultiPolygon`（`tools/build-data.js` 也统一转成它），所以这个问题被藏了整整一个项目周期。而标准 GeoJSON 的 `Polygon` 只有两层，喂进来时"环"被当成"点"去解构，当场炸掉：

```
MultiPolygon: coordinates = [多边形][环][点]   ← DataV 导出的成都数据
Polygon:      coordinates = [环][点]           ← 手写数据 / 别家数据源常见
```

**修法**（只改引擎，不动测试断言）：进点之前把两种写法统一补齐成同一形状。

```js
const geom = feature.geometry;
const rawPolygons = geom.type === 'Polygon' ? [geom.coordinates] : geom.coordinates;
```

fixture 顺势改成**两种写法混用**（甲区 `Polygon`、乙区/丙县 `MultiPolygon`），并加了一条断言把这条兼容性钉死。

**验证方式**：用 `git show HEAD:js/geomap.js` 的旧实现和新实现分别构建成都全图，深比对结果 —— **76819 字节输出完全一致**（投影 / 路径 / bbox / 质心一个数字没变），确认修复对老数据零影响。

**教训**：
1. **"我用得对"不等于"它能通用"** —— 真实数据恰好落在某一个分支上，另一个分支的 bug 就可能潜伏很久；做通用化时，要主动去问"还有哪种写法/哪种输入"
2. **换一份极端数据当探针，是性价比最高的通用性测试**：喂 3 个假区县，比再通读一遍 1500 行代码更快命中问题
3. **把输入契约写进文档**：引擎到底吃哪种 GeoJSON 写法，不能在实现里"隐式约定"（现已写进 3.4 的字段表）
4. 排查顺序仍然是"抓现场数据 > 读代码猜"：`try/catch` 拿到异常行号，比通读 `buildGeoMap` 快得多

### 4.2 布局与响应式

#### 坑 #12：min-width auto 撑破容器（移动端横向溢出的真凶）

**现象**：390px 视口下，顶栏右侧的音效按钮和主题色点被推出屏幕。

**排查过程**（曲折，值得完整记录）：

| 尝试 | 结果 |
| --- | --- |
| 给 `body` 加 `overflow-x: hidden` | 溢出被裁，但控件还是在外 |
| 给 `.tray` 换 grid 布局 | 托盘问题缓解，顶栏依旧 |
| 给 `.board-column` / `.tray-panel` / `.stage` 逐层加 `min-width: 0` | **完全没变化** |
| **把每一层的实测宽度打出来** | **一击命中** |

实测数据（这是转折点）：

```
.app               w390   ← 容器看着完全正常
.topbar            w457   ✗ 子元素撑破了它
.stage             w457   ✗
.board-column      w457   ✗
body.scrollWidth   468   ✗ 整页溢出
```

**根因**：grid / flex 子项的默认值是 `min-width: auto`，含义是"**不小于内容的最小宽度**"。内容一宽，子项就撑破列 —— 而**容器本身宽度不变**，所以从外面完全看不出来。

**修法**：统一把子项的 `min-width` 归零。

```css
.app > *,
.stage > *,
.board-column > *,
.sidebar > * { min-width: 0; }
```

修完 `body.scrollWidth` 从 **468 → 390**（正好等于视口宽）。

**还剩一处**：`.topbar-right` 仍是 457。因为它内部三个东西（关卡标签 + 音效 + 主题）总宽刚好卡在临界，需要另外收尺寸：

```css
@media (max-width: 620px) {
  .topbar-right { width: 100%; max-width: 100%; overflow: hidden; gap: 7px; }
  .level-tabs { flex: 1 1 auto; min-width: 0; }
  .sound-btn { width: 31px; height: 31px; }
  .theme-dot { width: 15px; height: 15px; }
}
```

**教训**：
1. **"容器宽度正常"不等于"没溢出"** —— 一定要量子元素和 `scrollWidth`
2. 这类问题靠盯 CSS 是找不到的，**必须打印实测数据**
3. 防御性写法：给所有 grid/flex 子项加 `min-width: 0` 应该成为肌肉记忆
4. 最终加了断言守着：`body.scrollWidth ≤ window.innerWidth`

#### 坑 #9：`height:100vh + overflow:hidden` 把底部内容永久裁掉

**现象**：矮屏笔记本上，底部的碎片托盘和操作按钮被裁，而且**滚不动**。

**根因**：桌面端锁一屏的写法过于刚硬。内容一旦超过视口高度，多出来的部分既看不到也够不着。

**修法**：各区块的 `min-height` 改用 `min(400px, 46vh)` 跟着视口收缩，`body` 保留 `overflow-y: auto` 兜底。

#### 坑 #10：`min-height:100vh` 又把信息卡撑到 930px

**现象**：修完坑 #9 后，"点击模式"的测试挂了 —— 点击坐标 y=1178，而视口只有 1000px。

**根因**：我把 `.app` 从 `height: 100vh` 改成 `min-height: 100vh`。高度不再固定后，侧栏里 `.info-card` 的 `flex: 1` 被内容**撑到了 930px**，整页变成 1255px，托盘被顶出视口。

**修法**：`.app` 必须锁死高度，但要配合各区块的 `vh` 自适应：

```css
@media (min-width: 1081px) {
  body { overflow-x: hidden; overflow-y: auto; }  /* 兜底 */
  .app { height: 100vh; }                          /* 必须固定 */
}
```

**教训**：**坑 #9 和 #10 是一对**。第一次修方向对了一半（要允许滚动），但错在把 `.app` 的高度也放开了。**"允许滚动"和"放开高度"是两件事**。

#### 坑 #3：`file://` 下 `fetch` 本地 JSON 被 CORS 拦

**现象**：双击 `index.html` 后地图不显示。

**根因**：`file://` 协议下，浏览器把每个文件视为独立 origin，`fetch('data.json')` 直接被拦。

**修法**：构建脚本把数据处理成 `window.CHENGDU_GEO = {...}`，用 `<script>` 标签加载（不受 CORS 限制）。

### 4.3 CSS 与 SVG 的边界

#### 坑 #8：通道变量当完整颜色用（"银杏金不金"）

**现象**：图案预览页里"银杏金"那一列显示的是黑色图案。

**根因**：项目里有两套颜色变量，我混用了：

```css
--c-gold: 224 169 74;   /* 通道值：只能写在 rgb(... / α) 里 */
--amber:  #e0a94a;      /* 完整颜色：可以直接赋值给 color */

/* ✘ 无效声明，浏览器静默忽略 → 回退成继承色 */
.box.gold { color: var(--c-gold); }
```

**修法**：`color: rgb(var(--c-gold))`。并全局排查了同类错误（`game.js` 的图例用的是 `--amber`，本来就对）。

**教训**：通道变量和完整颜色变量**必须靠命名区分**（加 `c-` 前缀），否则迟早混用。而且这类错误**不报错**，只是静默失效 —— 必须靠肉眼比对或截图发现。

#### 坑 #14：`<use>` 引用的是 shadow tree，CSS 选择器进不去

**现象**：结算画面的"空星"几乎看不见，想改成空心星却改不动。

```css
/* ✘ 无效：选不中 shadow tree 里的 path */
.modal-star.is-empty path { fill: none; }
```

**根因**：`<use href="#id">` 引用的内容在 shadow tree 里，外部 CSS 选择器无法命中。

**修法**：**删掉 symbol 上的 `fill` 属性，改用继承**——继承能穿透 shadow boundary，选择器不能。

```html
<!-- symbol 里不写 fill/stroke -->
<symbol id="i-star" viewBox="0 0 24 24"><path d="..."/></symbol>
```
```css
.modal-star          { fill: currentColor; stroke: none; }   /* 实心 */
.modal-star.is-empty { fill: none; stroke: currentColor; }   /* 空心 */
```

**同一个原理的另一处应用**：熊猫的双色部件之所以要拆成 `--panda-ink` / `--panda-paper` 两个变量，也是因为外部没法用选择器改 shadow tree 内部的元素。

**教训**：用 `<symbol>` + `<use>` 做图案库时，**颜色一律走"继承"（`currentColor` / CSS 变量），不要走"选择器"**。

#### 坑 #4：`rgba()` 在 SVG 呈现属性里的行为

（次要）SVG 的 `fill` / `stroke` 作为**呈现属性**时优先级低于 CSS 规则。这既是坑也是解法 —— 上面那条正好利用它。

### 4.4 数据与状态

#### 坑 #1：崇州 / 邛崃的 adcode 写反了

**现象**：无。**这才是最可怕的** —— 玩家拼对了，信息卡却显示另一个区县的介绍。

**根因**：凭记忆写 `districts.js`，把两个相邻区县的行政区划代码弄反了：

```
正确：邛崃 510183、崇州 510184
我写的：崇州 510183、邛崃 510184   ✘
```

**发现方式**：派了一个后台任务去核实 20 个区县的资料（面积、地标、特产），交叉比对时发现的。

**修法**：改正，并加断言：

```js
check('崇州 510184 / 邛崃 510183 没有写反',
  nameMap.get(510184) === '崇州市' && nameMap.get(510183) === '邛崃市');
```

**教训**：**这类"静默错误"（不报错、但结果全错）只有靠外部数据交叉验证才能发现。** 凭记忆写的数据，必须有独立的核实环节。

#### 坑 #13：Number(null) 等于 0 导致全部解锁

**现象**：改徽标时发现"锁图标根本不显示"，一查 DOM 才知道三关一直是全解锁的 —— **用户明确要的"逐关解锁成就感"从未生效**。

**根因**：

```js
// ✘ 不带参数时 get() 返回 null，而 Number(null) === 0
// → 被误判成 "?level=0" → state.unlocked = LEVELS.length
const wanted = Number(new URLSearchParams(location.search).get('level'));
```

**修法**：

```js
const rawLevel = new URLSearchParams(location.search).get('level');
const wanted = rawLevel === null || rawLevel === '' ? NaN : Number(rawLevel);
```

**副作用**：修好后，测试里"跳到第三关"的用例挂了 —— 因为它原本是靠这个 bug 走的后门。改成**真的把第二关拼完**再进第三关，反而验证了完整的解锁链路。

**教训**：`Number()` 对 `null` / `''` / `[]` 的隐式转换是经典陷阱。**URL 参数解析一定要先判断"存在性"再判断"值"。**

#### 坑 #6：`statHints` / `statTries` 从来没被更新

**现象**：提示按钮点了没反应（数字不变）。

**根因**：`updateStats()` 里只更新了进度，漏了这两个计数。

**修法**：补上。

**教训**：这类"忘了接线"的 bug 靠人工点一遍容易漏，**用断言覆盖每个 UI 数字**更可靠。

#### 坑 #7：`click` 事件与 `pointerup` 撞车

**现象**：点击碎片无法选中（状态被切换了两次，等于没选）。

**根因**：为了支持键盘，我监听了 `click`；而鼠标点击时 `pointerup` 和 `click` **都会触发**，于是 `toggleSelect` 被调了两次。

**第一次修法**（不稳）：用 `ev.detail === 0` 区分"键盘触发的 click"。这个判据不可靠。

**最终修法**：键盘直接听 `keydown`，彻底避开与 pointer 事件的冲突。

```js
btn.addEventListener('keydown', (ev) => {
  if (ev.key === 'Enter' || ev.key === ' ') { ev.preventDefault(); toggleSelect(adcode); }
});
```

**教训**：同一个用户操作能用**两套事件路径**触发时（鼠标走 pointer、键盘走 click），要么统一走一条路，要么用**互斥且可靠**的标志位区分。

### 4.5 测试与工具链

#### 坑 #2：headless Chrome 在 `Runtime.enable` 时崩溃

**现象**：想用 CDP（Chrome DevTools Protocol）驱动浏览器测试，WebSocket 连上了、`Browser.getVersion` 也返回了，但一调 `Runtime.enable` 就 `SIGTRAP` 崩溃。

**排查**：试了 5 组参数组合：

```
--headless                  → exit 133 (SIGTRAP)
--headless=new              → exit 133
--headless=old              → exit 133
--headless --disable-gpu    → exit 133
--headless --no-sandbox     → ✔ 正常，截图 436KB
```

**结论**：**这台机器的 Chrome sandbox 起不来**，必须 `--no-sandbox`。

**修法**：所有测试脚本都带 `--no-sandbox`，并在注释里写明原因（换机器可以去掉）。

**同时放弃了 CDP**，改用"页面自己跑测试 + 隐表单 POST 回传结果"。这个方案反而更简单、更稳，而且**零依赖**。

**教训**：调试协议跑不通时，**"让被测页面自己上报结果"是一条极好的退路**，比死磕协议划算得多。按红线"最多试 2 次"的原则，我在第 5 组参数上找到出路后就立刻转向了。

#### 坑 #15：测试断言写错（`.piece` 没过滤 `is-used`）

**现象**：断言"刷新后托盘里少了那一块"失败（实际 8，期望 7）。

**根因**：被拼上的碎片是加 `is-used` 类（`display:none`），**元素仍在 DOM 里**。`querySelectorAll('.piece').length` 把它们也算进去了。

**修法**：过滤掉。

**教训**：**断言本身也会写错。** 失败时先怀疑断言，再怀疑代码 —— 但一定要验证到底哪个错了，不能直接改断言让它变绿。

#### 坑 #16：动态创建的 iframe 在 `file://` 下被跨源拦

**现象**：想在测试里新建一个 390px 宽的 iframe 验证移动端 CSS，报 `SecurityError: Blocked a frame with origin "file://"`。

**根因**：`file://` 下每个文件是独立 origin。HTML 里**静态声明**的 iframe 有 `--allow-file-access-from-files` 罩着，**动态创建**的没有。

**修法**（按红线不纠缠）：把这项检查移到 **Node 侧做静态检查** —— 直接读 `style.css`，按大括号配平抠出媒体查询块，检查关键规则是否存在。

**教训**：浏览器里做不了的事，**挪到 Node 侧做静态分析**往往是更省事的选择。

#### 坑 #17：静态检查抠错了 CSS 块

**现象**：刚写的静态检查报"缺规则"，但规则明明在文件里。

**根因**：CSS 里有**两个** `@media (max-width: 620px)`（一个管布局、一个管性能降级），`indexOf` 只拿到第一个。

**修法**：遍历所有匹配块，拼接后再检查。

**教训**：**按关键字定位代码块时，要假设它可能出现多次。**

#### 坑 #18：开场覆盖层挡住所有点击，测试全挂

**现象**：加完开场动画后，"点击模式"测试又挂了。

**根因**：`.intro` 是 `position: fixed; inset: 0; z-index: 1200`，把所有点击都接走了，`elementFromPoint` 命中的是它而不是碎片。

**修法**：测试开头先验证开场动画存在，然后关掉它，再测后续。

**教训**：这是一个**好**的失败 —— 它反过来证明了开场动画确实生效。加全屏覆盖层时，要预料到它会打断所有基于坐标的测试。

### 4.6 视觉设计

#### 坑 #19：竹子初版画砸了

**现象**：第一版竹子看起来像藤蔓/含羞草 —— 竹竿太细、竹节几乎看不见、叶子太小太散。

**发现方式**：**做了个图案预览页，把每个图案放大到 128px 并排显示**。缩放后的截图里问题一览无余。

**修法**：竹竿加粗到 5.5、竹节用明显短横线、叶片加宽（宽 11~13）并加粗描边。

**二次踩坑**：加宽叶子后还是像羽毛 —— 因为叶片是**斜的**，垂直方向的宽度不等于垂直于叶轴的宽度。再调贝塞尔控制点才到位。

**教训**：**做 SVG 图案必须有一个"放大预览"的环节**。在 62px 的实际尺寸下，很多缺陷是看不出来的。

#### 坑 #20：脸谱第一版太"线描"

**现象**：画出来像一张对称面具，不像川剧脸谱。

**根因**：线宽只有 1.7~2.2、填充透明度 0.15~0.22 —— 那是在**描轮廓**，不是画脸谱。川剧脸谱的特征是**浓重的对称色块**。

**修法**：三处改动

1. 额心火焰纹改为**实心**（透明度 0.88）
2. 眉毛加粗到 **4.5**
3. 眼窝改为**粗描边椭圆 + 淡填充**，做出彩绘的体积感

**中间又踩一坑**：眉加粗后**和眼窝是分离的**，看着还是两条孤立弧线。把眉的末端从 y=21.5 **下压到 y=25**，让它接住眼窝上缘（y≈28），眉眼才连成一体。

**教训**：极简图形里，"元素之间的**连接关系**"比"单个元素画得多准"更决定观感。

#### 坑 #21：图案尺寸的适用边界

顺手量了一下脸谱的线宽：

```
54px 显示 → 实际线宽 0.81 ~ 3.04px   ✔ 清晰
15px 显示 → 实际线宽 0.23 ~ 0.84px   ✘ 糊成一团
```

**结论**：脸谱**只适合中大型展示（≥48px）**，不能当 15px 的小图标用。后续用它时守住这个下限。

---

### 4.7 地图工厂这一轮踩的坑

#### 坑 #23：块注释里写 `js/maps/**/*.js`，`**/` 里的 `*/` 把注释提前闭合了

**现象**：给 `engine.js` 改**注释**（纯注释，没动一行代码），166 项测试直接掉到 8 通过，报 `SyntaxError: Unexpected token '<'`。

**根因**：注释里写了 `js/maps/**/<id>.js`。`**/` 这个序列里含 `*/`，块注释在那里就结束了，后面的 `<id>.js` 变成了代码。

**教训**：**"改动都在注释行内"不等于"注释没被提前闭合"**。我当时的自查是"逐行看 diff 是否以 `*` 开头"，全部通过 —— 但注释早已断开。
改注释后必须跑 `node --check`（或测试），**别用肉眼审注释**。另外文档里引用通配路径时，别在块注释里写 `**/`。

#### 坑 #24：DataV 全国数据里混着非行政区 feature，把生成物写成了非法 JS

**现象**：自动生成的中国地图包一加载就崩，`china.data.js` 语法错误。

**根因**：`100000_full.json` 有 35 个 feature，其中一个是**南海九段线**，`adcode` 是字符串 `"100000_JD"`。
资料文件是按 `adcode` 当对象 key 生成的，于是写出了 `100000_JD: {` —— 非法 JS 字面量。

**修法**（两层）：
1. 生成 key 时一律 `JSON.stringify`（防御性，数字 key 也加引号）
2. 明确区分"行政区"与"非行政区"：`isAdminAdcode()` 只认 6 位数字，**非行政区 feature 留在 GeoJSON 里画底图，但不进关卡、不进资料卡**
3. adcode 是数字串的统一转成 `Number`，否则 `Map` 的 key 类型会不一致

**教训**：真实数据里总有"看着像但不是"的脏数据。生成代码时**永远不要相信字段的合法形态** ——
把它当 key 写进代码前，先问一句"这玩意儿一定是合法标识符吗"。

#### 坑 #25：测试跟 300ms 的吸附动画抢时间，看起来像引擎坏了

**现象**：新写的多地图冒烟套件里，**每一张**地图（包括本来能跑通的成都）都"拖不进去"：
`placed=0`、碎片还在托盘、`tries=1`、没有任何飘字。

**排查过程**（值得记的是方法，不是结论）：
1. 先怀疑坐标算错 → 给 iframe 里的 `getScreenCTM` / `isPointInFill` **打桩**，记录引擎实际问了哪个点、得到什么答案
2. 记录显示：引擎拿到的是正确坐标、`isPointInFill` 返回 `true`、候选高亮也亮了 —— **引擎完全正常**
3. 既然判定正确，就去读 `commitPlace`：它只播吸附动画，真正的落位在 `SNAP_MS` 之后由 `revealDistrict()` 完成
4. 而测试只等了 **280ms**，`SNAP_MS = 300ms` —— 差 20ms

**修法**：不要 sleep 一个"看起来够大"的固定值，改成**等结果**：
轮询"幽灵层消失 + tries 已累加"才认为这次拖拽结算完（三种结局耗时不同，定时值必然踩坑）。

**教训**：
- **动画时长是实现的内部细节，测试不该知道它**。凡是要等某个副作用出现，就轮询它出现，而不是猜时间。这个项目里同一个错误犯过两次（第一次是等地图就绪，第二次是等落位）
- 排查时**先证明被测对象有没有问题**，再怀疑自己的测试。打桩记录"它实际收到什么"，比反复读代码猜快得多

> 同一轮里还有两个**测试环境**的坑，一并记下：
> **① 被测 iframe 必须留在正常文档流里**。第一版把 iframe 藏到 `left:-99999px`，
> 拖拽要靠 `getBoundingClientRect`（碎片中心）和 `getScreenCTM`（落点）两套坐标，
> 藏太远会让两者错开几十像素，于是"每一张地图都拖不进去"。
> **② 关卡开始时视图会用 `viewBox` 插值推近**，动画期间取坐标必然错位；
> 套件里要先等 CTM 稳定再算落点。

#### 坑 #26：`flex-basis: 100%` 在 column 容器里，含义从"宽度"变成了"高度"

**现象**：新加的地图导航条在桌面上好好的，手机上一看：导航条不见了、右侧还有东西被裁掉，
`body.scrollWidth` 是 779 而视口只有 500。

**根因**：导航条是 `.topbar` 的第 3 个子项，靠 `flex-basis: 100%` 换到第二行。
但窄屏的布局块里有一条 `.topbar { flex-direction: column }` —— **主轴换成了竖直方向，
`flex-basis` 于是成了"高度 100%"**，导航条被撑成一整列高，还被挤出屏幕。

**修法**：在那个 620px 断点里补一句 `.mapbar { flex-basis: auto; }`（纵向排列本来就各占一行）。

**教训**：
- **`flex-basis` 是主轴尺寸，不是宽度**。给 flex 子项设 `flex-basis` 前，先确认祖先容器在
  **所有断点**下都是同一个 `flex-direction`，否则同一份 CSS 会在某个断点下突然失效
- **横向溢出的断言必须覆盖窄屏**。这个项目的浏览器测试全跑在 1600px 宽的 iframe 里，
  手机上出的事它们一个都看不见 —— 现在多了一条"390×844 专项"（`tools/map-smoke.html`）
- 截图是发现这类问题的有效手段：数值断言会告诉你"没溢出"，而截图会告诉你"导航条压根没在那儿"

#### 坑 #27：`getScreenCTM()` 和 `clientX/clientY` 可能不在同一个坐标系里

**现象**（排查坑 #25 时撞上的伴生问题）：第一版多地图冒烟套件把被测 iframe 藏到
`left: -99999px`，结果**每一张地图都"拖不进去"**。

**根因**：拖拽要同时用两套坐标 —— 碎片中心来自 `getBoundingClientRect()`，
凹槽落点来自 `getScreenCTM()`。iframe 被挪到远离原点的位置后，这两套坐标错开了几十像素，
落点自然判不到凹槽上。

**修法**：被测 iframe 必须留在**正常文档流**里（照抄 `tools/selftest.html` 的做法，
iframe 放在 body 最前面）。另外关卡开始时视图会用 `viewBox` 插值推近，
动画期间取坐标必然错位，所以套件里要先等 `getScreenCTM()` 连续两次采样一致再算落点。

**教训**：同时使用多个坐标 API 时，先确认它们**参考系相同**；测试环境本身
（iframe 的位置、可见性）也是被测逻辑的一部分，别以为"页面没变就没影响"。

#### 坑 #28：换数据源会改变 adcode 集合，而人工资料文件按设计不会被覆盖

**现象**：全量换源到天地图官方数据后，879 项里躺了 1 项 —— 凉山彝族自治州"加载不就绪"，
浏览器里只有一句 `Cannot read properties of undefined (reading 'd')`。

**根因**：会理**县**(`513425`) 在官方 2024 数据里已经是会理**市**(`513402`)。
换源更新了 geo，但 `.data.js`（关卡里的 adcodes）**按设计不覆盖**（保护人工文案），
于是关卡引用的 adcode 在新 geo 里不存在 → 引擎 `shapes.get(adcode)` 得到 undefined → 崩。

**修法**：
1. 对**占位资料**的地图（还没人工填过内容的）用 `--force` 重新生成 `.data.js` / `.js`
2. 新增离线自检 `tools/test-maps.js`（已接入 `e2e-test.js`）：逐张校验
   "关卡 adcode ⊆ geo adcode""关卡覆盖全部行政区""每个行政区有资料卡""存储 key 唯一""带来源元信息"

**教训**：
- **换数据源不只是换几何，还可能换"主键"**。任何"用外部数据的 id 当关联键"的设计，
  在数据源切换时都必须重新校验一遍关联完整性
- 人工文件"永不覆盖"是保护，但也意味着**它会停留在旧数据上** —— 保护与一致性要各配一只手：
  这里那只手就是自检脚本
- 浏览器里 `Cannot read properties of undefined` 这种错，根因往往在数据而不在渲染。
  把它前移成**离线断言**（2 秒跑完、直接指出是哪张图的哪几个 adcode），比在浏览器里猜快得多

#### 坑 #29：拿 adcode 前缀当父子关系，`--parent=510000` 一张地图都选不中

**现象**：给面积工具加 `--parent=<省 adcode>` 后，`node tools/area-from-geo.js --parent=510000`
只匹配到**四川自己**，21 个市州一个都没进来。

**根因**：**父子关系不等于 adcode 前缀关系**。四川是 `510000`，但下辖的乐山是 `511100`
—— `511100` 并不以 `510000` 开头。省级 adcode 的规则是 `xx0000`，地级是 `xxYY00`：
只有前两位（`51`）相同，中间两位不同。用 `startsWith('510000')` 去筛，等于只筛出了省本级。

**修法**：父子关系一律走**包配置里声明的 `parent`**（与登记册同一份真相来源），
不要再造第二套判定规则。对"区县直接写在市自己的 `.data.js` 里"的叶子地图（如各市），
`--parent=<市 adcode>` 回退为**该市自己**，因为它的下级没有独立地图包。

```js
const parentSlug = ids.find((id) => String(scan.maps[id].adcode) === parentStr);
if (parentSlug) {
  const kids = ids.filter((id) => scan.maps[id].parent === parentSlug);
  return kids.length ? kids.map((id) => scan.maps[id]) : [scan.maps[parentSlug]];
}
```

**遗留**：`tools/replace-geo-source.js` 的 `--parent` 用的是另一套"前两位相同 + 兄弟层级"
启发式，对成都这类（`510100` → 区县 `510104`）同样会匹配失败。
它当前那条路径靠 `--only` 也能用，所以**本轮没动它**，只记在这里 —— 要修的话按同一思路改。

**教训**：同一个概念（父子关系）出现**第二套判定逻辑**时，几乎一定是 bug 温床。
登记册里已经有 `parent` 字段了，任何新工具都该复用它，而不是自己再推一遍。

#### 坑 #30：脚本把人工核实过的面积覆盖掉了（"能改"≠"该改"）

**现象**：批量补面积后，成都锦江区的 `area` 从人工核实的 `62.0` 变成了几何计算值 `60.8`。
20 个区县全被覆盖，而成都文件顶部的口径说明写着"采用四川省情网三调数据"。

**根因**：脚本逻辑是「值不一样就替换」，**分不清一个数字是"人工填的"还是"自己上次算的"**。
只要两轮之间边界数据有细微变化，人工值就会被静默冲掉 —— 而项目通篇的设计原则恰恰是
**"脚本只写 .geo.js，绝不碰人工的 .data.js"**。

**修法**：让产物自带**来源标记**，把"能改"收窄成"该改"：

```js
area: 62.0,   // 人工填的：没有标记 → 脚本默认不动（要改需显式 --overwrite-human）
area: 465,    // [geo-area] 依官方边界几何计算（km²） → 脚本自己写的，可随边界更新刷新
area: null,   // 占位 → 直接补
```

**教训**：
- **凡是"批量脚本写人工文件"，都必须能区分"谁写的"**。三个状态（占位 / 机器写的 / 人写的）
  对应三种处理方式（补 / 刷新 / 别动），少一个状态就会出事
- 升级旧产物时**故意保守**：老版本注释没有标记，于是被当成人工内容 —— 只会"少改"，不会"改错"
- 顺带发现第二个假溯源：工具原本给**每个**扫到的文件都加"面积由工具计算"的声明，
  连面积全是人工填的成都也加。改成**只有真的写了计算值才加声明** —— 溯源也要讲诚实
- 这个错是**自查时发现的**（对比 git diff 发现成都被改），不是测试报的：
  884 项测试全绿的情况下数据照样被改错了。**测试全绿 ≠ 没出错**

#### 坑 #31：调抽取规则时反复重跑抓取，把外部站点打成了 IP 级限流

**现象**：给"区县事实抽取"写正则时，每改一次规则就重跑一遍 `--map=zigong`（6 个词条）。
几轮之后，所有请求开始返回 **6313 字节的拦截页**（正常词条页 200 KB–1 MB），
再后来整站不可达（0 字节）。**整条抓取链路当场报废。**

**根因**：把"调规则"和"抓数据"耦合在了一起。规则是本地逻辑，改一百遍也不该联网；
而我每改一次都重新抓 6 个页面，等于把站点当成了"可反复读的本地文件。
抽取规则的迭代成本被外部站点替我付了 —— 它选择不付，直接封我。

**修法（三件事，缺一不可）**：

1. **磁盘缓存**：`.cache/facts/<url-sha1>.html`，同一 URL 30 天内永不重抓。
   抓取层先查缓存，命中就直接解析。**调规则时全部命中缓存，零联网。**
2. **限流识别**：正常页 ≥50 KB；小于这个阈值一律判为限流页 → 退避重试 →
   仍失败就明确返回 `throttled: true`。
   **绝不能把限流页当正文** —— 那会让下游得出"这个词条没资料"的**假结论**，
   比报错更危险（假成功）。
3. **串行 + 间隔**：默认 1.5 s 一个请求，不并发。实测并发 4 抓 6 个就触发了限流。

**教训**：
- **"能重试"不等于"该重试"。** 对外部站点，缓存不是性能优化，是**礼貌**，
  也是"我随时能复现"的前提
- **抓取和解析必须解耦**：抓一次落盘，之后所有规则迭代都在本地跑。
  这条同时省 token、省时间、不惹站点
- **假成功比报错更危险**：限流页有 200 状态码，如果只看 `res.ok`，
  就会把拦截页当正文抽出 0 条，然后自信地写"该区县无资料"。
  凡是"抓取结果为空"，都要先问一句**"是真没有，还是我没抓到"**
- 这一轮我把一个源用废了。**下次抓新站点，先用 `--limit=1` 试水，确认缓存写成功再放量**

#### 坑 #32：降权过滤器的"兜底失效"——把"最像但不是"的句子漏给了下游

**现象**：攀枝花三个区县的面积全被判为"抓错条目"：

| 区县 | 抓到的句子 | 真相 |
| --- | --- | --- |
| 米易县 | 「响水河…**集雨面积** 284 平方千米」 | 是河流集雨面积 |
| 盐边县 | 「该河干流长 38 千米，**流城面积** 616 平方千米」 | 河流流域面积（原文还有错别字"流城"） |
| 西区 | 「**石龙区**总面积 60.8 平方公里」 | 抓成了河南平顶山的石龙区 |

**根因**：面积抽取器给"流域面积/集雨面积"这类句子降权，然后**用 `score > 0` 过滤**。
当某个区县恰好没有真正的行政区面积句时，过滤后一个不剩 ——
这个"兜底"根本不是兜底：**它没能表达"我没找到"，而是让下游无法区分
"真没有"和"被过滤掉了"**，于是优先级最低的句子成了唯一可见的候选。

**修法**：给每条面积句加 `kind` 标记，**降权的句子也返回，但明说它不是行政区面积**：

```js
score > 0  → { text, kind: 'region' }   // 像是本行政区面积，可以用
score ≤ 0  → { text, kind: 'other' }    // 河流/工程/网格等，**不是行政区面积，不能用**
```

**两条教训**：
1. **降权 ≠ 丢弃。** 把判断依据（kind）传下去，比替下游做决定更安全 ——
   否则下游看到的是"空空如也"，无法判断是"真没有"还是"被过滤掉了"
2. **幸好有几何交叉校验兜底。** 这三条全被 `❗ 面积对不上` 拦下（0.13 / 0.19 / 0.50 倍）。
   **多重独立校验的价值就在这里**：抽取层的 bug 由校验层接住，没有污染数据

#### 坑 #33：landmark 里的名字必须原样出自素材，不能"用记忆补全"

**现象**：子代理产出里有两处 landmark 是**素材里根本不存在的名字**：

| 条目 | 子代理写的 landmark | 素材实际写的 |
| --- | --- | --- |
| 恩阳区 | 「恩阳古镇」 | 「**古镇内**既有连接川陕的米仓古道…」（从没出现"恩阳古镇"五个字） |
| 巴州区 | 「南龛摩崖造像」「北龛古建筑群」 | 「有东龛、南龛、西龛、**北龛等古建筑**，以及 7200 余尊摩崖造像」 |

**根因**：`facts-verify.js` 原本只检查 **evidence 是否出自素材**，
而子代理很"聪明"——evidence 抄的是真句子，但**填进 landmark 的名字是它用常识补的**。
这类错误查 evidence 查不出来，因为 evidence 本身完全合法。

**修法**：加一条硬检查 —— **landmark 的每个成分都必须能在素材里找到**
（按 `、，/` 拆成候选项，逐个在素材语料里搜索）：

```
✘ 巴州区：landmark 里的「南龛摩崖造像」、「北龛古建筑群」 在素材里找不到 → 疑似用记忆补充
✘ 恩阳区：landmark 里的「恩阳古镇」 在素材里找不到 → 疑似用记忆补充
```

**教训**：
- **"引用是真的"不等于"结论是真的"。** 模型可以在合法引用的掩护下塞进自己的知识。
  校验必须同时检查**结论本身**是否可追溯，而不只是检查它的出处
- 这条检查加完立刻抓出两个错 —— 而它们**在此之前全部通过了校验**。
  **守门人的规则是慢慢长出来的**：每发现一种新的造假形态，就把它变成一条断言
  （这也是为什么 `tools/test-facts-verify.js` 要专门测"该拦的必须拦住"）

#### 坑 #34：同一个抽取函数改到第四次，才想起给它写测试

**背景**：面积抽取器 `pickArea()` 前后出过**四次**同类问题，每次都是同一个后果
（某个区县被填成错误的面积）：

| # | 误命中的句子 | 是哪种面积 |
| --- | --- | --- |
| ① | 「中丘中谷地貌…分布面积仅 0.01 平方千米」 | 片区面积 |
| ② | 「响水河…集雨面积 284 平方千米」（米易县） | 河流集雨面积 |
| ③ | 「该河…流城面积 616 平方千米」（盐边县，原文还有错别字"流城"） | 河流流域面积 |
| ④ | 「东部丘陵区，面积 263.4 平方千米，**占全区幅员面积的 40.65%**」（德阳旌阳区） | 区内某片区 |

**问题不在"又漏了一种模式"，在流程**：前三次我都是"发现 → 改正则 → 继续跑"，
**从没把它钉成测试**。于是同一个函数被改了四次，每次都靠下游（几何交叉校验）兜住，
而不是靠测试拦住。**第四次才建 `tools/test-area-pick.js`**，一次性把四个真实案例固定下来。

**建了测试之后的额外收获**：顺手发现自己的用例写错了（用了"万亩/万公顷"，
而抽取器本来就只认"平方千米/平方公里"——**那是对的**）。
**没有测试时，我甚至无法判断"失败"是代码错还是用例错。**

**教训**：
- **改到第二次就该写测试了。** 第一次是"修 bug"，第二次是"这个函数有系统性风险"的信号。
  连续四次靠下游兜底，说明上游缺的不是正则，是**回归网**
- 判据要写进测试，不能只在注释里。"子区域面积要降权"这件事，
  写在注释里我第四次还是漏了；写进测试用例（`占…面积 X%`）它才会一直生效
- 测试用例本身也要审：**用错的单位去测对的逻辑，会得出"代码有 bug"的假结论**

#### 坑 #35：质量退化是"渐进"的——获奖名单会冒充"介绍"

**现象**：流水线跑到泸州/德阳/眉山时，`tagline`/`funFact` 大面积退化成两类内容：

| 类型 | 实例 |
| --- | --- |
| 地理志 | 「境内有条形山脉 2 支」「年平均降雨量 1184.2 毫米」「境内大小断层近百处」 |
| 获奖名单 | 「入选"成渝十大文旅新地标"」「上榜 2024 年全国县域旅游发展潜力百佳县」 |
| 行政统计 | 「辖 3 个街道 7 个镇 2 个乡 58 个村 39 个社区」 |

**根因（两条，都要治）**：

1. **指令的措辞在引导它**。原指令写"从 `factual` 里挑特点"——
   "特点"这个词很自然地被理解成"地理特征"，于是子代理大量选用地形/降雨/断层。
2. **没有给"不要什么"的清单**。只说"要有意思"是主观的；
   明确列出**禁止类型**（地理志/统计/获奖名单/纯沿革）才可执行。

**修法**：把取舍标准改写成**可操作的禁止清单 + 优先清单**，
并在开头点明使用场景（"玩家点击一块碎片会看到这三行字 —— 判断标准不是'对不对'，
而是'有意思吗、能让人记住这个区县吗'"）。

**教训**：
- **给模型的"质量标准"必须可执行。** "要有意思"没法执行；
  "不要写年平均降雨量、不要写辖几个村、不要写入选某某名单"可以执行
- **质量会随批量渐进下滑**：单看一个市还过得去，跑三个市就显形了。
  所以**每批都要抽查**，不能因为"前两批通过校验"就跳过过目
- **校验器管不了这个**：这些句子全都"可追溯、非编造"，能过所有机械检查。
  **机器的边界就在这里** —— 它保"真"，保不了"好"

#### 坑 #36：面积抽取器第五次踩坑（"某个具体对象的面积"）

**现象**：并行跑雅安批次时，一次误判了 4 个县：

| 区县 | 抓到的句子 | 那是什么的面积 |
| --- | --- | --- |
| 雨城区 | 「砂石集中**开采区**1 个…面积为 24.67 平方千米」 | 砂石开采区 |
| 汉源县 | 「其中万里**工业园**规划面积 7.2 平方公里」 | 工业园区 |
| 石棉县 | 「位于石棉县蟹螺**乡江坝村四组**…面积 24 平方公里」 | 一个村组 |
| 天全县 | 「**红星村**坐落于国道 318 线旁，面积 59.6 平方千米」 | 红星村 |

共同特征：句子里的面积讲的是**一个具体对象**（园区/村/开采区），而不是整个行政区。

**修法**：加"具体对象词"降权 —— 出现 `工业园|产业园|开发区|开采区|矿区|园区|景区|
自然保护区|基地|规划面积|建成区|新区|片区`，或 `村/乡/镇/街道/社区 + 面积|坐落|位于` 的，
一律降到 `other`。

**这一次的关键不同：有回归测试兜着。**
坑 #34 建的 `tools/test-area-pick.js` 在这之前就存在，
所以修完能**立刻验证没有把正常句子一起打掉**（17 项 → 补到 21 项全绿）。
前四次都是"改完就上线、靠下游几何校验兜底"；这一次是"改完先跑测试"。
**这就是坑 #34 里说的"改到第二次就该写测试"的实际回报。**

**另外两处顺手修的：**
- 校验器的 `normalize()` 没处理 `\uXXXX` 残留（来自内联 JSON 的正文会带），
  导致"证据看起来一样却比对失败"（石棉县被误判疑似编造）→ 加解码
- 实测确认校验器**没有过严**：资阳乐至县的 evidence 前半部在素材里确实找不到
  （子代理把两句素材**拼接润色**了）。**校验器拦对了**，退回重做即可

#### 坑 #37：规模化跑批把站点打到限流——"小批 + 长间隔"才是对的姿势

**现象**：一轮抓甘孜（18 个县，`--delay=1800`）时，**14 个被限流**（只有 4 个成功）。
凉山也掉了 3 个。我还在后台反复等那个已经被限流的任务 —— 纯浪费时间。

**根因（两层）**：
1. **间隔太短 + 数量太大**。1.8 秒 × 18 个，对百科镜像这种站点太多了。
   计数上：限流触发后我的重试逻辑又追加请求（每个词条最多 4 次），
   **越被限流打得越狠** —— 自我放大。
2. **产物被覆盖**。`facts-batch` 每次直接覆写 `out/facts-<市>.json`，
   所以分小批抓时，**第二批会把第一批的结果冲掉**（缓存里明明有页面，产物却丢了）。
   我当时只能手工用 python 合并 4 个临时文件 —— 这正说明流程有缺陷。

**修法（两条，都落地了）**：

```bash
# ① 分小批 + 长间隔（每个词条最多重试 1 次，失败就跳过、之后再来）
node tools/facts-batch.js --map=ganzi --only=<adcode1>,<adcode2>,<adcode3> --delay=7000
# 批之间再 sleep 25s
```

```js
// ② facts-batch 改为**按 adcode 增量合并**，不再覆盖
//    并且：新结果抓取失败、旧结果成功时 → 保留旧的成功结果
//    （限流是暂时的，不该因为一次重抓失败就把已拿到的内容丢掉）
// 加 --fresh 才强制只保留本次结果
```

**效果**：分 4 小批抓完后，一条 `--delay=0` 全量重抽（全走缓存）**0.3 秒拿到 18/18**。

**教训**：
- **批量抓外部站点，"总间隔"比"单次间隔"更重要。** 18 个词条 × 1.8 s 不足以避开限流；
  改成分批（每批 3 个）+ 批间 25 s 就稳了。**宁可慢，别被封**
- **重试要克制。** 被限流时重试等于加码。**"失败就跳过、稍后再来"比"当场硬重试"安全得多**
- **产物必须可增量累积。** 任何"分多批完成"的流程，产物都不能是"每次覆盖"——
  否则中间被中断就前功尽弃。这一条对"要跑几百个城市"的项目是硬要求
- **别在已被限流的任务上等待。** 我那次反复 `job_output wait` 等了十分钟，
  而它只是在一遍遍撞墙。**看到限流信号就该立刻改策略**

#### 坑 #38：`collision` 是弱信号，不能当硬性拒绝条件

**现象**：凉山批次有 4 个条目被判定"疑似同名词条撞车"而整条拒掉 ——
西昌市、会理市、会东县、喜德县。但**西昌市是凉山州首府**，页面里不可能不提"凉山"。

**根因**：我的归属校验只用了一个信号——"正文里有没有出现所属市名"。
但实测这几条的正文**压根不写"凉山"二字**（很多条目直接写"位于四川省…"或不写上级），
而它们的**面积与几何完全一致、地名也对**。

**真正的撞车信号是面积比值，不是"上级市名在不在"。**
对比两个案例：

| 案例 | 上级市名 | 面积比值 | 真相 |
| --- | --- | --- | --- |
| 西昌市（凉山） | ❌ 没出现 | ✅ 一致 | **正常条目**（被误拒） |
| 市中区（乐山，实为内江） | ❌ 没出现 | ❌ 0.05 倍 | **真撞车** |

**修法**：`collision` 从"硬性拒绝"降级为**提醒**，真正的拦截交给面积比值。

**教训（这条最值得记住）**：
- **一个弱信号被当成硬条件，会造成批量误杀。** 而且误杀是**静默**的：
  那 4 条只是"没被填"，看起来像"素材不够"，不会有人去查
- **判断"这条素材能不能用"，要用能区分案例的信号。** "上级市名没出现"在正常条目里也成立，
  所以它区分不了；"面积比值差 20 倍"能区分
- 降级一个检查时，**要同步更新它的测试用例**（我把"必须拦下"改成"不拦但给出提醒"，
  并补了一条"真撞车靠面积拦"的用例），否则测试会拦着正确的修复

---

#### 坑 #39：裁剪过的数据，只"传进去"还不够 —— 要断言它真的落进了产物

**现象**：`tools/bundle.js`（单文件离线打包）做完，命令行打印"含 22 张地图、22 条登记项"，
文件也生成了，看起来一切正常。但实际打开产物，地图选择器里**仍然是全国 363 张**。

**根因**：一行参数传错了。

```js
const pruned = pruneRegistry(registry, ids);
const html = inlineIndex(html, registry, ids);   // ← 传的是原始 registry，不是 pruned
```

`pruneRegistry` 老老实实算了，`console.log` 也打印了裁剪后的条数（那是从 `pruned` 读的），
但真正写进 HTML 的是**没裁剪的原始登记册**。两个变量名很像，编译器不会拦，
运行时也不报错 —— 产出一个"能打开、切地图就崩"的 HTML。

**为什么这个 bug 特别危险**：这份 HTML 是要**发给别人**的。
对方点开一张没打进包的图 → loader 去注入一个不存在的脚本 → 报错。
而对方不会回来告诉你哪里错了，他只会关掉。

**修法**：修那一行；同时把断言从"命令行打印了什么"改成**"产物里有什么"**：

```js
/* 产物里内联的登记册必须真的被执行一遍，再检查条目数 */
const m = /var REGISTRY = (\{[\s\S]*?\});\n  global\.MAP_REGISTRY/.exec(html);
vm.runInNewContext('...global.MAP_REGISTRY = ' + m[1] + '...', { window: sandbox });
check('内联登记册的条目数与裁剪结果一致', Object.keys(sandbox.MAP_REGISTRY.maps).length === ids.length);
check('内联登记册里没有北京（说明确实裁过）', !sandbox.MAP_REGISTRY.maps.beijing);
```

**教训（可迁移）**：
- **日志不是证据，产物才是证据。** 我打印的条数来自另一个变量，所以日志是"绿"的
- **"裁过的数据"要有一条"反向断言"** —— 不只查"该在的在"，还要查"**不该在的不在**"
  （`!sandbox.MAP_REGISTRY.maps.beijing` 这一条才是真正抓到 bug 的那条）
- 名字相近的中间变量（`registry` / `pruned`）在"传参"位置最容易搞混：
  **算完立刻用，别隔几行再用**

---

#### 坑 #40：`--virtual-time-budget` + `--dump-dom` 会让 Chrome 挂住

**现象**：想验证单文件包能不能真的跑起来，用
`chrome --headless=new --virtual-time-budget=9000 --dump-dom file://...`，
结果**一直不返回**，最后被 SIGTERM 杀掉，输出文件是 0 字节。

**根因**：`--virtual-time-budget` 会把定时器"快进"，而这个页面里有
`setInterval`（计时器）+ 开场动画 + 音效调度。虚拟时间预算到了以后 Chrome
不一定退出 —— 它等的是"没有 pending task"，而周期定时器永远有。

**修法**：不带 `--virtual-time-budget` 直接 `--dump-dom`（load 事件后立刻 dump，
而地图加载在产物里是同步内联的，load 时已经就绪）；再套一层"后台跑 + sleep 后 kill"，
不要指望它自己退出：

```bash
(chrome --headless=new --dump-dom "file://$PWD/out/x.html" > /tmp/dom.html 2>/dev/null &
 CPID=$!; sleep 22; kill $CPID 2>/dev/null; wait $CPID 2>/dev/null)
```

**教训**：**用一个会挂住的探针去验证，等于没有探针。**
给任何外部工具加参数前，先确认它"什么条件下会退出"。

---

#### 坑 #41：单浏览器跑完 363 张地图会退化 —— "跑得越久越慢"不是错觉

**现象**：冒烟套件（逐张打开每张地图、真的拖一块进去）在 363 张地图上跑，
**1800 s 都没跑完**。但把同样的 363 张拆成两半、各起一个 Chrome，两半分别 176 s / 355 s
就跑完了，加起来 531 s —— 也就是说**同样的工作量，一次跑完要慢 3 倍以上**。

**根因**：一个 Chrome 进程里连开 363 个 iframe（每个都装着一份完整页面 + 几百 KB 的
GeoJSON），内存持续增长后再也没回落。实测每张地图的耗时从 1.5 s 漂到 5 s 以上。

**为什么这个坑特别坑人**：
- 超时本来是给"卡死"兜底的，结果变成"地图接多了就红"—— 而地图只会越来越多
- 页面**只在最后回传一次结果**，所以卡在第 300 张时，前面 299 张的一万多条断言**全丢**
- 中途没有任何输出，看起来就是"卡住了"（这正是我反复被问"怎么又卡住了"的原因）

**修法**：`tools/e2e-test.js` 给可分批的套件加 `batchable: true`，
每批 `SMOKE_BATCH`（默认 60）张地图、一个独立 Chrome、一个独立超时：

- 每批稳定 **88.5 s**（1.47 s/张），全程 536 s，不再退化
- 卡死只丢一批；某批崩了会**继续跑后面的批次**，最后一起报（可能是地图的问题，
  也可能只是浏览器偶发，不该因为一次偶发放弃剩下的）
- 顺带把通过的日志默认关掉：1.3 万条 ✔ 只会把要看的东西挤出 `tail` 的窗口

**教训（可迁移）**：
- **"测试越来越慢"要当成 bug 查，不要当成常态忍受。** 一旦开始忍受，
  下一步就是"不跑了" —— 而不跑的测试等于没有测试
- **长跑任务要能增量交付结果**：只在最后回传一次的测试框架，
  和一个没有断点续传的爬虫是同一类问题
- 超时值不该随规模一起涨。**规模涨了，要拆，不是把闸门抬高** ——
  抬高闸门只会让"真卡死"和"跑得慢"变得无法区分

---

## 五、开发 SOP

> **接手顺序建议**：先读根目录 `HANDOVER.md`（含 §1.5 进度快照）→ 跑 `node tools/status.js`
> → 再按需查下面的小节。**不要通读全文**。
> 架构与可移植性另见 `docs/架构与可移植性评估.md`；方法论 checklist 见附录 B。

### 5.1 每次动手前的三步

1. **读相关文件**，理解现有结构和风格（不要凭印象改代码）
2. **用数据验证假设**（尤其是"我觉得这里是对的"这类判断）
3. **说明改哪里、为什么**，再动手

### 5.2 改动流程

```
改代码 → node --check 语法 → node tools/e2e-test.js（两套都要绿）→ 截图目视 → 汇报
```

**关键：改完必须跑测试。** 这个项目的 166 项断言逮住过多个我自己没意识到的 bug —— 包括两个静默错误，以及引擎化重构时的 GeoJSON 兼容问题。

### 5.3 遇到"看不到效果"时的排查顺序

```
1. 打印实测数据（宽度 / 坐标 / computedStyle）   ← 命中率最高
2. 查 DOM 实际结构（dump-dom）
3. 查控制台报错
4. 最后才怀疑 CSS 写法
```

坑 #12 就是典型：前三次都靠"改 CSS 试试"，全部无效；**打印数据一次命中**。

### 5.4 如何加一个新关卡

1. `js/districts.js` 的 `LEVELS` 里加一项（`id` / `name` / `short` / `blurb` / `adcodes`）
2. 配色二选一：在 `js/cities/chengdu.js` 的 `palette.hueByLevel` 里加一个色相，**或者**直接给这个关卡写 `hue: 200`（关卡自带的 `hue` 优先级更高）
3. 确认 `adcode` 在 `map-data.js` 里存在（跑测试会校验）
4. 若关卡数量变化，检查 `unlocked` 相关逻辑与测试断言

### 5.5 如何加一套新主题

1. `css/style.css` 的 `:root[data-theme='xxx']` 里覆盖颜色令牌（**只需覆盖颜色类，圆角/缓动沿用默认**）
2. `index.html` 的主题切换区加一个色点按钮
3. `js/cities/<city>.js` 的 `themes.list` 里加 id（还可以用 `themes.fallback` 指定默认主题）
4. 注意：暖色/红色在深底上**视觉亮度天然低于青色**，描边色需要比青绿主题提亮一档

### 5.6 如何加一个新图案

1. 在 `index.html` 的 `<svg class="svg-sprite">` 里加 `<symbol>`
2. **颜色走 `currentColor` 或 CSS 变量，不要在 symbol 里写死 fill**（否则外部改不动，见坑 #14）
3. 在 `tools/icon-preview.html` 里放大检查三种底色下的效果
4. 确认在目标显示尺寸下线宽够（可用 `线宽 × 显示尺寸 / viewBox 尺寸` 估算）

### 5.7 如何重新生成地图数据

新地图（或刷新某张地图的边界）走自动化脚本，**不要手写**：

```bash
# 接入一张新地图（父级没接入会自动补全祖先链）
node tools/add-map.js --adcode=510300 --name=zigong --parent=sichuan

# 只想刷新某张地图的边界，人工写的资料一个字都不动
node tools/add-map.js --adcode=510100 --name=chengdu --parent=sichuan --geo-only

# 先看它打算干什么，不联网、不写盘
node tools/add-map.js --adcode=510300 --name=zigong --parent=sichuan --dry-run
```

成都这张图还留了一个薄封装（内部和 `add-map.js` 共用同一份转换逻辑）：

```bash
curl -o /tmp/cd_full.json https://geo.datav.aliyun.com/areas_v3/bound/510100_full.json
node tools/build-data.js /tmp/cd_full.json
```

> 自检小技巧：重构构建脚本后，用同源数据重新生成一次，
> 和旧产物做**逐字节比对**——一致才说明重构没改变行为。

### 5.8 怎么跑测试

```bash
node tools/e2e-test.js
```

- 一次跑三套 + 两项离线检查，末尾打汇总。正常输出是 **`合计：882 通过 / 0 失败`**（城市回归 89 + 引擎功能 77 + 多地图冒烟 585 + WKT 13 + 地图包自洽 118）
- 每套会打印耗时（23 张地图时冒烟套件约 30s）；超时按套配置，见 `SUITES` 里的 `timeoutMs`
- 只想过其中一套：把别的从 `tools/e2e-test.js` 顶部的 `SUITES` 数组里注掉即可
- 脚本里带的 `--no-sandbox` 是**这台机器必需的**（见坑 #2），换机器可以去掉
- 测试失败时会打印布局诊断（各层宽度、`elementFromPoint` 命中结果），便于快速定位
- 每套测试各起一个独立 Chrome（独立 `user-data-dir`），因此两边的 `localStorage` 互不可见 —— 引擎套件里"没有污染 `chengdu-*` 存档 key"那条断言就是这么成立的
- **新增地图后必须跑一遍**：第三套会自动把登记册里**每一张**地图都打开、真的拖一块进去，验"生成物能不能玩"

三套的分工：

| 套件 | 被测页 | 管什么 |
| --- | --- | --- |
| `tools/selftest.html` | `index.html` | 成都这张图的全部 UI/动画细节、方位、存档、主题、键盘 |
| `tools/engine-test.html` | `tools/engine-host.html` | 引擎通用性（只用虚构 tiny-city，断言里不含真实地名） |
| `tools/map-smoke.html` | `index.html?map=<id>` | **地图工厂的验收**：每张地图都真能加载、数据自洽、能拖进去、导航正确、刷新后进度还在；外加窄屏（390×844）与"点选切换/面包屑返回"专项 |

> 第三套是"文件生成了 ≠ 东西能用"的那道闸。它第一次运行就抓出了两个真问题
> （全国数据里的非行政区 feature 把资料文件写成了非法 JS；以及测试自己跟 300ms 吸附动画抢时间）。

### 5.9 如何加一张新地图（一条命令 + 补文案）

完整规范见 [3.5 世界地图层级规范](#35-世界地图层级规范地图工厂)，动手流程就两步：

**第一步 · 脚本生成骨架**

```bash
node tools/add-map.js --adcode=510300 --name=zigong --parent=sichuan
```

它会补全祖先链、下载边界、生成三件套、更新 `registry.js`，最后打印一张"还需人工补什么"的清单。

**第二步 · 人工补内容**（脚本编不出来的部分）

| 要补的 | 在哪个文件 | 说明 |
| --- | --- | --- |
| `area` / `landmark` / `tagline` / `funFact` | `<id>.data.js` | 面积、地标、一句话介绍、冷知识 |
| 关卡分组与 `blurb` | `<id>.data.js` | 脚本只按"每 8 个一组"机械切分，好玩的关卡要按地理/文化逻辑重排 |
| 主色调 | `<id>.js` 的 `palette` | 脚本按 adcode 派生了一个稳定色相，觉得不好看就改 |

**注意**：`.data.js` 与 `.js` 已存在时脚本**永不覆盖**，所以你补的内容是安全的；
重新下载边界请用 `--geo-only`，**不要**用 `--force`（那会连人工文件一起冲掉）。

然后跑测试：

```bash
node tools/e2e-test.js            # 166 项必须全绿
```

调试单张地图：`index.html?map=<id>` 直接打开即可（如 `?map=zigong`）。

### 5.10 如何写一套 fixture 测试（新玩法）

假数据是**通用性的探针**：它跑得通，才说明引擎不是"只对成都有效"。

1. **写数据**：`tools/fixtures/<name>.js`，规模压到极小（这里就 3 个假区县 + 2 关），**并且刻意偏离真实配置** —— 不同色相/饱和度、不同默认主题、不同存储 key、不同城名。只有这样才能证明这些值真的"由外部传入"
2. **写宿主页**：`tools/engine-host.html`，**DOM 结构与 `index.html` 同构**（引擎按 id 缓存 DOM，CSS 布局依赖 `.app / .stage / .board-column / .board` 这套层级），然后三行启动：
   ```js
   window.__ENGINE__ = MapPuzzleEngine.create(window.TINY_CITY_CONFIG);
   window.__ENGINE__.start();
   ```
3. **写套件**：`tools/engine-test.html` —— 报告机制与 `check / section / drag / insidePointOf` 这套辅助函数和 `selftest.html` 同款，但**期望值全部由 fixture 反推**，断言里不出现任何真实城市的名词
4. **接进驱动**：在 `tools/e2e-test.js` 的 `SUITES` 数组里加一项 `{ name, page }`
5. **顺手把边界条件塞进 fixture**：凹多边形、两种 GeoJSON 写法、单块关卡…… 这些极端情况放在假数据里最合适（[坑 #22](#坑-22几何层只认-multipolygonpolygon-直接抛错)就是这么被逮住的）

### 5.11 批量地图操作手册

单张地图用 `add-map.js`（见 5.9）；**一次接入一整批**用 `batch-add-maps.js`。
两者的关系是"调用"而不是"复制"：批量脚本直接 `require` 了 `add-map.js` 抽出来的
`addMap()` 函数（`add-map.js` 现在同时是 CLI 和一个库），所以单张与批量永远走同一套逻辑。

#### 一、批量接入一个省

```bash
# 先看计划（不联网、不写盘，纯列清单）
node tools/batch-add-maps.js --parent=510000 --dry-run

# 正式跑：接入四川省下辖的全部 21 个市州
node tools/batch-add-maps.js --parent=510000

# 只要其中几个
node tools/batch-add-maps.js --parent=510000 --only=511100,511300
```

传的是**父级 adcode**（这里是 510000 = 四川省）。脚本会：

1. 确保父级已接入；没接入就先把它补出来（它自己也会向上补，直到中国）
2. 下载 `bound/{adcode}_full.json`，取出全部子级（**一次请求就够**，不是每个子级一个请求）
3. 逐个调用 `addMap()`：算目录 → 下载该子级的 `_full.json` → 生成三件套
4. 最后**统一重建一次** `registry.js`（批量时只重建一次，不是 21 次）
5. 写 `batch-report.json`

> **一个市的区县同理**：`node tools/batch-add-maps.js --parent=510100` 会把成都的 20 个区县
> 各生成一张地图包。但区县已经是最下一级了 —— 它们的 `_full.json` 不存在，会退化成
> "只有自己一个 feature"，拼图只有 1 块。**所以批量接"市"才是常规用法。**

#### 二、目录名（slug）是怎么来的

地图包的 id / 目录名是英文（`zigong`），而**拼音没法从 adcode 或中文名推出来**，
除非引一个拼音库（违背零依赖）。所以：

| 层级 | 命名来源 |
| --- | --- |
| 省级 | 内置 34 条省表（adcode 前两位 → slug），见 `tools/lib/slugs.js` |
| 地级 | 内置地级表 `CITY_SLUGS`（**按省逐步补**，目前是四川 21 个） |
| 查不到 | 兜底成 `map-<adcode>`，并写进报告的 `fallbackSlugs` 提醒人工改名 |

**新增一个省时的动作**：往 `tools/lib/slugs.js` 的 `CITY_SLUGS` 里加那个省的地级市，
几行而已，不动任何逻辑。忘了加也不会失败 —— 只是目录会叫 `map-440300` 这种名字。

#### 三、报告怎么看（`batch-report.json`）

| 字段 | 含义 |
| --- | --- |
| `parent` | 父级 adcode / slug / 中文名 / 数据来源 URL |
| `summary` | `children` 子级总数、`created` 新建、`existing` 已接入跳过、`failed` 失败 |
| `maps[]` | 每张地图一行：状态、下级行政区数、关卡数、跳过的非行政区 feature、文件路径 |
| `failures[]` | 失败的 adcode / 名称 / 完整错误（**这些是跳过的，没重试**） |
| `needsHuman[]` | **待人工补的清单**：哪个文件、多少个下级行政区、要补哪些字段 |
| `dataGaps[]` | 数据缺口：没有下级的、含非行政区 feature 的 |
| `fallbackSlugs[]` | 用了兜底命名、建议改成拼音的 slug |
| `registry` | 重建后的地图总数、根、孤儿、告警 |

跑完先看 `summary.failed` 和 `needsHuman` 这两个字段，其余是排查时才翻的细节。

#### 四、数据缺失怎么处理

三种情况，处理方式完全不同 —— 别把它们混成一句"数据不全"：

| 情况 | 表现 | 对策 |
| --- | --- | --- |
| **下载失败**（网络 / 404） | 该地图 `status: "failed"`，出现在 `failures[]` | 脚本**记下、跳过、不重试、不死循环**；回头**再跑一次同样的命令**即可（已接入的会被识别成 `existing` 跳过，只有失败的那几个会重下） |
| **该层级没有下级区划** | `dataGaps` 里 `kind: "no-children"` | DataV 上 `<adcode>_full.json` 不存在，退化成"只有自己"。**说明这一级就是叶子**，不该单独做成一张拼图 |
| **混着非行政区 feature** | `dataGaps` 里 `kind: "non-admin-feature"` | 典型是全国数据里的南海九段线（adcode 是字符串 `100000_JD`）。它**画在底图上，但不进关卡、不进资料卡** —— 这是刻意的，见坑 #24 |

> **红线：不要 `--parent=100000`。** 脚本里硬编码拒绝了整个中国：
> 34 个省级 × 各自的下一级 = 数百张地图，规模远超"批量"该有的样子。
> 正确姿势是一个省一个省来。

#### 五、怎么补卡片资料（面积用脚本，文字靠人）

> **2026-09 更新：238 条面积已全部补齐，面积这一步不用再做了。** 流程保留在下面，接新省时照用。

**第 0 步（脚本，一次做完）：批量补面积**

天地图官方行政区划数据只有 `adcode / name / center`，**不带面积**；逐个去别处抄既慢又容易混口径。但面积本来就是边界的属性 —— 用项目自带的官方边界直接算即可：

```bash
node tools/area-from-geo.js --check                  # 校验计算方法（拿公布值当锚点，5/5 通过）
node tools/area-from-geo.js --only=leshan            # 预览（默认不写盘，推荐先看）
node tools/area-from-geo.js --only=leshan --write    # 落盘
node tools/area-from-geo.js --parent=510000 --write  # 按省批量（= 21 个市州）
node tools/area-from-geo.js --parent=511900 --write  # 按市（叶子地图 → 选中它自己的区县）
node tools/area-from-geo.js --overwrite-human ...    # ⚠️ 连人工填的面积一起覆盖，一般不用
```

它**只改 `area`**，`landmark / tagline / funFact` 一个字都不碰；写入前对每个文件做语法预检，并会把「面积口径」写进文件头。

**三个安全设计**（都是踩了坑才加的）：

| 设计 | 防的是什么 |
| --- | --- |
| `area` 行带 `[geo-area]` 标记才算"机器产物" | 人工核实过的面积被静默覆盖（坑 #30） |
| 父子关系走包配置的 `parent`，不看 adcode 前缀 | `--parent=510000` 选不中任何市州（坑 #29） |
| 只有真写了计算值的文件才加口径声明 | 给纯人工文件贴假溯源 |

实测精度（与公布值比对，`--check` 会逐条跑）：

| 锚点 | 几何计算 | 官方公布 | 差 |
| --- | --- | --- | --- |
| 五通桥区 | 465 | 465（乐山市政府网） | 0 |
| 峨边彝族自治县 | 2383 | 2382（同上） | 1 |
| 乐山全市合计 | 12742 | 12720.03（百科引用统计口径） | 22（0.17%） |
| 绵阳市 | 20256 | 20200（百科引官方口径 2.02 万 km²） | 56（0.28%） |
| 南充市 | 12491 | 12482（同上） | 9（0.07%） |

> 差异来自**制图综合**（官方制图会略去小碎岛、简化边界）。所以产物在文件头声明为
> **"几何计算值"**，不冒充官方统计口径 —— 数据诚实比数字好看重要。
> 锚点存在 `tools/lib/geo-area.js` 的 `REFERENCE_ANCHORS`，加新省时可以往里追加。

**第 1 步（人工，不能外包给脚本）：填文字**

面积补完后，占位符长这样（注意 `area` 已经是有意义的数字）：

```js
"512002": {
  area: 1636,  // 由 tools/area-from-geo.js 依官方边界几何计算（km²）
  landmark: '📖 资料收录中，欢迎参与共建',
  tagline: '📖 资料收录中，欢迎参与共建',
  funFact: '📖 资料收录中，欢迎参与共建',
},
```

填文字的标准流程：

1. 跑 `node tools/status.js` 看还有几条占位（**按"条"统计**，混合文件不会被漏掉）
2. **按 adcode 逐个填**（`landmark / tagline / funFact` 三段文字）。
   **不要动 key（adcode）** —— 它必须和 `<id>.geo.js` 里的 feature 严格对应，
   写反了就会"拼对位置、弹出别人的介绍"（坑 #1）
3. **只写可核实的公开内容**：地名、所属、地标、命名由来、明确记载过的事件。
   核实不了就**保持占位文案**，不要用"大概是这样"凑数（铁律见 HANDOVER）
4. 关卡（`levels`）也要重排：脚本只按"每 8 个一组"机械切分，好玩的关卡应按地理/文化逻辑分组，并补上 `blurb`
5. 跑测试：`node tools/e2e-test.js` —— 冒烟套件会核对"每个下级行政区都有资料卡"、
   "关卡覆盖与 GeoJSON 完全一致"，**漏填或写错 adcode 会被逮住**
6. 只想刷新边界数据（不动你写的字）：`node tools/add-map.js --adcode=… --name=… --geo-only`
   —— **千万别用 `--force`**，那会连人工文件一起覆盖

> **进度指示**：`node tools/status.js` 会给「真实资料卡 N 条 / 共 M 条」和「待补 N 条」，
> 比 grep 可靠（旧的按文件口径会把"补了一半的市"两边都漏掉，已修）。
>
> **参考样板**：`js/maps/china/sichuan/chengdu.data.js`（首个人工版）与
> `js/maps/china/sichuan/leshan.data.js`（首个"脚本补面积 + 人工填文字"完整版，
> 文件头记录了全部资料来源）。

**一次补一个市的实操节奏**（乐山 11 个区县实测约 1 轮对话）：

```bash
node tools/status.js                                      # 1. 挑一个市，看缺口
node tools/area-from-geo.js --only=<市> --write            # 2. 面积一次到位
node tools/facts-batch.js --map=<市> --draft               # 3. 批量抓素材 → out/（见第九节）
# 4. 人工核实素材 → 写进 js/maps/china/sichuan/<市>.data.js
node tools/test-maps.js 2>&1 | tail -4                     # 5. 自检
node tools/e2e-test.js 2>&1 | tail -6
git add -A && git commit -m 'feat: <市> 资料卡补齐'
```

#### 九、token 效率：为什么"一轮一个区县"是错的

> 这一节回答一个具体问题：**批量补资料时，怎么在保持质量的前提下少烧 token。**
> 结论先给：**省 token 的关键不是省带宽（页面大小），而是省"轮数 × 每轮都要过大模型"。**

**先量化。** 用老办法补一个市（以乐山 11 个区县为例）实际发生的开销：

| 环节 | 老办法 | 实测 |
| --- | --- | --- |
| 联网抓取 | 每区县一次 tool call | ~22 次 |
| 搜索引擎 | Bing 找页面 | ~14 次（且中文长 query 漂移严重，多半是噪音） |
| 页面消化 | 每次 dump 2–6 KB 进上下文 | ~2.5 KB × 22 |
| 我的推理 | 每轮都要读一遍再挑句子 | 22 轮 |

**单页原网页 941 KB，但真正进上下文的只有几 KB** —— 所以瓶颈不是"页面大"，
而是**"一个市几十轮 tool call，每轮都要重新联网、重新截断、重新过一遍模型"**。
更要命的是：**越调抽取规则，越要重跑全量**，于是同一批页面被反复抓了十几遍。

**改造后的分工（三层，各管一段）：**

```
① 抓取 + 抽取   → 脚本（tools/lib/facts-source.js）      不占上下文
② 结构化素材     → out/facts-<市>.json                    落盘，可复用、可复跑
③ 判断 + 落笔    → 人（或人复核过的子代理）                占上下文，但只读短结论
```

一条命令代替几十轮：

```bash
node tools/facts-batch.js --map=zigong --draft     # 6 个区县：1 次调用，约 15 秒
# 主上下文只收到 6 行摘要；原文留在 out/facts-zigong.json 与 out/draft-zigong.md
```

**实测对比（自贡 6 个区县）**：

| | 老办法 | 新流水线 |
| --- | --- | --- |
| 主上下文收到的调用结果 | 22 条 × 2.5 KB ≈ **55 KB** | 1 条摘要 ≈ **0.6 KB** |
| 需要我推理的轮数 | ~22 轮 | 1 轮（读摘要）+ 核实 |
| 同一页面重复抓取 | 每次调规则都重抓 | **磁盘缓存，同一 URL 永不重抓** |

**三条硬机制（都是踩坑后加的，不是设计洁癖）：**

1. **磁盘缓存**（`.cache/facts/`，按 URL sha1）：同一份内容绝不抓第二次。
   起因：调抽取规则时反复重跑，把站点打限流了。
2. **限流识别 + 退避**：正常词条页 200 KB–1 MB；若返回 <50 KB 就判为限流页，
   退避重试，重试仍失败就**如实报 `⏳ 被限流`**。
   —— 绝不能把限流页当正文，那样会得到"这个词条没资料"的**假结论**。
3. **几何面积交叉校验**：抓来的面积句与本地几何面积比对，
   比值不在 0.75–1.35 就标 `❗`。实测拦下了"乐山市市中区"抓到"内江市市中区"
   （文本 387.5 vs 几何 840，比值 0.46）—— **脚本直接拦下，不用靠人肉眼扫。**

**质量没有降级**，因为：
- 文案字段的来源仍是"可核实的公开来源"，脚本只**收集候选句**，不生成结论
- 抽查/交叉校验前移到脚本（面积比对、同名词条归属校验）
- 最终落笔仍要人过目 `out/draft-<市>.md`

**踩过的坑（已写进坑 #31）：**

> 调抽取规则时我反复重跑同一个市的抓取，把百科镜像站点打成了 IP 级限流
> （之后所有请求返回 6 KB 拦截页，直至完全不可达）。
> **教训：抓外部站点时，"能重试"不等于"该重试"** —— 抽取规则应该对着**已落盘的缓存**
> 调，而不是对着线上反复抓。缓存不是优化，是**礼貌**，也是稳定性的前提。

**给未来的建议：**
- 新增省/市时：先 `facts-batch` 抓一次落盘，调规则只对 `out/` 和 `.cache/` 复跑
- 抓取间隔默认 1.5 s（`--delay=` 可调），别贪并发

---

#### 十、第三层：把"判断"也委派出去（附防编造机制）

> 第九节把"抓取"踢出了主上下文；这一节把**"判断"**也踢出去。
> 但委派有代价：**子代理可能编**。所以这一层的核心不是"怎么让子代理干活"，
> 而是**"怎么机械地核对它干得对不对"**。

**三个角色，各管一段：**

| 角色 | 职责 | 是否占主上下文 |
| --- | --- | --- |
| 脚本 `facts-batch` | 接触外部世界（抓取、抽取、落盘） | 否（只回一行摘要） |
| 子代理 | 读素材、逐条判断、产出结构化草稿 | 否（它在自己的上下文里读素材） |
| 脚本 `facts-verify` | **机械核对**子代理产出 | 否（只回通过/不通过） |
| 人 | 过目 + 落笔 `data.js` | **是**（但只读结论） |

**操作流程（自贡实测，6 个区县）：**

```bash
node tools/facts-batch.js --map=zigong --delay=3000 --draft --prompt
# → out/facts-zigong.json（素材）out/draft-zigong.md（给人看）
#   out/prompt-zigong.md（给子代理的自包含指令）

# 把 out/prompt-zigong.md 整段交给一个子代理（它只读素材，禁止联网）
# 子代理返回一个 JSON，存成 out/verify-zigong.json

node tools/facts-verify.js --map=zigong --in=out/verify-zigong.json
# → 逐条核对：每个字段能否追溯到素材原句？面积是否与几何一致？
#   素材被判不可信的条目有没有偷偷写进正文？
```

**防编造机制：每个字段都要能追溯到一句原文。**

子代理必须为每个填了的字段附 `evidence`（素材里**原样存在**的句子），
`facts-verify.js` 把它规范化后与素材全部候选句比对 —— **对不上就判"疑似编造"，不通过**。
这套约束不是"请勿编造"这种口头要求，而是**可执行、可测试**的规则：

| 拦下的情况 | 实测结果 |
| --- | --- |
| evidence 在素材里找不到 | ✘ 判"疑似编造" |
| 面积与几何不一致 | ✘ 拦下 |
| 撞车条目写进正文 | ✘ 拦下（应进 `rejected`） |
| 抓取失败的条目写进正文 | ✘ 拦下 |
| 用占位文案凑数 | ✘ 拦下 |
| 填了内容但没给 evidence | ✘ 判"无法追溯" |
| 合规产出 | ✔ 通过 |

**这些规则本身有测试**：`tools/test-facts-verify.js` 造 7 种输入，
要求"该拦的必须拦住"（14 项断言，已接入 `e2e-test.js` 离线套件）。
理由：这个校验器是**守门人**，守门人自己坏掉，就会静默地把编造内容放进项目 ——
所以它必须被测试钉住。

**实测效果（自贡 6 个区县）：**

| 项 | 结果 |
| --- | --- |
| 抓取 | 6/6 成功，15.2 s（缓存命中后 0.1 s） |
| 面积交叉校验 | 3 个 ✅ 对得上；3 个素材无面积句（用几何值） |
| 子代理产出 | 5 条填了 14 个字段，1 条被拒（大安区素材为空） |
| 校验器 | ✔ 全部通过 —— 14 个字段全部可追溯 |
| 落笔 | 5 个区县写入，**大安区保持共建占位**（宁缺毋假） |

**重要：校验器通过 ≠ 内容够好。** 人工过目时抓到两个质量问题（都不是编造）：

1. 子代理给荣县填的冷知识是「沟内有一高约40米的巨石…石笋沟因此而得名」——
   **句子是真的，但它是"石笋沟"的得名，不是"荣县"的冷知识**。
   根因是抽取层只按"因…得名"匹配。已修：给得名句加 `kind` 标记
   （`region` = 区县名本身 / `featured` = 某个具体对象的得名），
   草稿里显式标注，避免子代理被误导。
2. 于是我把荣县冷知识换成了素材里更有说服力的
   「明洪武九年（1376年），降荣州为荣县，延续至今」。

> **结论：脚本能保证"可追溯"，保证不了"有价值"。**
> 前者交给机器（已自动化），后者仍要人过一眼 —— 但这一眼只花在看结论上，
> 不用再翻几十个网页。**这才是省 token 与保质量的正解：把体力活交给机器，
> 把判断力留给人，并且让人的判断有据可查。**

---

#### 十一、规模化：一步落笔，把"人读一遍"也省掉

> 第十节把"判断"委派出去了，但流程里还剩**最贵的一环：人把子代理的 JSON 读一遍**。
> 实测：一个市的 verify JSON 约 8 KB，**每一个市都要过我一遍**。
> 这一步省不掉的话，全国 300 多个地级市 × 8 KB 就是几 MB 的上下文——不可行。

**关键认识：人读的那一遍，绝大多数只是在确认脚本已经确认过的事。**

脚本已经程序化验证了：证据可追溯、面积与几何一致、没有编造、没偷渡不可信条目。
我读一遍真正新增的信息只有"内容好不好"——而那部分**机械校验本来也管不了**。

**所以：让脚本自己落笔，人只看异常清单。**

```bash
# 以前：3 条命令 + 我读 8 KB JSON + 我写一个落笔脚本
node tools/facts-batch.js --map=<市> --prompt
# （子代理产出 out/verify-<市>.json）
node tools/facts-verify.js --map=<市> --in=out/verify-<市>.json   # 校验
# 我读 JSON → 写 fill-<市>.js → 跑 → 提交

# 现在：2 条命令，我只看摘要
node tools/facts-batch.js --map=<市> --delay=2200 --prompt
# （子代理产出 out/verify-<市>.json）
node tools/facts-verify.js --map=<市> --in=out/verify-<市>.json --apply
```

`--apply` 的行为（全部有断言保护）：

| 行为 | 说明 |
| --- | --- |
| 只替换**仍是占位**的字段 | 已有人工内容的字段一个字都不碰（与 `area-from-geo.js` 同一套安全原则） |
| 落笔前做**语法预检** | 语法不过就整体中止，绝不留下跑不起来的 `.data.js` |
| 落笔后打印**逐条清单** | 明确写出哪几条写了、几个字段被跳过未覆盖 |
| `--dry` | 只看会改什么，不落盘 |

**实测收益（绵阳，7 个区县 / 14 个字段）：**

| | 旧流程 | 新流程 |
| --- | --- | --- |
| 进主上下文的验证产物 | verify JSON **8201 字节** | 落笔摘要 **约 300 字节** |
| 我的操作 | 读 JSON + 写落笔脚本 | 看摘要（异常才介入） |
| 省下的上下文 | — | **约 96%** |

**必须说清的代价（这不是白捡的）：**

`--apply` 把质量档位从"人工挑过"降到"机器验证过"。
具体差别：机械校验能保证**真**（可追溯、非编造、面积对），
但保不了**好** —— "地理志"（降雨量/断层）和"获奖名单"（入选某某百佳县）
这两类平淡内容**全部能通过校验**（实测泸州/德阳/眉山的批次就是如此）。

**权衡结论**：一条真实但平淡的 landmark，比一句"📖 资料收录中"有价值得多；
而且这些内容以后可以**挑着重新优化**（数据在文件里，随时可改）。
所以对"把全国铺完"这个目标，`--apply` 是唯一可行的档位。

> **可选的折中**：对已完成的重点市（成都/乐山）保持人工档；
> 对批量铺开的市用 `--apply` 机器档，之后按 `out/facts-*.json` 挑值得优化的重做。

---

#### 十二、并行：一轮对话推进多个市

> 十一节砍掉了"人读 JSON"，但流程里还是**一个市一次子代理调用**。
> 主上下文每轮都在等，且我的叙述/等待也是成本。这一节把它也解决掉。

**工具：`workflow`** —— 它能让**多个子代理并行干活**，而每个子代理的开销都在它们自己的
上下文里，主上下文只收**每个市 3 行摘要**。

```js
// 一次 fan-out 4~8 个市（真实用过的形式，见 git 历史里的 workflow 调用）
phase('并行核实');
const cities = ['guangyuan', 'suining', 'ziyang', 'yaan'];
const results = await parallel(cities.map((city) => async () => {
  const text = await agent(/* 读 out/prompt-<city>.md 并产出 out/verify-<city>.json */);
  return { city, summary: text.trim().split('\n').slice(0, 3).join(' | ') };
}));
return results.filter(Boolean);
```

**主上下文收到的回报（四个市的全部产出）：**

```json
[
  { "city": "guangyuan", "summary": "wrote out/verify-guangyuan.json | filled: 7 | rejected: none" },
  { "city": "suining",   "summary": "wrote out/verify-suining.json | filled: 4 | rejected: 船山区（…）" },
  { "city": "ziyang",    "summary": "wrote out/verify-ziyang.json | filled: 1 | rejected: 安岳县（…）" },
  { "city": "yaan",      "summary": "wrote out/verify-yaan.json | filled: 3 | rejected: 雨城区…汉源县…石棉县…天全县…" }
]
```

**约 400 字节换 4 个市的核实结果。** 之后逐个 `--apply`，也不需要我读内容。

**推荐的一轮节奏（实测可行）：**

```bash
# ① 批量抓取 + 生成委派指令（一条 bash，串行避免限流）
for m in <市1> <市2> … <市8>; do node tools/facts-batch.js --map=$m --delay=1800 --prompt | grep 成功; done
# ② 一次 workflow fan-out 全部 8 个市（并行，主上下文只收摘要）
# ③ 逐个落笔 + 自检
for m in <市1> … <市8>; do node tools/facts-verify.js --map=$m --in=out/verify-$m.json --apply; done
node tools/test-maps.js 2>&1 | tail -3
```

**实测一轮 4 个市**（广元/遂宁/资阳/雅安）：抓取 83 s（串行，含 1.8 s 间隔）、
并行核实一轮、落笔 54 个字段——**全部过程我只看了几十行摘要。**

**并行要注意的两件事：**
1. **抓取不要并行**。站点按 IP 限流（见坑 #31），抓取必须串行 + 间隔；
   并行的是"**核实**"这一步（纯本地读文件，不碰网络）
2. **子代理返回必须自我压缩**。在委派指令里明确要求"只回 3 行"，
   否则它会贴一大段解释，把并行省下来的上下文又还回去

---

#### 十三、铺全国骨架（2026-09-17 完成）：踩到的三个"隐蔽"问题

> 目标：把 33 个省/自治区/直辖市的地级地图全部接进来（从 23 张到 363 张）。
> 脚本层面很快（`batch-add-maps.js` 每省 1~5 秒，33 省零失败），
> 但**三个问题都极其隐蔽**——每一个都表现为"看起来正常，其实坏了"。

**问题 ①：`registry.js` 是首屏文件，会随地图数线性膨胀**

SOP 3.5.5 的承诺是"地图数量再多，首屏也不变重"，但每条目带了一个
`scripts` 数组（三个脚本路径，约 96 字节/条）——它是 `dir + id` 的**纯推导结果**。
23 张时无所谓；494 张就是 208 KB 的首屏负担。

修法：registry 不存可推导字段，`loader.js` 现算（全部代码只有一处用它）。
效果 10909 → 7463 字节（-32%）。并加两条断言钉住这个不变量
（`registry.js` 不得出现 `scripts`；单条平均 < 600 字节）。

**问题 ②：省级地图的 geo 里混着"地图自己"**

天地图对**省级**地图返回的 `_full.json` 里，第一条 feature 就是它自己
（如 `110000 北京市`混在 `110101 东城区`…里）；**地级市的数据不带这一条**。
旧逻辑把它当成一个下级行政区 → 引擎会把 geo 里每个 feature 都渲染成一块拼图
→ 多出一块永远放不对的"省自己" → **拖拽判定整体错乱**。

表现为：北京/天津/上海/重庆/台湾/香港 6 张省级地图的冒烟测试全挂在
"拖不进去，飘字说 XX 还在别处"。而**地级市全部正常**——所以极易误判成"引擎坏了"。

修法：`add-map.js` 拿到 geo 后立刻剔除"自己"（geo / 关卡 / 资料卡三处用同一份下级列表）；
特例：若剔除后一个不剩（澳门下级就只有一条"澳门"）则保留，否则地图会空白。

**问题 ③：slug 撞名导致地图"静默"没建成**

`吉林市` 的拼音 slug 是 `jilin`，而**`jilin` 已经是吉林省的 slug**。
于是一张地图都没建成，但报告只写"跳过 1"——看起来完全正常。
同类：`海南藏族自治州` → `hainan`（撞海南省）、`河北区`（天津）→ `hebei`（撞河北省）。

修法：给撞名的加后缀（`jilinshi` / `hainanzhou` / `hebeiqu`）。
**教训：生成 id 后必须做一次全局唯一性校验**（含"与已有实体撞名"），
不能只在自己的类别内查重。

**顺带确立的一条设计规则（很重要）**：

> **只为"有下级"的行政区建地图。**
> 以 adcode 判断：以 `00` 结尾的是地级（省级 `x0000`、地级 `xx00`）；
> 非 `00` 结尾的是县级 —— 对直辖市的区（北京东城区）和省直管县级
> （海南儋州/五指山、湖北仙桃/潜江/天门）来说，**它们就是最下一级**。
> 单独建地图 → `DISTRICTS`/`LEVELS` 全空 → 引擎抛
> `Cannot read properties of undefined (reading 'adcodes')`。
> 而它们**本来就作为上级地图的碎片存在**（北京地图的 16 块就是东西城…）。
>
> 给省级/地级地图加拼图碎片时，这些"没有下级的行政区"必须**包含在内**
> （海南地图的碎片里有儋州，甘肃地图里有嘉峪关），否则会产生"上级地图缺一块"的新问题。

**铺完的规模**：363 张地图（省级 34 + 地级 329 等），面积 3214/3214 全覆盖，
`registry.js` 77 KB，`js/maps` 20 MB。

**仍未接入的 7 个"地级"**（经逐个核实，全部**没有下级**，按上述规则不单独建地图）：
东莞、中山（不设区的地级市）、儋州（海南直管）、嘉峪关（甘肃），
以及甘肃的 3 个非标准行政区（中农发山丹马场、莲花山风景林自然保护区、太子山天然林保护区）。

**澳门没有下级区划数据**：天地图官方没有 `820000_full.json`，整个澳门就是一个 feature。
硬要建地图只能是"1 块碎片的拼图"，已按退化地图移除（`prune-empty-maps.js` 会识别并清理）。

**测试配套的两个改进**（地图多了以后必须有）：
- `e2e-test.js` 新增 `--only-maps=<ids>` / `--only-suite=smoke|offline`：
  排查单张不该被迫跑 8 分钟全量。冒烟超时从 10 分钟放宽到 30 分钟
  （实测 363 张 ≈ 470 s，贴着旧上限就会误报"超时"）
- `map-smoke.html` 的落点搜索从 9×9 网格改成"形心 + 41×41 加密网格"：
  台湾（狭长）/三沙（群岛）的边界框里 9×9 格点全落在轮廓外，会误报"找不到落点"

#### 六、一次批量接入的完整节奏

```bash
node tools/batch-add-maps.js --parent=510000 --dry-run   # 1. 看计划
node tools/batch-add-maps.js --parent=510000             # 2. 真跑
node tools/e2e-test.js                                   # 3. 全绿（地图变多，冒烟套件耗时也变长）
git add -A && git commit -m 'feat: 接入四川省 21 个市州'  # 4. 提交（地图数据也是代码）
```

#### 七、实战记录（2026-09 · 四川）

| 项 | 值 |
| --- | --- |
| 命令 | `node tools/batch-add-maps.js --parent=510000` |
| 子级 | 21 个市州 |
| 结果 | 新建 19、已接入跳过 2（成都、自贡）、**失败 0** |
| 登记册 | 4 张 → **23 张地图**，根仍是 `china`，无孤儿、无告警 |
| 待人工补 | 19 张地图、共 **157 个下级行政区** × 4 项字段 |
| 测试 | 276 项 → **751 项全绿**（冒烟套件逐张打开 23 张地图、每张真拖一块，耗时 30.7s） |
| 兜底命名 | 0 个（21 个市州都在 `CITY_SLUGS` 表里） |

### 5.12 地图合规与数据来源

> **先看现状，再看目标。** 这一节的核心不是"改成天地图"这个动作，而是**让数据来源可追溯、让声明永不撒谎**。

#### 一、现状（已完成全量切换 · 2026-09）

| 地图 | 数据源 | 审图号 | 可商用 |
| --- | --- | --- | --- |
| **全部 23 张**（中国 / 四川 / 21 市州） | **国家地理信息公共服务平台（天地图）· 服务中心行政区划** | **GS(2024)0650号** | 见下方"审图号适用范围"待确认项 |

页面上的合规声明读的是每张地图**自己带的来源元信息**（`MAP_GEO_META`），所以现在显示：

> 地图数据来源：国家地理信息公共服务平台（天地图）· 服务中心行政区划，审图号：GS(2024)0650号

**数据传输路径**（不需要 Key、不需要登录）：

```
GET /api/portal/region/menu                       → 全国行政树（3254 个节点，含 gb/pGb）
GET /api/portal/region/map?gb=<gb>&level=<level>  → 该节点全部下级的边界 GeoJSON
   响应是打包二进制：HTTP gzip → 每 4 字节大端 int32 → >>2 → 取低字节 → UTF-8 → JSON
   （解码链从官方前端 bundle 里逆出来，见 tools/lib/tianditu-portal.js）
```

`level` 参数的取值是**实测拟合**的（官方菜单里没有这个字段）：中国/省 = 2、地级 = 3。

**下载与换源**：

```bash
node tools/tianditu-download.js                 # 抓官方数据到 data/tianditu-official/
node tools/replace-geo-source.js --source=file --dir=data/tianditu-official
node tools/e2e-test.js                          # 879 项
```

> 默认数据源已改成 `tianditu-portal`（官方通道），所以**新加地图不带 `--source` 也是官方数据**。

#### 二、为什么声明必须"由数据驱动"，不能写死

一度想把那行字直接写进 HTML —— 但那意味着：数据还是 DataV 的，页面上却标着天地图的审图号。
**那不是合规，那是伪造合规声明，比不写更糟**（审图号是"这份地图经过审核"的凭证，
标错等于对使用者做虚假陈述）。

所以做法是：下载脚本把来源元信息**和边界数据写进同一个文件**：

```js
// js/maps/china/sichuan/chengdu.geo.js
(window.MAP_GEO = ...).chengdu = { ...边界数据... };
(window.MAP_GEO_META = ...).chengdu = {
  provider: "datav", providerLabel: "阿里云 DataV.GeoAtlas",
  approval: null, note: "开发期数据源，未取得审图号，不可用于公开商用",
  adcode: 510100, sourceUrl: "https://geo.datav.aliyun.com/...", fetchedAt: "2026-09-16"
};
```

页面（`js/game.js` 的 `renderSourceNotice`）按它渲染：

| `approval` | 页面显示 |
| --- | --- |
| 有（天地图） | `地图数据来源：国家地理信息公共服务平台（天地图），审图号：GS(2024)0650号` |
| 无（DataV） | `地图数据来源：阿里云 DataV.GeoAtlas（开发期数据源，未取得审图号，不可用于公开商用）` |
| 元信息缺失 | `地图数据来源：未标注（本地数据缺少来源元信息）` |

于是"换了数据源却忘了改声明"这类事故**从结构上不可能发生**：声明永远等于数据自己的来历。

#### 三、两条接入路线（Key 只解决其中一条）

买了 Key 之前先弄清：**审图号 GS(2024)0650号 是"天地图发布的那套行政区划数据"的审图号**，
而"调 API 拿到的边界"是否同样受它覆盖，**需要向天地图确认**（见第五节风险）。

| | A. 行政区划 API | B. 官方行政区划数据包 |
| --- | --- | --- |
| 需要 | 开发者 Key（tk），逐区县请求 | 下载一次数据包 |
| 返回 | **WKT 字符串**（简化边界），要转换 | GeoJSON / SHP |
| 审图号 | 需确认是否覆盖 | 数据包自带 GS(2024)0650号 |
| 适合 | 增量接入新地图 | **批量换源、合规要求明确** |
| 项目里对应 | `--source=tianditu` | `--source=file --dir=<目录>` |

> 本项目的建议：**批量换源走 B（口径清楚），日常增量接入走 A。** 两条都已实现，切换只差一个参数。

#### 四、怎么申请 Key（天地图开发者）

1. 注册开发者账号：<https://uums.tianditu.gov.cn/register> （需手机号 + 实名认证）
2. 登录控制台：<https://console.tianditu.gov.cn/> → **创建应用**
3. 在应用里勾选需要的服务（本项目用「行政区划」），拿到 **Key（tk）**
4. 把 Key 填进下面任一位置（**按优先级**）：

```bash
# ① 命令行（一次性）
node tools/replace-geo-source.js --source=tianditu --tk=你的Key

# ② 环境变量
export TIANDITU_TK=你的Key

# ③ 配置文件（推荐；已在 .gitignore 里，不会外泄）
cp tools/tianditu.config.example.json tools/tianditu.config.json
# 然后把 tk 填进去；delayMs 控制批量间隔（默认 600ms）
```

> 免费账号有**每日配额**。所以批量脚本默认每张之间延时 600~800ms，
> 且**遇到"Key 非法 / 配额用尽"这类错误立刻停下**（重试只是白烧配额）——
> 这对应项目红线里的"加延时、不死循环重试"。

#### 五、换源操作与验收清单

```bash
# 1. 先看会动哪些地图（不联网、不写盘）
node tools/replace-geo-source.js --source=tianditu --tk=<Key> --dry-run

# 2. 拿 2~3 张试水，确认边界形态没问题再放量
node tools/replace-geo-source.js --source=tianditu --tk=<Key> --only=chengdu,zigong

# 3. 全量换源（只覆盖 *.geo.js，人工写的 *.data.js / *.js 一个字都不动）
node tools/replace-geo-source.js --source=tianditu --tk=<Key>

# 4. 跑测试（764 项）
node tools/e2e-test.js
```

换源后**必须逐条确认**：

- [ ] 页面上那行声明变成「天地图 + 审图号：GS(2024)0650号」（`git grep MAP_GEO_META` 可自查）
- [ ] 每张地图能正常游玩（`tools/map-smoke.html` 会逐张真拖一块）
- [ ] **边界形态没有退化**：天地图 API 给的是*展示用简化边界*，
      零件会不会出现明显缺口、飞地会不会丢、原 DataV 数据里的细节（成都 6279 个顶点）会不会变粗 ——
      这一条**必须靠眼睛**，测试断言看不出来
- [ ] `geo-source-report.json` 里 `failed` 为 0（有失败就再跑一次同样的命令，已成功的会照常重刷）

#### 六、风险与待确认（不想当然）

1. **审图号适用范围（仍未关闭）**：GS(2024)0650号 覆盖的是天地图发布的行政区划数据产品；
   我们抓的是**同一个官方服务**的数据（服务中心 · 行政区划），但"抓取副本用于商业产品"
   是否等同于"使用该发布数据"，**请向天地图/测绘主管部门书面确认**。已写进 manifest 的
   `approvalNote`，不做无根据的断言。
2. **边界精度（已实测并接受）**：官方数据是简化边界 —— 成都 20 个区县的顶点数
   从 DataV 的 6279 降到 **1806（29%）**，形状明显更"方"。
   但**拓扑是干净的**：相邻区县共用顶点，实测每个区县到最近邻顶点的距离 = 0 米，
   **不会有细缝**；覆盖面积比值 0.9993。已与需求方确认接受该精度（合规优先）。
3. **九段线 / 境界线（已解决 · 2026-09）**：中国层的官方响应里有 **8 条 `境界线`
   （MultiLineString）**：九段线（530 顶点，纬度到 3.40°N）、琼州海峡、上海/浙江海域、
   深圳/香港海域、新疆未定国界段等。

   **做法（不动引擎）**：下载时把这几条线按 ±0.004°（≈440 米）做成极窄的"缓冲带"多边形，
   作为**一条非行政区要素**（`adcode: "100000_JD"`，名"南海诸岛及海上界线"）并入 geo。
   于是它：
   - 由底图正常绘制 ✓（引擎对非面要素只画底图、不做拼图块）
   - 不进关卡、不进资料卡、不参与拼图 ✓（非数字 adcode 的约定，见坑 #24）
   - **把地图范围拉到南海** ✓ —— 官方数据里海南省自身就含南海诸岛（纬度到 3.83°N），
     所以含海南的那一关视野覆盖南海，九段线可见（见 `docs/ui-china-nanhai.png`）

   **可见性说明**：引擎每一关会把视图推近到本关行政区范围，所以九段线只在
   "视野覆盖南海"的时候可见（第四关含海南 / 整图总览）。要做到"任意关卡都显示"，
   需要改引擎的 viewBox 计算逻辑 —— 那属于红线，要先单独讨论。
4. **服务条款与配额**：官方服务中心接口实测无频控问题（本次 24 次请求、间隔 800ms 全部成功），
   但服务条款要求署名，商用仍可能有额外约定。API Key（`tools/tianditu.config.json`）
   对**边界数据用不上**（那是给地图瓦片/地名搜索等服务的），保留备用。
5. **`--source=file` 的合规前提**：走本地数据包时，**必须确认那份数据本身是官方带审图号的**，
   否则只是把不合规从 API 挪到了本地文件 —— 脚本无法替你判断这一点，所以它把
   `approval` 标成"（以数据包自带为准，需人工确认）"。

#### 七、规矩：新地图必须同源

**后续新增地图必须使用与现有地图相同的数据源**，否则同一张树上会混着两种口径的边界
（拼接处对不上、审图号也说不清）。落地方式：

1. 新增地图一律用 `add-map.js` / `batch-add-maps.js`，并按需带 `--source=`
2. 团队默认源写在 `tools/lib/geo-source.js` 的 `DEFAULT_SOURCE`（目前是 `datav`；
   **完成全量换源后应改成 `tianditu` 并提交**，这样忘记带参数时也不会混源）
3. 换源后**跑一次全量测试**（`tools/test-wkt.js` + 三套浏览器测试），确认没有混源痕迹

---

## 六、文件目录结构

```
deepseekharness/
├── index.html                      431 行 · 页面结构 + 手绘图案库（12 个 <symbol>）
├── batch-report.json              · 最近一次批量接入的报告（成功/失败/待人工补清单）
├── geo-source-report.json         · 最近一次换源的报告（每张地图的来源与结果）
├── README.md                      · 项目说明文档
├── 地图拼图项目_开发SOP与交接文档_v1.0.0.md   · 本文
│
├── css/
│   └── style.css                  1695 行 · 设计令牌 / 三套主题 / 地图导航 / 全部动画
│
├── js/
│   ├── engine.js                 1554 行 · 通用引擎 MapPuzzleEngine（不含任何地图数据）
│   ├── geomap.js                  181 行 · 墨卡托投影 + path/bbox/质心（两种 GeoJSON 写法都吃）
│   ├── game.js                    208 行 · 启动器 + 地图导航 UI（面包屑 / 选择器，宿主层）
│   └── maps/                       · 地图工厂（层级目录，规则见 3.5）
│       ├── registry.js            113 行 · 总登记册【自动生成，勿手改】
│       ├── loader.js              141 行 · 运行时按需注入脚本 + trail() 面包屑接口
│       ├── china.js / .geo.js / .data.js      中国（根地图，34 个省级行政区）
│       └── china/
│           ├── sichuan.js / .geo.js / .data.js  四川省（21 个市州）
│           └── sichuan/
│               ├── chengdu.{js,geo.js,data.js}  成都 · 20 个区县【资料是人工核实的】
│               ├── zigong.{js,geo.js,data.js}   自贡 · 6 个区县
│               └── … 另外 19 个市州（panzhihua / luzhou / deyang / mianyang /
│                  guangyuan / suining / neijiang / leshan / nanchong / meishan /
│                  yibin / guangan / dazhou / yaan / bazhong / ziyang / aba /
│                  ganzi / liangshan），全部由 batch-add-maps 生成
│                  · .geo.js 是构建产物 / .data.js 是占位资料（待人工补）
│                  · 目录规则见 3.5.2：包文件 = 父地图的子目录 + <id>.js
│
├── tools/
│   ├── add-map.js                 620 行 · 单张接入（CLI + 可被调用的 addMap() 库函数）
│   ├── batch-add-maps.js          320 行 · 批量接入一个省/市 + 写 batch-report.json
│   ├── replace-geo-source.js      · 批量换源：把已有地图的 *.geo.js 整体换成另一个数据源
│   ├── test-wkt.js                · WKT → GeoJSON 的离线单测（13 项）
│   ├── test-maps.js               · 地图包自洽离线自检（118 项：adcode/关卡/资料/来源/key/九段线）
│   ├── tianditu-download.js       · 抓官方行政区划数据（无需 Key/登录）
│   ├── tianditu-check.js          · 天地图 API（需 Key 那套）的接入自检
│   ├── replace-geo-source.js      · 批量换源
│   ├── build-registry.js           71 行 · 扫描 js/maps/ 生成 registry（children 反推）
│   ├── build-data.js               64 行 · 只重刷成都边界的薄封装（新地图请用 add-map）
│   ├── lib/
│   │   ├── inline-geo.js          229 行 · 公共库：GeoJSON 规范化 / 内联模块 / DataV 下载
│   │   ├── map-tree.js            371 行 · 公共库：目录规则 / 包元信息扫描 / registry 生成
│   │   ├── slugs.js                90 行 · 公共库：adcode ↔ 拼音 slug（省表 / 地级表 / 兜底）
│   │   ├── wkt.js                  · 公共库：WKT / 坐标串 → GeoJSON（天地图那层）
│   │   ├── tianditu-geo.js         · 公共库：天地图行政区划接口客户端（Key/延时/≤2 次重试）
│   │   └── geo-source.js           · 公共库：数据源抽象（datav | tianditu | file）
│   ├── e2e-test.js                238 行 · 测试驱动（跑三套 + CSS 静态检查 + 汇总）
│   ├── selftest.html              560 行 · 城市回归套件（89 项 · 成都真实数据 + UI/动画）
│   ├── engine-test.html           503 行 · 引擎功能套件（77 项 · 只用虚构数据）
│   ├── map-smoke.html            584 行 · 多地图冒烟套件（拖拽 + 导航 + 点选切换 + 窄屏）
│   ├── shot.js / shot.html        · 截图工具（把应用跑到想要的样子再让 Chrome 拍）
│   ├── engine-host.html           引擎测试宿主页（与 index.html 同构的瘦页面）
│   ├── fixtures/
│   │   └── tiny-city.js           虚构测试城市（3 个假区县 + 2 关 + 自配色）
│   ├── shot.js                    · 截图工具（生成 docs/ 里的界面配图）
│   ├── shot.html                  · 截图载体（负责"把 load 事件拖住"）
│   ├── icon-preview.html           手绘图案预览（需 http 打开）
│   └── districts-source.json       核实过的区县资料（数据存档）
│
└── docs/
    ├── level1.jpg                  第一关截图
    ├── level2.jpg                  第二关截图
    ├── intro.jpg                   开场动画截图
    ├── settle.jpg                  结算画面截图
    ├── patterns.jpg                手绘图案总览
    ├── ui-chengdu.png              地图导航 UI（成都，面包屑 + 选择器）
    ├── ui-zigong.png               地图导航 UI（自贡，占位资料）
    ├── ui-china.png                地图导航 UI（中国，根地图）
    └── ui-mobile.png               窄屏下的导航 UI（390×844）
```

**加载顺序**（`index.html` 末尾，不可调换）：

```html
<script src="js/maps/registry.js"></script>  <!-- 轻量元信息：谁是谁的父级、脚本在哪 -->
<script src="js/maps/loader.js"></script>    <!-- 按 registry 在选中地图时注入脚本 -->
<script src="js/geomap.js"></script>         <!-- 投影与几何 -->
<script src="js/engine.js"></script>         <!-- 通用引擎（不自动启动） -->
<script src="js/game.js"></script>           <!-- 读 ?map= → loader.load() → start() -->
```

> 注意：具体的**地图包不再写死在 `index.html` 里**。成都的三个文件是运行时由
> `loader.js` 注入的，所以地图从 1 张长到 100 张，首屏依然是上面这 5 个脚本（≈75KB）。

**运行方式**：

| 文件 | 打开方式 |
| --- | --- |
| `index.html` | **双击即可**（零依赖、无外链） |
| `tools/icon-preview.html` | 需 http（`python3 -m http.server`），`file://` 下会被同源策略拦 |
| `tools/selftest.html`、`tools/engine-test.html` | 由 `node tools/e2e-test.js` 自动驱动 |
| `tools/engine-host.html` | 被 `engine-test.html` 装进 iframe；也可以直接打开，看假数据的渲染效果 |

---

## 七、已知限制

1. **面积口径差异**：双流区 1067 km² 是含托管区口径（实际直管约 466）；简阳 2213.5 km² 含成都东部新区。已在 README 注明。
2. **脸谱不能小尺寸使用**：线宽限制，低于 48px 会糊（见坑 #21）。
3. **预览页依赖 http**：`file://` 下无法读取图案库，页面里已给出明确提示。
4. **测试依赖系统 Chrome**：硬编码了 macOS 的 Chrome 路径，换平台需调整 `tools/e2e-test.js` 里的 `CHROME` 常量。
5. **`--no-sandbox`**：当前机器的 Chrome sandbox 不可用，测试脚本必须带此参数。
6. **一个地图只有一份进度**：进度按地图的 `storage` key 存，切换地图不会互相覆盖，但也没有"跨地图总进度"这种东西。
7. **导航栏只在多地图时出现**：只有一张地图且没有上下级时，`renderNav()` 会把整条导航收起来（`hidden`），免得只有一个选项的下拉占地方。
8. **世界/大洲层没有数据源**：DataV 只覆盖中国区划（`world.json` 实测 404），世界层需要换 Natural Earth / world-atlas，是独立任务（见 [3.5.7](#357-层级与现实行政区划以及世界层待接入)）。
9. **自动生成的资料是占位**：`add-map.js` / `batch-add-maps.js` 生成的下级行政区资料是
   `area: null` + `【待补充：<地名><字段>】`，面积/地标/冷知识必须人工补；关卡也只是"每 8 个一组"的机械切分。
   目前四川 19 个新市州共 157 个区县都是占位状态（成都、自贡之外的卡片资料都还没写）。
10. **fixture 没覆盖 `palette.fallbackHue`**：只有"关卡既不自带 `hue`、`hueByLevel` 里也查不到"时才会走这条分支，要覆盖它得再加一关（`tools/fixtures/tiny-city.js` 顶部已注明）。
11. **引擎套件依赖宿主页**：引擎按 id 缓存 DOM，所以必须有 `tools/engine-host.html`。宿主页的 DOM 结构或 id 一旦改动，这个文件要跟着改。
12. **孤儿地图**：子地图声明的 `parent` 若还没接入，它会挂在 `registry.orphans` 里（能玩、能选，但没有"返回上一级"）。这是过渡态信号，不是错误。

---

## 八、几条最值钱的经验

### 1. 注释写错比代码写错更危险

坑 #5 里，一句写反的注释让我在"验证"时直接跳过了方位检查。**错误的注释会让人停止怀疑。**

### 2. "容器宽度正常"不等于"没溢出"

坑 #12 里，`.app` 的宽度一直是正确的 390px，发现问题的是子元素。**只看外层永远找不到 `min-width: auto` 类的坑。**

### 3. 打印数据 > 改代码试

坑 #9、#10、#12 的共同点：前几次"改 CSS 试试"全部无效，一次**打印实测数据**就命中根因。建立"先量后改"的反射。

### 4. 静默错误最需要外部验证

坑 #1（adcode 写反）和坑 #13（全解锁）都不会报错，只是**结果全错**。这类问题只能靠**独立数据源交叉验证**或**断言**发现。

### 5. 测试体系要在第一个阶段就搭

这个项目的测试是阶段 1 建的，之后每一轮改动都靠它兜底。它逮住了 4 个我自己没意识到的 bug —— 包括那两个静默错误。**测试不是收尾工作，是开发基础设施。**

### 6. 遇到跑不通的工具，及时换路

坑 #2 里，CDP 试了 5 组参数后发现是环境问题（sandbox 崩溃），立刻转向"页面自己上报"的方案。**新方案反而更简单**。按"最多试 2 次"的红线执行，省下了大量 token。

### 7. 视觉细节必须放大看

坑 #19、#20 都是"实际尺寸下看不出问题，放大后一览无余"。**为视觉工作建一个放大预览页**，是个投入产出比极高的做法。

### 8. 有些"完成"其实从未生效

坑 #13 里，用户明确要的"逐关解锁"从一开始就没工作过，直到我很偶然地去查徽标 DOM 才发现。**对每个功能做一次"真的生效了吗"的验证，比相信自己的实现更重要。**

### 9. 拿假数据当探针，能逼出真实数据掩盖的问题

坑 #22 里，几何层"只认 MultiPolygon"这个隐含契约，在真实成都数据上跑了整整一个项目周期都没暴露 —— 因为真实数据恰好就是 MultiPolygon。**换 3 个假区县喂进去，第一次运行就炸了。**

这和"测试要用构造数据"是同一个道理：真实数据是"一种恰好走通的情况"，不是"全部情况"。做通用化重构时，**先写一份最小假数据，往往比先通读一遍代码更快命中问题**；而且假数据是能长期留下的资产 —— 以后任何人改引擎，它都会替你再验一遍。

---

*文档完 · 2026-09（最后更新：地图工厂 + 层级架构 + 三套测试体系）*

---

# v1.0.0 成品交付说明

> 交付日期：2026-09　|　版本：**v1.0.0**　|　形态：零依赖纯前端单页（双击即玩）

## 一、这一版有什么

**一个可玩的多层级地图拼图**：把行政区的碎片拖回地图上的正确位置，拼对后弹出该地区的资料卡。
地图数据全部来自**国家地理信息公共服务平台（天地图）**，页面上常驻显示审图号。

## 二、地图层级树（当前支持）

```
中华人民共和国（34 个省级行政区 · 5 关）
└── 四川省（21 个市州 · 3 关）
    ├── 成都（20 个区县 · 3 关）        ← 资料卡为人工核实内容
    ├── 自贡市（6 个区县 · 1 关）
    └── 另外 19 个市州（攀枝花 / 泸州 / 德阳 / 绵阳 / 广元 / 遂宁 / 内江 /
        乐山 / 南充 / 眉山 / 宜宾 / 广安 / 达州 / 雅安 / 巴中 / 资阳 /
        阿坝 / 甘孜 / 凉山）            ← 资料卡为"共建"占位，白天人工补
```

**共 23 张地图**，注册表自动生成（`js/maps/registry.js`）。
页面上可经"切换地图"下拉或面包屑（`中国 › 四川省 › 成都市`）自由切换，**每张地图进度独立存档**。

## 三、资料卡策略（不伪造，只降级）

| 地图 | 资料卡 |
| --- | --- |
| 成都（20 个区县） | **人工核实**：面积 / 地标 / 一句话 / 冷知识 |
| 其余 22 张 | 统一显示「📖 资料收录中，欢迎参与共建」 |

**为什么这么做**：资料必须真实才有价值。脚本生成的是**占位**，我们**不编造**任何"冷知识"，
而是把占位措辞统一成社区共建口径（`tools/soften-placeholders.js` 负责批量替换文案，
它只改措辞、不填内容）。面积缺失时页面显示"—"（未知值的通用表达）。

## 四、测试证明

```bash
node tools/e2e-test.js
```

**884 项全部通过**（`0 失败`）：

| 套件 / 检查 | 项数 | 覆盖 |
| --- | --- | --- |
| 城市回归 · 成都 | 89 | 真实数据 + UI/动画 + 方位 + 存档 + 主题 + 键盘 |
| 引擎功能 · 虚构 tiny-city | 77 | 通用性探针（断言里不含任何真实地名） |
| 多地图冒烟 · 逐张地图 | 585 | **23 张地图逐张真拖一块** + 导航 + 点选切换 + 窄屏 |
| 离线检查 · WKT → GeoJSON | 13 | 天地图数据格式转换 |
| 离线检查 · 地图包自洽 | 120 | adcode / 关卡 / 资料 / 来源 / 存储 key / 九段线 |
| CSS 静态检查 | 1 | 手机端性能降级规则 |

## 五、合规声明

- **数据来源**：国家地理信息公共服务平台（天地图）· 服务中心行政区划服务
- **审图号**：**GS(2024)0650号**（页面常驻显示，且**声明由数据自身来源驱动** —— 见 5.12）
- **九段线/境界线**：已把官方 8 条「境界线」转成细长多边形并入 geo，底图正常绘制
  （见 `docs/final-china.png`，海南以南可见九段线）
- **⚠️ 商用待法务确认**：GS(2024)0650号 覆盖天地图对外发布的行政区划数据；
  我们使用的是**同一官方服务**的抓取副本，"抓取副本用于商业产品"是否等同于"使用该发布数据"，
  **须由法务/主管部门书面确认**，本版本不做无根据的断言（详见 5.12 第六节）。
- 抓取副本与来源清单：`data/tianditu-official/`（含 `manifest.json`：URL、时间、审图号）

## 六、怎么启动

```bash
# 方式一（推荐）：双击 index.html —— 零依赖、无需服务器
open index.html

# 方式二：用 DSH 的本地 Web 界面
dsh web

# 方式三：任意静态服务器
python3 -m http.server 8000     # 然后访问 http://localhost:8000
```

切换地图：页面右上「切换地图」下拉，或用 URL `index.html?map=zigong`。

## 七、验收要点（建议按顺序看）

1. 打开首页 → 顶部显示**审图号**，标题为「中国地图拼图」
2. 用「切换地图」跳到 **四川省**、**成都**、**自贡市**，确认面包屑与进度各自独立
3. 拖动任意碎片到正确位置 → 播放吸附动画 + 信息卡弹出
4. 刷新页面 → 进度仍在（每张地图独立存档）
5. 窄屏（<620px）→ 导航不溢出、选择器可用
6. 跑 `node tools/e2e-test.js` → **884 通过 / 0 失败**

## 八、这一版不包含什么（留到后续）

| 项 | 说明 |
| --- | --- |
| 21 个市州的真实资料卡 | 需人工核实（面积/地标/冷知识），白天补 |
| 中国其余 33 个省级的下级地图 | 一条命令一个省（见 5.11），但测试耗时与仓库体积会线性增长，需先解决 5.12 提到的规模问题 |
| 任意关卡都显示九段线 | 需改引擎的 viewBox 计算 —— 属红线，需先讨论 |
| 世界/大洲层 | 数据源与层级规范已在 3.5 定义，尚未接入 |

---

# 附录 A · 本轮开发复盘：从 DataV 全量切到天地图（2026-09）

> 这一节回答三个问题：**这一轮是怎么做成的**（可复用的流程）、**踩了哪些坑**（以后怎么躲）、
> **还剩什么没做**（明天从哪继续）。

## A.1 推进流程：七步法

每一步都有**明确的进入条件与通过判据**，不通过就不进下一步。

| 阶段 | 动作 | 通过判据 | 产出 |
| --- | --- | --- | --- |
| ① 确认边界 | 复述需求、确认红线（不改引擎／不改测试／不伪造数据） | 需求方逐条确认 | 一份红线清单 |
| ② 勘察现状 | 读代码、跑现有测试拿到基线 | `node tools/e2e-test.js` 全绿（基线 = 764） | 基线数字 |
| ③ 摸外部契约 | **先写探针**验证接口真实行为，再写代码 | 探针拿到真实请求/响应样例 | `docs: 实测记录…` 提交 |
| ④ 建接缝 | 把"会变的东西"抽象成一个 provider | 旧数据源仍可用（默认不变），测试仍全绿 | `tools/lib/geo-source.js` |
| ⑤ 能离线测的先测透 | 把纯计算部分（WKT/二进制解码）单独抽出来测 | 离线单测全绿（13 项） | `tools/test-wkt.js` |
| ⑥ 小样本试水 | 只换 2 张，跑全量测试 + **肉眼比对** | 全绿 + 需求方确认可接受 | 试水提交（764 全绿） |
| ⑦ 全量 + 归档 | 全量执行 → 报告 → 回归 → 打 tag | 全绿 + 报告 + tag | 879→884 全绿 + `v1.0.0` |

**为什么这个顺序重要**：⑥ 放在 ⑦ 前面，是为了让"方向错了"这件事**只花 2 张地图的代价**就暴露出来
（本次就在这里发现了边界精度问题，需求方当场决定接受，避免全量返工）。

## A.2 实际执行的关键命令（可复制）

```bash
# ② 基线
node tools/e2e-test.js                       # 764 → 记下这个数

# ③ 摸契约：先探针（每一次探测都间隔 ≥1s，不并发）
#    结论写进 tools/lib/tianditu-portal.js 的头部注释

# ④ 建接缝：加 provider，默认值先不动，确认不影响现状
node tools/batch-add-maps.js --parent=510000 --dry-run

# ⑤ 离线测透
node tools/test-wkt.js

# ⑥ 试水两张
node tools/tianditu-download.js --only=510100,510300
node tools/replace-geo-source.js --source=file --dir=data/tianditu-official --only=chengdu,zigong
node tools/e2e-test.js                       # 必须仍全绿
node tools/shot.js --map=chengdu --drag=1 --out=/tmp/check.png   # 肉眼比对

# ⑦ 全量 + 归档
node tools/tianditu-download.js
node tools/replace-geo-source.js --source=file --dir=data/tianditu-official
node tools/e2e-test.js                       # 884 全绿
git tag -a v1.0.0 -m '…'
```

## A.3 方法论提炼（这是最值钱的部分）

1. **外部契约必须先探测再写码**。本次官方文档的路径已废弃、参数只认中文名、
   返回体是打包二进制 —— 这三件事**没有一件能靠读文档得知**，全靠探针试出来。
   探针的产出要**写进代码注释**，否则下一个人会重走一遍。
2. **把"能离线验证的"与"必须联网的"分开写**。拿到 Key 之前，WKT/二进制解码这类纯计算
   可以 100% 测透（13 项单测）；联网那部分只留"请求成不成功"一件事要查。
3. **小样本试水再放量**。2 张地图的成本极低，却能暴露精度、拓扑、adcode 变化这类
   只有真数据才显形的问题。
4. **让声明由数据驱动**。合规声明读数据自身携带的 `MAP_GEO_META`，
   于是"数据换了、声明没改"这种事故从结构上不可能发生 —— 比写死一行字可靠得多。
5. **数据层面的不变量要前移成断言**。踩到"换源后 adcode 对不上导致引擎崩"之后，
   立刻把它变成 118 项离线自检的一部分，而不是"下次注意"。
6. **换数据源 = 可能换主键**。任何"拿外部数据的 id 做关联键"的设计，
   切换数据源时必须重校验关联完整性（本次是 `513425 会理县 → 513402 会理市`）。
7. **每阶段一个 commit**。回滚粒度 = 阶段粒度；配合"做完就汇报"，
   出错时能精确回到上一个已知良好状态。
8. **用"它能不能被测试发现"来评估风险**。发现不了的（边界形态、视觉细节）→ 必须肉眼；
   能发现的 → 立刻写成断言，别留在脑子里。

## A.4 今晚遇到的困难（按发生顺序）

| # | 困难 | 根因 | 修法 | 以后怎么避免 |
| --- | --- | --- | --- | --- |
| 1 | 天地图文档路径 404 | 旧文档 `/v2/administrative/district` 已废弃，网关把多余路径段拼到内部路径后 404 | 用探针找出真实端点 `/v2/administrative` | 官方文档可能滞后，**接口行为以实测为准** |
| 2 | 假 Key 报"WAF 拦截" | 只看 HTTP 状态码：实用 403 + JSON（鉴权错误），被误判成 WAF | **先读 body 再判状态码** | 任何"状态码 → 结论"的映射都要用真实响应验证一次 |
| 3 | 换源后引擎崩（`reading 'd'`） | 官方数据把会理县(`513425`) 改成了会理市(`513402`)，关卡里的旧 adcode 找不到 | 重生成该地图占位资料 + **新增 118 项离线自检** | 换源后**必须校验关联键完整性**；把不变量变成断言 |
| 4 | 九段线被"下级过滤"误伤 | `file` 数据源读单张地图文件时按"直接下级"过滤，非行政区要素被滤掉 | 区分 `--dir`（整份文件=一个包）与 `--file`（全国大文件才需挑选） | 同一个参数承担两种语义时，**必须显式分开** |
| 5 | 补丁"改了没生效"（两次） | python 批量替换**静默失败**（锚点不匹配也不报错） | 第二次加 `assert` 才暴露 | **批量改代码的每一步都要断言**，"改完"≠"改对" |
| 6 | 截图拍到开场动画 | Chrome 在 `load` 事件后立刻拍，而地图是按需注入脚本加载的 | "钉住 load 事件"：静态 `<img src="/__hold__">`，载体干完活再 `/__release__` | 截图/自动化与异步加载的时序问题，用**确定性握手**而不是 sleep |
| 7 | 视图动画覆盖 viewBox | 引擎逐帧插值改 viewBox，设置一次会被下一帧盖回 | 放行 load 后**持续压住** viewBox 直到进程被收走 | 与"自己会动的系统"打交道时，**要么等它停、要么持续压** |
| 8 | 手机窄屏导航消失 + 横向溢出 | 窄屏下 `.topbar` 变 column，`flex-basis:100%` 的含义从"宽度"变成"高度" | 断点内改 `flex-basis: auto` + **补 390×844 窄屏专项** | `flex-basis` 是**主轴**尺寸；写之前确认所有断点的 `flex-direction` |
| 9 | 冒烟套件每张地图都"拖不进去" | 测试只等了 280ms，而落位在 300ms 的吸附动画**之后**才提交 | 改成轮询"幽灵层消失 + tries 累加" | **动画时长是内部细节**，永远等结果、不等时间 |
| 10 | 被测 iframe 藏到屏幕外导致落点错位 | `getScreenCTM` 与 `getBoundingClientRect` 参考系不同 | iframe 留在正常文档流 | 混用多个坐标 API 前先确认参考系一致 |
| 11 | 块注释里 `**/` 提前闭合注释 | `js/maps/**/*.js` 中的 `*/` 终止了块注释 | 改写措辞 | 注释里避免 `**/`；改注释也要跑 `node --check` |
| 12 | 全国数据里混着"九段线"非行政区要素 | 官方数据 35 个 feature 含 1 条 `100000_JD` | 非行政区要素画底图、不进关卡（并处理成细面保证可见） | 真实数据总有"看着像但不是"的脏数据，**别信字段的合法形态** |

## A.5 交付后自审（本轮复查）发现并已修

| 问题 | 影响 | 处理 |
| --- | --- | --- |
| README 仍写"数据源是 DataV" | 误导使用者 | 已改为天地图官方 + 审图号，并注明"开发期曾用 DataV" |
| SOP 3.5.7 层级表里每级都标"DataV ✔" | 同上 | 全部改为"天地图官方 ✔"；世界/大洲改注"官方通道没有，待接入" |
| `--source=tianditu`（API Key 路线）仍是可选项 | 选了会得到一个看不懂的报错 | provider 改为**立即失败并指路**（告诉你去用 `tianditu-portal`/`file`），三个相关文件顶部标"已休眠" |
| `tools/replace-geo-source.js` 不支持 `--parent` | 指令里用过这个参数，实际只支持 `--only` | **已补上** `--parent=<adcode>`（按 adcode 前缀筛选），例：`--parent=510000` → 四川的 21 个市州 |
| 换源汇总打印的是 provider 的静态审图号 | 显示"（以数据包自带为准…）"而非真实审图号 | 改为打印**实际用到的来源**（从 manifest 读取） |

**仍未解决（如实列出）**：
1. `area: null` 在页面显示"—"（引擎 `formatArea()` 对非数字返回"—"）。这是"未知值"的通用表达，
   但如果你希望它也显示共建文案，需要改引擎或在数据层塞字符串 —— **前者是红线，后者会破坏字段语义**，需你定。
2. 九段线只在"视野覆盖南海"时可见（引擎每关推近视图）。要"任意关卡都可见"必须改引擎 viewBox 逻辑。
3. `tools/test-maps.js` 用 `grep` 检查占位文案 → **非 macOS/Linux 环境会失败**（Windows 需改用纯 Node 实现）。

## A.6 遗留工作清单（明天从这里继续）

### P0 · 资料卡（**面积已全覆盖，剩下的全是文字**）
- **现状（2026-09 更新）**：**面积 238/238 已填**（中国 34 省级 + 四川 21 市州 + 183 区县）；
  **文案**已补 **31 条**（成都 20 + 乐山 11），**待补 207 条 / 621 个字段**。
  用 `node tools/status.js` 看准确数字（面积与文案分开统计）。
- **面积已完成，不用再管**：`node tools/area-from-geo.js --parent=510000 --write`
  一条命令补完一个省，它只改 `area`（见 5.11 第五节的精度实测）。
- **做法**（只剩人工核实文字这一步）：
  ```bash
  node tools/status.js --verbose                    # 1. 挑一个市，看还有几条占位
  # 2. 核实文字：填 landmark/tagline/funFact（人）
  #    ⚠️ 不要改 key（adcode），它必须和 <市>.geo.js 里的 feature 严格对应
  node tools/test-maps.js 2>&1 | tail -4            # 3. 自检
  ```
- **验收**：`node tools/status.js` 的「待补文案」条数下降；全量测试仍全绿。
- **注意**：**不要用 AI 编造冷知识**。核实不了就保持占位文案，宁缺毋假。

### P1 · 结构：把剩下的省接进来（一条命令一个省）
- **官方行政树统计（准确数）**：3254 个节点 = 中国 1 + **省级 34** + **地级 492** + **县级 2727**。
- **已完成**：省级 1（四川）、地级 21。
- **待接入**：**315 个地级** + **2701 个县级**（县级作为叶子地理，随所属市一起生成）。
- **做法**：
  ```bash
  # 1. 往 tools/lib/slugs.js 的 CITY_SLUGS 补这个省的地级市拼音（十几行）
  # 2. 抓数据 + 换源（默认源已是官方通道，不加 --source 就是天地图）
  node tools/tianditu-download.js            # 会按登记册抓（新省要先接入）
  # 或直接一条命令接入一个省：
  node tools/batch-add-maps.js --parent=440000   # 广东（父级没接入会自动补）
  node tools/e2e-test.js
  ```
- **三个必须先解决的规模问题**（详见 5.12 与下方风险）：
  1. 注册表首屏体积：422 B/张 → 356 张 ≈ 148 KB（现在 10.7 KB）
  2. 冒烟测试耗时：1.33 s/张 → 356 张 ≈ 8 分钟/次（需抽样策略）
  3. 仓库体积：≈95 KB/张 → 312 张 ≈ +30 MB

### P2 · 体验与合规收尾
- 九段线"任意关卡可见"（需讨论是否动引擎）
- 世界/大洲层（数据源改用 Natural Earth，层级规范已在 3.5 定义）
- `area` 空值的展示口径（见 A.5 未解决 1）

## A.7 明天第一件事（复制粘贴即可）

```bash
cd <项目目录>
git pull --tags                     # 确认在 v1.0.0
node tools/e2e-test.js              # 基线：884 通过 / 0 失败
node tools/soften-placeholders.js --check   # 看还有哪些占位

# 想先补资料就从 P0 开始；想先铺地图就从 P1 开始：
#   P0: 打开 js/maps/china/sichuan/leshan.data.js 填 11 个区县
#   P1: 给 tools/lib/slugs.js 补一个省，然后 node tools/batch-add-maps.js --parent=<省 adcode>
```

> **一句话交接**：结构是脚本的事（一条命令一个省），资料是人的事（一条一条核实）。
> 脚本能保证"不崩、不混源、不漏块"，保证不了"内容是对的" —— 后者只能靠人。

---

## 附录 B · 方法论沉淀（铺完全国骨架后的复盘）

> 这一节不是讲"我们做了什么"，而是**"下次怎么做得更好"**。
> 每条都对应一个真实踩过的坑，可以当 checklist 用。

### B.1 优先怀疑"静默失败"，而不是"报错"

本轮三个最贵的问题**没有一个报错**：

| 现象 | 表面 | 实际 |
| --- | --- | --- |
| slug 撞名 | 报告写"跳过 1" | 一张地图根本没建 |
| registry 膨胀 | 一切正常 | 首屏多背 130 KB |
| 省级 geo 混入"自己" | 页面能开、能拖 | 拖拽判定全错，只有 6 张地图暴露 |

**可操作的做法**：
- 每个批量脚本都要检查**产出是否等于预期数量**（`新建 + 跳过 = 子级总数`），
  差额必须能解释清楚
- 任何"跳过/失败/未处理"的计数都要**带着原因打印**，不能只给数字
- 新增一类生成物时，**同时写一条"数量对不对"的断言**（比事后人眼数可靠）

### B.2 行政区划代码（adcode）的层级规则，必须当成不变量

本轮所有几何/资料错位的根因都可以归结为"没把 adcode 的结构当规则用"：

```
省级  x0000   （如 510000 四川省）
地级  xx00    （如 510100 成都市、110000→北京这种直辖市的"市辖区"层）
县级  xxxx    （如 510104 锦江区）
```

**由此推出三条硬规则**（已在代码里落实，别再违反）：
1. **父子关系不要用 adcode 前缀猜** —— 乐山 511100 的父级是 510000，两者不同前缀。
   一律走包配置里声明的 `parent`（坑 #29）
2. **只给"有下级"的行政区建地图** —— 判断依据就是 `adcode % 100 === 0`（地级）
3. **省级地图的 geo 里"第一条 feature 常是自己"** —— 必须剔除，
   否则引擎会把它当一块拼图（坑 #37）

### B.3 校验要在"生成"和"展示"两层都有

本项目最终的可靠性来自**四道互相独立的关卡**，缺一道就会漏：

| 关卡 | 时机 | 抓什么 |
| --- | --- | --- |
| `test-maps.js`（1822 项） | 离线，秒级 | 结构：adcode 对应、关卡覆盖、资料齐全、体积不变量 |
| `map-smoke.html`（9085 项） | 浏览器，8 分钟 | **真的能玩**：每张地图真拖一块、信息卡对、刷新进度在 |
| 几何交叉校验（面积） | 生成期 | 抓错条目（同名词条） |
| 事实追溯校验 | 补资料期 | 编造内容（evidence 必须出自素材） |

> **"能跑"和"结构对"是两件事**。本轮省级地图"结构对但玩不了"，
> 就是靠浏览器层（真拖一块）才发现的 —— 离线断言全绿。

### B.4 优化之前先量，别凭直觉

本轮两次纠正都是"先量再判"救回来的：

- 我以为 registry 是"必要元信息" → 实测 430 字节/条、494 张要 208 KB → 发现 `scripts` 可推导
- 我以为 `hueFor` / `hueByLevel` 是冗余 → 实测是"生成期默认 + 运行期覆盖"的有意分层 → **不是冗余**
- 我提出"应该加一条 adcode 归属断言" → 实测该断言**早已存在**（`关卡覆盖全部行政区`）

**结论**：评估"冗余/缺失"时，`grep` 一遍、跑一遍、数一遍，**别靠印象**。
报告里要能贴出实测命令与数字。

### B.5 测试是重构的前提，而不是负担

`engine-test.html` 用**虚构的 tiny-city** 测引擎 —— 这条设计极其值钱：
它让"改引擎"这件事与"363 张真实地图"解耦，回归成本从几分钟降到 12 秒。
**新增玩法时优先沿用这个套路**（虚构 fixture + 独立套件）。

### B.6 为将来的移植留门：纯逻辑与渲染分开

`js/geomap.js`（181 行、零 DOM）是本项目可移植性最好的一层。
反观 `js/engine.js`（1554 行、62 处 `document.`）——**逻辑与渲染混在一起**，
将来若要做原生端，主要成本就在这里。

**下次改 engine.js 时顺手做的一件事**（成本低、收益大）：
把这三块显式分层，依赖注入一个 `renderer` 接口而不是直接调 `document.*`：

1. 关卡状态机（`placed/tries/solved/unlocked` 的迁移）
2. 落点判定（多边形命中 + 容错）
3. 几何→屏幕坐标（viewBox 变换）

详见 `docs/架构与可移植性评估.md`。

### B.7 快速上手指令（节省摸索成本）

```bash
node tools/status.js                                  # 现状（地图数/资料缺口/下一步）
node tools/todo-report.js                             # 资料缺口（按市分组，可 --map=<id>）
node tools/test-maps.js 2>&1 | tail -3                # 结构自检（秒级）
node tools/e2e-test.js --only-suite=offline           # 只跑离线检查（秒级）
node tools/e2e-test.js --only-maps=<ids> --only-suite=smoke   # 只测某几张地图
node tools/batch-add-maps.js --parent=<省 adcode>      # 接一个省（秒级）
node tools/area-from-geo.js --parent=<省 adcode> --write      # 补面积（秒级）
```

**别做**：手写 `js/maps/**/*.geo.js`、手改 `js/maps/registry.js`、
用 `cat/head/tail` 看 `.geo.js`（单行 100 KB+）、
改引擎后忘了重建 registry。

