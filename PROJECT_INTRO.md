# 地图拼图 · 项目介绍

> 一个**纯静态、零依赖**的中国行政区划拼图。拖动碎片放回地图上正确的位置，
> 拼对了弹出这个省 / 市 / 区县的介绍卡。
>
> 本文面向第一次接触这个项目的人（包括替你 review 的 AI）：
> 讲清它是什么、怎么搭起来的、为什么这么做、以及它现在**还不能**做什么。

---

## 一、一句话描述

**用拼图认识中国。** 覆盖全国 34 个省级、329 个地级、3214 个下级行政区，
双击一个 HTML 文件就能玩，不需要服务器、不需要联网、不需要注册。

- 仓库规模：363 张地图包、3214 条资料条目、约 7000 行手写代码（不含地图数据）
- 地图数据：20 MB（全部内联为 `.js`，见 §4.2）
- 测试：**20000+ 项断言**，其中 13926 项是"把 363 张地图逐张真的拖一块进去"

---

## 二、核心架构图

```
                     ┌───────────────────────────────────────┐
   浏览器            │  index.html（静态骨架 + 全部 UI 容器）  │
                     └───────────────────┬───────────────────┘
                                         │ 按顺序加载 10 个脚本
     ┌───────────────────────────────────▼───────────────────────────────────┐
     │  纯逻辑层（零 DOM、可在 Node 里单测）                                    │
     │  ┌────────────┬────────────┬────────────┬────────────┬──────────────┐ │
     │  │ progress   │ score      │ share      │ modes      │ contribute   │ │
     │  │ 进度/成就   │ 计分/连击   │ 分享卡文案  │ 六套模式规则 │ 众包纠错文案  │ │
     │  └────────────┴────────────┴────────────┴────────────┴──────────────┘ │
     └───────────────────────────────────┬───────────────────────────────────┘
                                         │ 被宿主层调用
     ┌───────────────────────────────────▼───────────────────────────────────┐
     │  宿主层  js/game.js                                                    │
     │  · 地图导航（面包屑 / 选择器）  · 玩法模式选择与接线                      │
     │  · 进度条 / 徽章 / 每日一图 / 家乡  · 纠错面板  · 错误边界               │
     └───────────────────────────────────┬───────────────────────────────────┘
                                         │ 只传"配置对象"（不认识具体城市）
     ┌───────────────────────────────────▼───────────────────────────────────┐
     │  通用引擎  js/engine.js（+ js/geomap.js 投影）                          │
     │  拖拽判定 · 关卡推进 · 信息卡 · 存档 · 提示 · 得分回调                    │
     │  ★ 不含任何地图数据：geo / levels / palette / storage / texts 全靠传入   │
     └───────────────────────────────────┬───────────────────────────────────┘
                                         │ 配置来自
     ┌───────────────────────────────────▼───────────────────────────────────┐
     │  地图工厂  js/maps/                                                    │
     │    registry.js  总登记册（363 条元信息，8.6 万字节，首屏加载）            │
     │    loader.js    按需注入：选中某张图时才下载它的 3 个脚本                 │
     │    <id>.js       配置（配色/存储/文案）                                 │
     │    <id>.geo.js   边界数据（构建产物，几十 KB ~ 1.6 MB）                  │
     │    <id>.data.js  资料与关卡（人工维护）                                 │
     └───────────────────────────────────────────────────────────────────────┘
```

**一条铁律贯穿全图**：引擎不知道"成都"，也不知道"模式"。
地图数据靠配置传入，玩法模式靠几个通用开关传入
（`pieceNames` / `hintLimit` / `softenFeedback` / `disableMotion` / `onPlacement`）。
所以加一张地图、加一套玩法，都不用碰引擎。

---

## 三、分层与职责

| 层 | 文件 | 职责 | 为什么这样分 |
| --- | --- | --- | --- |
| 纯逻辑 | `progress.js` `score.js` `share.js` `modes.js` `contribute.js` | 账本、计分、文案、模式规则、众包文本 | 不碰 DOM ⇒ 能在 Node 里跑断言，也能量到别的端（React Native / Swift） |
| 投影 | `geomap.js` | 墨卡托投影、path/bbox/质心 | 手写，不引 d3（§4.3） |
| 引擎 | `engine.js` | 拖拽、判定、关卡、信息卡、存档 | 与具体地图无关，可用虚构城市单独测 |
| 宿主 | `game.js` | 导航、模式、面板、错误边界 | 这些是"页面级"的事，不该进引擎 |
| 数据 | `js/maps/**` | 三件套地图包 | 构建产物与人工内容分离 |

---

## 四、技术亮点与设计决策

### 4.1 为什么零依赖、纯静态

**决策**：不用框架、不用构建工具、不用 npm 依赖、不用后端。`file://` 双击即玩。

**理由**：这个项目的运行成本必须是 **0**，并且**十年后还能打开**。
没有构建链就没有"依赖过期"，没有后端就没有"服务器到期"。
代价写在 §5.1。

### 4.2 为什么把 GeoJSON 内联成 `.js`

**决策**：边界数据存成 `<id>.geo.js`（`window.MAP_GEO['chengdu'] = {...}`），
而不是 `.json` 让页面 `fetch`。

**理由**：`file://` 协议下浏览器会拦 `fetch` 本地 JSON（CORS），
但普通 `<script>` 不受影响。"双击即玩"这条底线就是这么保住的。

### 4.3 为什么手写墨卡托投影，不引 d3

**决策**：`js/geomap.js` 自己实现投影与路径计算。

**理由**：只需要球面墨卡托 + 面积 + 质心三件事，d3 的体积（几百 KB）换不来什么。
**收益**：面积算法用同一份公式算出了与官方公布值吻合的结果
（五通桥 465/465、峨边 2383/2382、乐山 12742/12720），并且**与拼图所用边界严格同源**。

### 4.4 关卡按什么分组（这是一个内容决策，不是技术决策）

| 图 | 分组依据 |
| --- | --- |
| 中国 | **地理分区**手工编排 7 关：东北 3 / 华北 5 / 华东 7 / 华中 3 / 华南 6 / 西南 5 / 西北 5 |
| 省 | **按离省会距离**（成都的"圈层"：中心城区 / 近郊 / 远郊） |
| 市 | **按民政部官方行政区划类型**（市辖区 / 县级市 / 县 / 自治县 / 旗…） |

**为什么市一级用"行政类型"**：它是**有含义的默认值**，而不是随机切分。
想按地理相邻或文化圈重排，直接改数据文件里的 `levels` 即可。

> 中国图的关卡 id 刻意用语义名（`dongbei` / `huabei`…）而不是 `l1`/`l2`：
> `tools/regroup-levels.js` 有一条"手写关卡"守卫 —— 只要发现非 `l\d+` 的 id，
> 就认定这份关卡是人工编排的、整体跳过不重排。这是保住人工分区的机制。

### 4.5 玩法模式：声明成开关，而不是写六套逻辑

六套模式（普通 / 计时挑战 / 教学学习 / 考试刷题 / 儿童 / 本地双人）
全部由**几个通用开关**组合而成：

| 开关 | 作用 | 谁在用 |
| --- | --- | --- |
| `pieceNames` | 碎片是否常显地名 | 教学（常显） |
| `hintLimit` | 提示次数上限 | 考试（0）/ 计时·普通（3）/ 儿童与简单（不限） |
| `softenFeedback` | 放错反馈是否放软 | 儿童 |
| `disableMotion` | 跳过引擎动画 | 考试 |
| `onPlacement` | 每次放置的回调 | 双人回合 / 错题集 / 正确率 |

**收益**：加第七套模式不需要改引擎，只要在 `js/modes.js` 的 `MODES` 表里加一行。

### 4.6 所有状态都在 localStorage

进度、成就、最佳成绩、错题集、对战战绩、模式偏好、教学选区 —— 一共三个键，
互不覆盖：

```
map-puzzle-progress   跨地图总账（progress.js）
map-puzzle-modes      模式偏好 + 最佳成绩 + 错题集 + 对战 + 选区（modes.js）
<id>-map-puzzle       每张地图自己的存档（引擎）
```

**代价**：换设备 / 清缓存会丢。**换来**：永远不花钱、永远打得开、不需要账号。

### 4.7 测试为什么这么多

**决策**：20000+ 项断言，其中冒烟套件会把**登记册里每一张地图**都打开、
真的派发 `PointerEvent` 拖一块进去、刷新确认进度还在。

**理由**：这个项目最大的风险不是"代码写错"，而是"数据文件写错但没人发现"。
批量生成了 363 张地图之后，"文件生成了"和"东西能用"是两件事。
这套测试抓出过的真 bug 包括：

- 几何层只认 `MultiPolygon`，喂标准 `Polygon` 直接抛错
- 省级地图的 geo 里混进了自己的轮廓 → 6 张图拖不进去
- 344 张地图的显示名被静默兜底成拼音 slug（`hebei` / `shijiazhuang`）
- 中国图 1 关 34 块，关卡名却标成"地级市"

### 4.8 冒烟套件为什么分批跑

一个 Chrome 里连开 363 个 iframe 之后，实测每张地图耗时从 1.5s 漂到 5s+，
1800s 都跑不完；拆成每批 60 张、各起一个独立 Chrome，全程只要 536s。
顺带解决两件事：每批有独立进度输出；真卡死时只丢一批，不是全丢。

---

## 五、已知限制（诚实清单）

### 5.1 技术限制

| 限制 | 说明 | 会影响谁 |
| --- | --- | --- |
| **`pages.dev` 在国内不稳** | Cloudflare 在中国大陆没有节点，有被墙报告 | 用 Cloudflare 托管的访客 |
| **单文件版首次加载慢** | 全国版是 18.5 MB 单个 HTML，浏览器要解析十几秒 | 想要"发文件就能玩"的人 |
| **没有后端** | 收不到用户数据、做不了排行、做不了账号 | 想看到"多少人在玩"的维护者 |
| **没有 SEO** | 单页 + 查询参数，搜索引擎收录困难 | 想靠搜索获客 |
| **存档只在本地** | 换设备 / 清缓存即丢 | 多设备用户 |
| **只做过 Chrome 自动化测试** | Safari / Firefox 只做过人工点，没有自动断言 | 非 Chrome 用户 |

### 5.2 内容限制（**当前最大的问题**）

| 指标 | 数字 |
| --- | --- |
| 资料条目 | 3214 条 |
| **内容完整** | **116 条（3.6%）** —— 四川的成都、乐山，加上 34 个省级行政区 |
| 待补 | 3098 条 / 9173 个字段 |
| 面积 | 3214 / 3214 ✅（从官方边界几何算出，与公布值核对过） |

**约 96% 的区县点开只有一句「📖 资料收录中，欢迎参与共建」。**

### 5.3 合规与口径限制

- 边界数据全部来自**国家地理信息公共服务平台（天地图）**，审图号 **GS(2024)0650号**，
  页脚始终显示来源声明
- 34 条省级文案里的**数字与时效表述需要人工终审**（见 `CHANGELOG.md`）
- **不编造内容**：查不到就留占位、不猜 —— 这是项目的硬规则

### 5.4 项目状态

- **真实用户数：0**（正在做第一轮真人验证）
- 没有任何收入，也没有成型的变现设计

---

## 六、怎么跑起来

```bash
# ① 直接看（最省事）
open index.html

# ② 本地服务（更接近线上）
node tools/serve.js --map=sichuan

# ③ 生成"发给别人就能玩"的单文件
node tools/bundle.js --province=sichuan    # → out/地图拼图-四川省.html

# ④ 生成"可以直接拖上静态托管"的裁剪包（< 1000 个文件）
node tools/deploy-pack.js --province=sichuan   # → deploy-四川省/（81 个文件）

# ⑤ 临时公网链接（手机/电脑/微信都能开）
node tools/share-online.js --map=sichuan

# ⑥ 跑测试
node tools/e2e-test.js --only-suite=offline 2>&1 | tail -12   # 秒级
node tools/e2e-test.js 2>&1 | tail -22                        # 全量，约 9 分钟
```

**URL 参数**：`?map=<地图id>`　`?mode=<模式id>`　`?diff=<难度id>`　`?level=<第几关>`

---

## 七、目录导航

```
index.html                    页面骨架 + 全部 UI 容器 + 手绘 SVG 图案库
css/style.css                 全部样式（设计令牌驱动，三套主题）
js/                           纯逻辑层 + 引擎 + 宿主（见 §2 架构图）
js/maps/                      363 张地图包（registry + loader + 三件套）
tools/                        40+ 个 Node 脚本（构建 / 校验 / 测试 / 打包 / 部署）
tools/lib/                    公共库（天地图客户端、WKT、面积、官方区划树…）
docs/                         设计与策略文档（见下）
deploy-templates/             Cloudflare 的 _headers / _redirects 模板
data/tianditu-official/       天地图官方数据快照（合规凭据）
```

**文档**：

| 文件 | 内容 |
| --- | --- |
| `地图拼图项目_开发SOP与交接文档_v1.0.0.md` | 开发 SOP + 42 条踩过的坑（最完整的一份） |
| `HANDOVER.md` | 交接：从哪接、跑什么命令 |
| `CHANGELOG.md` | 逐轮改动 + 待人工终审清单 |
| `README.md` | 面向使用者的说明 |
| `docs/部署到Cloudflare.md` | 部署与故障排查 |
| `docs/项目现状简报（给外部AI）.md` | 完整现状（含"需要人工提供什么"） |
| `docs/可持续性与内容生产.md` | 内容生产与成本策略 |
| `docs/玩法设计与传播机制.md` | 玩法与传播设计 |
| `docs/架构与可移植性评估.md` | 架构评估 |
| `README-如何发给同学.md` | 怎么让别人真的玩到 |

---

## 八、这个项目想验证的一件事

技术骨架已经完成：363 张地图 100% 接入、3214 个行政区、20000+ 项测试全绿。
**接下来要验证的不是技术，而是"有没有人想玩"。**

所以当前的阶段是 **验证**，不是开发：
在拿到 10 个真人的反馈之前，不再加新功能。
