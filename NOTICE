地图拼图 · 附加声明（NOTICE）
=====================================================================

## 一、许可范围

本项目的 **代码**（HTML / CSS / JavaScript / 构建脚本 / 文档）采用 MIT 许可，
全文见同目录的 `LICENSE`。

## 二、地图数据不适用 MIT

`js/maps/**/*.geo.js` 里的行政区划边界数据**不适用 MIT 许可**，它来自：

- **来源**：国家地理信息公共服务平台（天地图）· 服务中心行政区划
- **审图号**：GS(2024)0650号

这些是官方测绘成果，使用需遵守其自身的规定。
**数据与代码的许可是两回事**，请不要把 MIT 误当成对地图数据的授权。

若要把本项目用于公开发布或商业用途，**请自行确认**地图数据的使用合规性。

## 三、这些声明在页面上如何呈现（改动代码时请勿破坏）

- 来源与审图号存在 `.geo.js` 里的 `MAP_GEO_META[<地图 id>]`，与边界数据写在同一个文件里
  （写入逻辑见 `tools/lib/inline-geo.js`）
- `js/game.js` 的 `renderSourceNotice()` 读取它，渲染到页脚 `#mapSource`
- 也就是说**页脚文案不是硬编码的** —— 换数据源时声明会自动跟着变。
  这本身就是一条防伪造设计：数据是天地图却标别家的审图号，比不写更糟

## 四、为什么单独一个 NOTICE 文件

最初把上面这段直接接在 `LICENSE` 的 MIT 正文后面，结果 GitHub 把仓库许可
识别成 **NOASSERTION**（它一遇到正文之外的内容就不下结论），仓库侧栏因此
少了 MIT 徽章。实测对照：

| LICENSE 内容 | GitHub 识别结果 |
| --- | --- |
| MIT 正文 + 附加说明（blockquote 或 HTML 注释） | NOASSERTION |
| **只有 MIT 正文** | **MIT** ✅ |

所以：`LICENSE` 保持纯 MIT，说明放这里。这是 Apache 等项目的通行做法。
