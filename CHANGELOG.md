# CHANGELOG

> 本文件按轮次记录改动。每条都标注 **✅ 已完成** 或 **⏳ 待人工终审**。
> 项目维护规则见 `地图拼图项目_开发SOP与交接文档_v1.0.0.md`。

---

## 2026-09-20 · 第四轮：极小碎片自动放大 + 默认地图改中国

> 本轮的目标是"体验跃迁"里的 P0-1（默认地图）与 P0-2（极小碎片）。
> 全部改动都有实测数据支撑，下面记录**结论与被推翻的假设**。

### ⚙️ 功能类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| **默认地图改为中国图** | ✅ 已完成 | `js/game.js` 的 `DEFAULT_MAP`：`chengdu` → `china`。`tools/serve.js`、`tools/share-online.js` 的默认图同步跟上（这两个工具就是"发给人玩"，默认图必须等于产品第一印象） |
| 国家图首屏先亮全貌 | ✅ 已完成 | 根级地图首次进入时先展示完整版图（约 280ms）再平滑聚焦到第 1 关。此前首次进入是 `duration=0` 直接定位，而中国图第 1 关是"东北"——打开只能看到东北一角，实测全图视野仅覆盖 79%×50%。只有一关的图不绕这一趟 |
| **极小碎片拖拽：改自动放大** | ✅ 已完成 | 废弃"屏幕角落放大镜"，改为拖拽时**镜头自动推近**目标区域。推近挂在 `pointerdown`（按下那一刻），玩家真正开始拖动时立刻落定（`settleZoomNow`），保证整段拖拽镜头静止 |

### 🔬 本轮被实测推翻的假设（重要，避免后人重走）

| 原假设 | 实测真相 |
| --- | --- |
| "澳门 bbox 宽 0.1°" | 地图坐标系里是 **0.9×1.8 单位**（第 5 关屏幕上 0.81×1.63px） |
| "8× 推近够用" | 要达到可瞄准尺寸需要**约 66 倍**。凭直觉设 8 时推近后澳门只有 6.5px，**等于没推** |
| "推近后落点会自动正确" | **推近中松手会打到隔壁**（实测淮北判成宿州）。根因是镜头在拖拽期间仍在变化 |
| "1.6MB 的中国图首屏会很慢" | 实测 **65ms** 就绪（成都 84ms）。地图是按需注入的，首屏无空白期，骨架屏根本来不及显示 |
| "连续切图会导致内存持续上涨" | 切图走 `location.href` 整页重载，旧文档整体销毁。连续切 12 张：堆稳定 26.3MB、**增量 0** |

### 🎨 体验类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 镜头过渡改为"丝滑" | ✅ 已完成 | `easeOutCubic` → **`easeInOutCubic`**。前者起步即满速，第一帧位移过大，肉眼看到的是"跳"而非"移动"（实机反馈"猛、卡顿"）；时长 320→460ms |
| 推近幅度收敛 | ✅ 已完成 | `zoomFitPx` 104→76（澳门倍率 90×→66×，尺寸 73×147px→54×109px），仍远高于 44px 可点击下限 |
| **修复"放完一块后地图卡在放大状态"** | ✅ 已完成 | 真 bug。根因是退回全景的定时器被**后一次 `scheduleZoomOut` 取消**（那次 `zoomedFor` 已为 null 仍被排程），定时器再没触发。改为只在"确实处于近景"时才排程 |
| 放错位置后镜头缓退全景 | ✅ 已完成 | 与"拼完整图拉远"同一节奏（700ms），既给挫败一个缓冲，也提醒"它在整张图的哪儿" |
| 触屏判定点上抬 | ✅ 已完成 | `pointerType` 为 touch/pen 时判定点比触点高 8px（手指遮在下方、视线在上方） |

### 🧰 工程与工具类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 新增"极小碎片"回归套件 | ✅ 已完成 | `tools/tiny-drag-test.html`（42 项）。**必须用真实中国图**：虚构 tiny-city 每块接近满屏，永不触发推近，只能守"不该推时别推" |
| 引擎补 2 个公开入口 | ✅ 已完成 | `getLevelViewBox()` / `resetView()`。此前测试自己拿剩余空位反推"全景"，拼到后面空位变少会越推越小（实测 682609 算成 5788） |
| 探针合并 | ✅ 已完成 | 7 个一次性 `exp-*.html` → 2 个（`exp-boot.html` 用 `?what=fit\|perf\|mem\|mobile` + `exp-coord.html`）。一个测量一个文件等于把真相分散在多份文件里（坑 #44） |
| 首屏性能断言 | ✅ 已完成 | 冒烟套件每张地图新增 2 条（共 +726）：加载耗时 <4000ms、就绪后骨架屏已摘掉。阈值留 4 秒余量，只拦数量级退步 |
| 修正文档互相矛盾的基线 | ✅ 已完成 | README 写 16145/2053、`status.js` 写 2053，而真实是 17148/2202。三份文档各说各话已统一，并写明"快照不是真相，以实测为准" |

### 🖥️ 跨平台 UI/UX（P0-4）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 四态布局 | ✅ 已完成 | 原有三态（≥1081 桌面 / ≤1080 平板 / ≤620 手机），本轮补上**教室大屏 ≥1600**：字体 14→16px、侧栏 336→408px、控件放大、装饰动画弱化 |
| **修复手机横屏"地图在屏外"** | ✅ 已完成 | 844×390 实测：顶栏+导航+每日一图+我的家乡+关卡+来源+纠错叠到约 280px，视口只有 390px → 拼图区完全看不到。矮屏时收起"有则更好"的三块并压扁留白；**审图号与来源保留**（合规） |
| 动效时长统一 | ✅ 已完成 | 70+ 处硬编码 → 四档令牌（`--dur-fast/--dur/--dur-mid/--dur-enter`）。只替换 `transition` 里的时长，动画关键帧保留原值 |
| 键盘完整通关 | ✅ 已完成 | 新增断言：纯键盘把整关拼完 + 通关弹窗出现。此前只验了"单块能用键盘放" |
| 三主题兼容 | ✅ 已完成 | 5 断点 × 3 主题机器判定：主色互不相同、切换生效、切后仍无溢出 |
| 断点截图证据 | ✅ 已完成 | `docs/shots/` 7 张（手机竖横屏 / 平板 / 桌面 / 大屏 / 银杏 / 朱砂），`tools/exp-shot.js` 可重跑 |

#### ⚠️ 本轮踩到的坑（写给后人）

**CSS 媒体查询的顺序会静默吃掉你的规则。** 矮屏收口第一版写在文件中部，
而 `.daily-chip { display: flex }` 在更靠后的第 2090 行 —— 两者都是单类选择器
（优先级相同），**后来者胜**，于是 `display: none` 完全不生效。
靠肉眼看不出来（截图里它还在），是探针打出 `mediaMatch=true` 但
`display=flex` 才暴露。**教训：媒体查询要放在被覆盖规则之后。**

**断言要等"目标状态"，不要等"不再变化"。** 键盘通关那两条一开始失败，
原因不是引擎：通关弹窗是最后一块落位后**延迟 620ms** 才开的
（给吸附动画留时间），而断言立刻就读了 `modal.hidden`。
同一类错误本轮还犯过一次（等 viewBox"不再变化"会在"停在近景等退回"的空档返回）。

### ⏳ 待人工终审 / 待真机验证

| 项 | 说明 |
| --- | --- |
| 镜头推近的**手感** | 幅度与时长已按实机反馈调过一轮（见上）。需真人再试一次确认"丝滑" |
| 低帧率自动降级 | **本轮不做**。所有视口实测 60fps、0 掉帧，headless 无法可靠复现低端机；写在真机上量过再决定，避免加一条永远不会执行的代码路径 |
| 真机（低端 Android）帧率 | 同上。这是唯一还没被验证的性能维度 |

---

## 2026-09-19 · 第三轮：六套玩法模式 + 工程健壮性 + 部署文档

### ⚙️ 功能类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| **六套可切换玩法模式** | ✅ 已完成 | 顶栏新增「玩法」下拉。全部**本地离线**实现，不联网、不需要账号 |
| ├ 普通模式 | ✅ | 与改动前**完全一致**：不加任何信息条，只显示原有界面 |
| ├ 计时挑战 | ✅ | 三档难度（简单·提示不限 / 普通·提示 3 次 / 困难·无提示且藏地名）；按图按难度记个人最佳（localStorage）；自动收集易错行政区成"薄弱练习集" |
| ├ 教学学习 | ✅ | 碎片永久显示地名；可勾选部分行政区单独练；课堂大屏布局；导出 Markdown 练习报告；每个行政区一键跳百科参考 |
| ├ 考试刷题 | ✅ | 藏地名、提示 0 次、关闭动画、计时；结束给正确率、评级与错题清单 |
| ├ 儿童模式 | ✅ | 简化统计（隐藏得分/连击）、提示不限、只用鼓励文案（有断言保证不出现"失败/扣分/连击中断"） |
| └ 本地双人对战 | ✅ | 同机轮流拼、一块一轮、放对 +10 放错 −3（不低于 0）、累计历史胜负。无联网、无匹配 |
| 模式 → 引擎的翻译层 | ✅ | 新增 `js/modes.js`（纯逻辑）。引擎**不认识"模式"**，只认 5 个通用开关：`pieceNames`/`hintLimit`/`softenFeedback`/`disableMotion`/`onPlacement`。加第七套模式不用改引擎 |
| 教学模式选区 | ✅ | `?pick=<adcode,…>` 裁剪关卡；完整列表单独留一份，否则选完就加不回来 |
| 模式也可用 URL 直开 | ✅ | `?mode=exam` / `?diff=hard` / `?big=1` —— 老师可以直接把模式链接发给学生 |

### 🎨 体验类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 🔴 **修复 390px 横向溢出（老 bug）** | ✅ 已完成 | **改动前就存在**：中国图在 390px 下 `scrollWidth=583`。根因是顶栏 flex 子项的 `min-content` 下限把父级顶开；窄屏改为 block 布局收口。以前的窄屏断言用成都（标题短）所以没碰上 |
| `[hidden]` 全局兜底 | ✅ 已完成 | `.map-picker`/`.daily-chip` 等自带 `display:flex`，类选择器优先级高于 UA 的 `[hidden]` → hidden 形同虚设（实测：教学模式里"难度"选择器照样显示）。已加 `[hidden]{display:none!important}` |
| 提示按钮文案层 | ✅ 已完成 | 引擎改"剩 N 次"时用 `textContent` 会把左侧 SVG 图标一起抹掉 → 改为只改 `[data-hint-label]` |
| 碎片无障碍标签 | ✅ 已完成 | 每块报出"第 N 块／共 M 块"；教学模式额外报出地名（屏幕阅读器才"看得见"） |
| 模式界面 a11y | ✅ 已完成 | 模式信息条 `aria-live`、选择器 `aria-label`、面板 `role=dialog`、提示按钮 `aria-disabled` + title 说明为什么点不动 |
| prefers-reduced-motion | ✅ 已完成 | 新增的模式动画全部在 `@media (prefers-reduced-motion: reduce)` 里关掉；考试模式另有"全站去动画"规则 |

### 🧰 工程与工具类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 错误边界加固 | ✅ 已完成 | 交给引擎前先校验地图包（geo 缺失/为空、levels 为空、关卡里出现 geo 里不存在的 adcode）；不合法就给人话提示 + 结构化 `console.error`，不再静默卡死 |
| 加载失败报错 | ✅ 已完成 | 404/语法错误时控制台给结构化信息（message/stack/hint），页面上给"最可能的两个原因" |
| `tools/deploy-pack.js` | ✅ 已完成 | **专治 Cloudflare 1000 文件上限**：按范围裁出多文件部署包（四川 81 个文件 / 2.9MB，可直接拖拽）。内置 4 组产物断言：引用完整 / 三件套齐全 / registry 已裁 / 无越界目录 |
| `tools/bundle.js` 裁剪能力 | ✅ 已有 | `--maps=a,b` / `--province=` 输出**单文件**（区别于 deploy-pack 的多文件） |
| `tools/modes-test.html`（第 4 套浏览器套件） | ✅ 新增 | 77 项：六套模式 × 界面/引擎开关，固定用 china + chengdu。含溢出诊断（报出"是谁溢出的"） |
| `tools/test-modes.js` | ✅ 新增 | 108 项纯逻辑断言：模式定义、难度映射、最佳成绩、错题集、正确率、对战回合、报告、儿童文案、百科链接 |
| 冒烟套件分批 | ✅ 已有 | 每批 60 张、独立 Chrome；全量从"1800s 跑不完"降到 536s |

### 📄 文档类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| `PROJECT_INTRO.md` | ✅ 新增 | 项目概述、架构图、技术亮点与设计决策、已知限制、目录导航 |
| `deploy-templates/_headers` | ✅ 新增 | 安全头 + **保守**缓存策略（文件名无 hash，激进缓存会让老访客看到旧版）；附激进版注释块 |
| `deploy-templates/_redirects` | ✅ 新增 | 短链接（`/sichuan`、`/china/exam`…）+ 404 兜底；状态码用 302 |
| `docs/部署到Cloudflare.md` | ✅ 已补 | 新增"两条部署路线对比"与"_headers / _redirects 模板"两节 |
| `CHANGELOG.md` | ✅ | 本文件 |

### 🔒 合规类

| 项 | 状态 |
| --- | --- |
| 边界坐标 / 审图号 / 南海诸岛 / 台湾 / 藏南 / 阿克赛钦 | ✅ **本轮一个字都没动** |
| 34 省科普文案的事实类内容 | ✅ **一个字都没改**（按红线，等人工终审） |
| 关卡分区结构 | ✅ **未调整**（只补了说明性注释） |
| 技术栈 | ✅ 仍是纯原生 HTML/CSS/JS，**零依赖**；`file://` 双击即玩；新增状态全部在 localStorage |

---

## ⏳ 待人工复核清单（本轮新增 3 项）

### 1. 涉及事实类的文案

本轮**新增**的用户可见文案里，涉及数字/年份/地名的部分已按红线**留出复核标记**：

| 位置 | 文案 | 类型 |
| --- | --- | --- |
| 计时挑战·难度说明 | "提示最多 3 次" | 数字（我们自己定的规则，非外部事实） |
| 双人对战·计分说明 | "放对 +10 分，放错 −3 分" | 数字（自定规则） |
| 教学报告 | 面积取自 `districts[adcode].area` | **外部数字** —— 来自官方边界几何计算，需与公布口径确认 |
| 儿童模式鼓励语 | 4 档共 12 句 | 无事实内容，但请确认语气适合小学生 |
| 各模式说明文案 | "全程离线，不需要联网，也不需要账号" | 声明类，需确认与实际行为一致 |

**上一轮遗留的 34 条省级文案事实终审仍未完成**，见上一节"待人工终审清单"。

### 2. 关卡分区方案（上一轮遗留）

七分区为非官方分法，仍需确认：东北/华中只 3 块是否接受、台湾归华南、福建与江西归华东、重庆归西南。

### 3. 部署文档完整性（上一轮遗留）

`docs/部署到Cloudflare.md` 是**按记忆重建**的（原稿被误删且无 git 记录），需核对有无遗漏。
本轮新增的两节（部署路线对比 / `_headers` `_redirects`）是新的，也需要你过一遍。

---

## 测试基线

| 套件 | 数量 |
| --- | --- |
| 离线检查（11 项） | 2202 |
| 城市回归 · 成都 | 89 |
| 引擎功能 · tiny-city | 77 |
| 玩法模式 · 六套模式 | 77 |
| 多地图冒烟 · 363 张地图 | 13926 |
| **合计** | **16371** |

重跑全量：`node tools/e2e-test.js 2>&1 | tail -24`（约 9 分钟）
快速自检：`node tools/e2e-test.js --only-suite=offline 2>&1 | tail -14`
只跑模式套件：`node tools/e2e-test.js --only-suite=modes 2>&1 | tail -6`

---

---

## 2026-09-17 · 第二轮：P0 上线前置（合规 / 关卡 / 内容 / 体验）

### 🔒 合规类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 34 个省级行政区完整性核查 | ✅ 已完成 | `china.geo.js` 实测 35 个块 = 34 省级 + 南海诸岛及海上界线，全部齐全 |
| 澳门数据 | ✅ 已完成 | **核查结论：本来就不缺**。澳门（820000「澳门特别行政区」）的 geo 轮廓与资料条目都在中国图内。官方区划数据里澳门没有下级区划，按项目既有规则**不单独建地图包**（强行加进 `registry.children` 会导致面包屑 404，且需要伪造边界数据） |
| 边界坐标 | ✅ 未触碰 | 本轮未修改任何 geo 文件的边界坐标 |
| 南海诸岛及海上界线 | ✅ 未触碰 | 该 feature 仍在，新增断言钉住 |
| 台湾表述 | ✅ 未触碰 | 官方表述保持「台湾省」 |
| 港澳表述 | ✅ 未触碰 | 保持「香港特别行政区」「澳门特别行政区」 |
| 审图号与来源声明 | ✅ 未触碰 | `GS(2024)0650号` + 天地图来源，由 `MAP_GEO_META` 带出、`renderSourceNotice()` 渲染；新增断言 |
| registry 子级列表 | ✅ 无需变动 | `build-registry.js` 重跑**零 diff**（本轮无地图包结构变化） |

### ⚙️ 功能类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 中国图关卡拆分 | ✅ 已完成 | 原为 **1 关 34 块**，拆为 **7 关**：东北 3 / 华北 5 / 华东 7 / 华中 3 / 华南 6 / 西南 5 / 西北 5。34 块不重不漏，每关 3~7 块 |
| 关卡命名 bug 修复 | ✅ 已完成 | 原关卡名错标为「地级市」（中国图的下级其实是省级），已改为实际分区名 |
| 关卡配色同步 | ✅ 已完成 | `china.js` 的 `palette.hueByLevel` 键名同步为 7 个语义 id，否则碎片颜色会全部落到 fallback |
| 手写关卡保护 | ✅ 已完成 | 关卡 id 用语义名（`dongbei`/`huabei`…），`tools/regroup-levels.js` 的手写守卫据此整体跳过，自动重排不会覆盖人工分区（已实测） |
| 分享成绩文案 | ✅ 早已存在 | `js/share.js` + 结算页「分享成绩」→ Canvas 卡片 + 「复制文案」，27 项测试覆盖，本轮无需修复 |

### 🎨 体验类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 首屏标题显示「成都」 | ✅ 已完成 | 根因：`index.html` 静态文案硬编码「成都」，而地图是按需加载的，加载期间 JS 还没跑。已改为中性文案（「地图拼图」/「正在加载地图…」） |
| 加载期间动态显示地图名 | ✅ 已完成 | `boot()` 先从 registry 取名字填标题栏与文档标题，不必等 geo（中国图 1.6MB）下载完；就绪后 `updateBrand()` 再补「多少块」 |
| 极简骨架屏 | ✅ 已完成 | 银杏叶 + 不确定进度条，`markReady()` 淡出；**`showFatal()` 也摘掉**（只摘成功路径的话，出错时它会一直转，比没有更糟） |
| geo 按需懒加载 | ✅ 早已存在 | `js/maps/loader.js` 本来就是按需注入（首屏只载 226KB 通用代码）。**"首屏只加载第一关"未做** —— 底图需要整张图的轮廓当参照，切分 geo 属架构改动 |
| 移动端碎片拖拽放大 | ⏳ 未做 | P1 可选项，本轮未执行 |

### 🧰 工具类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| `tools/fill-china-provinces.js` | ✅ 新增 | 只改文案字段与关卡块。三条硬保证写成断言：**原始 area 字节级校验**（实测零 diff）、**块外字节完全一致**、**条目一一对应**。幂等（重跑 md5 不变） |
| `tools/test-china-provinces.js` | ✅ 新增 | 41 项断言，5 组：条目字段完整性 / 简称与省会 / 合规只读断言 / 关卡结构 / 港澳台面包屑完整性 |
| 省会（首府）机器推导 | ✅ 已完成 | 用 `tools/lib/mca-tree.json` 推导：省级码 `xx0000` → 省会 `xx0100`，31 个有下级的省级单位全部命中。把"我记得"变成"可复现的推导" |
| CI 接入 | ✅ 已完成 | `test-china-provinces.js` 已加入 e2e 离线套件 |
| 众包入口配置 | ✅ 已完成 | `MAP_PUZZLE_CONTRIB.repo` 填为 `Michael-YE666/map-puzzle`，Issue 提交面板现在真的可用 |

### 📄 文档类

| 项 | 状态 | 说明 |
| --- | --- | --- |
| `docs/部署到Cloudflare.md` | ⏳ 待人工终审 | **这份文档是因为我犯错而重建的**：原稿写在 `deploy/`（`.gitignore` 排除的可丢弃产物目录）里，被 `rm -rf deploy` 删掉，且从未进过 git。已按记忆重建并移到 `docs/`，**需人工核对有无遗漏** |
| `CHANGELOG.md` | ✅ 新增 | 本文件 |
| SOP 坑 #42 | ✅ 新增 | 记录"把文档放进可丢弃的产物目录"这个错误 |
| `china.data.js` LEVELS 注释 | ✅ 已完成 | 说明为何 7 关、东北 3 块是地理客观限制、该分区是国内通行分法非官方标准、如何调整 |
| `fill-china-provinces.js` 头部说明 | ✅ 已完成 | 说明只改两类字段、area 字节级校验、改分区只需改 LEVELS 表后重跑 |
| `deploy/` 与 `deploy-sichuan/` | ✅ 已重建 | 按新源文件重新生成（20MB / 1.3MB），二者都是可丢弃产物，不入 git |

---

## ⏳ 待人工终审清单

> 以下三项**机器无法判定对错**，必须人工确认后才能视为完成。

### 1. 部署文档完整性

`docs/部署到Cloudflare.md` 是**按记忆重建**的（原稿被误删且无 git 记录）。
需要核对：

- [ ] 1000 文件上限那两条解法（网页拖拽传单文件 / Wrangler 传完整版）是否与实测一致
- [ ] Cloudflare 界面步骤是否与你实际操作的界面一致
- [ ] "部署成功后打不开"的四步排查顺序是否有遗漏
- [ ] 是否还有你实测过但文中没写的坑

### 2. 科普文案事实准确性（34 条）

**红线要求：事实类内容（数字、地标、时效表述）不得由机器自行修改。**
本轮 34 条文案已写入 `js/maps/china.data.js`，但**事实部分需要人工终审**。

可信度分层：

| 部分 | 可信度 | 依据 |
| --- | --- | --- |
| 简称 + 省会（首府） | **高** | 行政区划简册口径；省会已用官方区划树**机器推导并逐条核对** |
| 地标 | 中 | 取自公认的代表性景点 |
| 冷知识 | **需重点核对** | 见下方"重点核对项" |

**重点核对项（数字类与时效类）**：

- 北京中轴线「2024 年列入世界遗产」—— 时效
- 新疆「面积约 166 万平方公里，约占全国六分之一」—— 数字
- 广东「海岸线总长约 4100 公里」—— 数字
- 甘肃「东西跨度约 1600 公里」—— 数字
- 内蒙古「东西直线距离约 2400 公里」—— 数字
- 三沙市「陆地面积最小的地级市」—— 排名类
- 北京「中轴线全长约 7.8 公里」—— 数字
- 贵州「山地丘陵占九成以上」—— 比例

**另外两项请一并确认**：

- [ ] **台湾省表述**：现写「简称台，省会台北，中国第一大岛」。该条无法用区划树机器推导（官方树里没有台湾的下级节点），属人工口径
- [ ] **香港/澳门未写"省会"**（它们是特别行政区，本身即城市）—— 机器已断言，但口径请你确认

修改方式：改 `tools/fill-china-provinces.js` 的 `CONTENT` 表 → 重跑脚本。
`area` 不受影响（脚本对它做字节级搬运）。

### 3. 关卡分区方案

**红线要求：本轮不得调整分区核心结构，只可补注释。** 已遵守。

分区**非官方标准**（国家未颁布地理分区定义），当前采用国内地理教材最通用的七分区：

```
东北 3：辽宁、吉林、黑龙江
华北 5：北京、天津、河北、山西、内蒙古
华东 7：上海、江苏、浙江、安徽、福建、江西、山东
华中 3：河南、湖北、湖南
华南 6：广东、广西、海南、台湾、香港、澳门
西南 5：重庆、四川、贵州、云南、西藏
西北 5：陕西、甘肃、青海、宁夏、新疆
```

需你确认：

- [ ] **东北 3 块、华中 3 块**是否接受（这是地理客观限制：东北就三个省，凑 5 块只能把内蒙古东部拆进来，会破坏"每块 = 一个省级行政区"的完整性）
- [ ] **台湾归华南**是否认可（也可单列）
- [ ] **福建、江西归华东**是否认可（有版本把福建算华南）
- [ ] **重庆归西南**是否认可

调整方式：改 `tools/fill-china-provinces.js` 的 `LEVELS` 表 → 重跑脚本 → 跑 `node tools/test-china-provinces.js` 验证"不重不漏 + 每关 3~7 块"。
**注意**：关卡 id 必须保持语义名，不要改成 `l1`/`l2`，否则会被自动重排工具覆盖。

---

## ⚠️ 本轮的一个已知失误

`deploy/README-部署到Cloudflare.md`（数百行部署说明）**被我删除且无备份**。

- **原因**：把文档放进了 `.gitignore` 排除的 `deploy/`，而重建流程的第一步是 `rm -rf deploy`
- **修法**：文档移到 `docs/部署到Cloudflare.md`（受版本管理）；SOP 新增坑 #42
- **状态**：⏳ 内容已按记忆重建，**完整性待人工核对**（见上方终审清单第 1 项）

---

## 测试基线

| 套件 | 数量 |
| --- | --- |
| 离线检查（9 项） | 2094 |
| 城市回归 · 成都 | 89 |
| 引擎功能 · tiny-city | 77 |
| 多地图冒烟 · 363 张地图 | 13926 |
| **合计** | **16186** |

重跑全量：`node tools/e2e-test.js 2>&1 | tail -22`（约 9 分钟）
快速自检：`node tools/e2e-test.js --only-suite=offline 2>&1 | tail -12`
