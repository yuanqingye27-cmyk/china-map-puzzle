# =====================================================================
# Cloudflare Pages 重定向模板
# ---------------------------------------------------------------------
# 用法：复制到部署根目录（和 index.html 同级），文件名就叫 `_redirects`。
# 语法：<来源路径>  <目标>  [状态码]
# =====================================================================

# ---- 短链接：给"发出去好看、好记"用（老师发给学生特别有用）----
# 例：把 https://xxx.pages.dev/sichuan 变成 https://xxx.pages.dev/?map=sichuan
/sichuan       /?map=sichuan        302
/chengdu       /?map=chengdu        302
/china         /?map=china          302
/china/exam    /?map=china&mode=exam      302
/china/teach   /?map=china&mode=teach     302
/china/duel    /?map=china&mode=duel      302
/sichuan/exam  /?map=sichuan&mode=exam    302

# ---- 兜底：未知路径不显示 Cloudflare 自带的 404，直接回到游戏首页 ----
# ⚠ 这一条会让所有拼错的路径都返回 index.html（状态码 404 但内容是页面）。
#   如果你更希望"未知路径老老实实报 404"，把这一行删掉。
/*             /index.html           404


# =====================================================================
# 说明
# ---------------------------------------------------------------------
# · 状态码用 302（临时）而不是 301（永久）：以后想换目标时，
#   301 会被浏览器和 CDN 长期记住，很难改回来。短链接没有 SEO 需求，
#   用 302 更安全。
# · 这里**不需要**做 SPA 的 history fallback —— 本站是单页 + 查询参数
#   (?map=xxx)，所有状态都在 URL 查询串里，没有客户端路由。
# =====================================================================
